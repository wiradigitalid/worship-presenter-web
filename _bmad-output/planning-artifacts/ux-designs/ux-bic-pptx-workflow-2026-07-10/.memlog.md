---
topic: bic-pptx-workflow UX
updated: 2026-08-02T09:26
---

- (decision) Fast path UX design for Web SPA operator interface and presentation views, heavily utilizing standard Shadcn/Tailwind.
- (decision) AD-UX-1: Clean, high-contrast, uncluttered UI meant for weekend operations. Utilize simple Tailwind/Shadcn defaults with minimal custom brand colors.
- (change) Update run 2026-07-29: as-built ratification, NOT a redesign. Trigger was drift found by the implementation-readiness assessment: both spines last touched 2026-07-19 and missed two shipped surfaces.
- (override) SHAPE CORRECTION: DESIGN.md previously carried a surfaces/route table and a component-inventory list. Under the bmad-ux contract DESIGN.md owns visual identity per the Google Labs design.md spec; IA and surfaces belong to EXPERIENCE.md. Content moved rather than deleted. The earlier suggestion to keep DESIGN.md as a 'surface register' was wrong and is withdrawn.
- (decision) AD-UX-2 [ADOPTED]: the palette is achromatic by shipped reality, not by taste. Every token in src/app/globals.css is oklch(L 0 0) -- chroma exactly zero -- except --destructive and a stray dark-mode --sidebar-primary (oklch 0.488 0.243 264.376, an unused shadcn leftover; there is no sidebar, the app uses Header.tsx). AD-UX-1's 'minimal custom brand colors' is literally zero brand hue. Ratified from code, not invented.
- (decision) AD-UX-3 [ADOPTED]: typography is Geist Sans / Geist Mono via next/font/google, verified in src/app/layout.tsx. --font-heading aliases --font-geist-sans, so there is no separate display face. Radius ramp derives from a single --radius: 0.625rem (10px) -> sm 6 / md 8 / lg 10 / xl 14 / 2xl 18 px.
- (assumption) OPEN GAP (do not claim as working): dark mode is unreachable as shipped. globals.css defines a full .dark block (33 tokens) behind @custom-variant dark (&:is(.dark *)), but no ThemeProvider is mounted anywhere -- next-themes is imported only by src/components/ui/sonner.tsx for toast theming. Nothing ever adds .dark to <html>, so the entire dark palette is dead code. The previous DESIGN.md claimed 'Dark/light via existing theme tokens', which was false.
- (assumption) OPEN DEFECT: src/app/layout.tsx still exports the create-next-app metadata -- title 'Create Next App', description 'Generated by create next app'. The browser tab and any bookmark of the production hub read 'Create Next App'. Recorded here as UX truth; fixing it is a one-line product change, not a UX decision.
- (change) Surfaces added to IA that shipped after the 2026-07-19 snapshot: /services/new (Epic 14 worship web input) and /admin/artifacts (Epic 16 canvas editor). /login was previously described only as 'Auth'. Verified by enumerating src/app/**/page.tsx: /, /login, /announcements, /services/new, /services/[id], /services/[id]/slideshow, /services/[id]/present, /services/[id]/present/projector, /admin, /admin/artifacts.
- (decision) Component reality (src/components): shadcn ui/ = button, card, dialog, popover, sonner (five only). App components = Header, LogoutButton, HymnNumberAutocomplete, ImageUploadField, ImageFieldPreview, SlideView, SlidePreviewList, artifacts/ArtifactSlide, admin/ArtifactEditor. Detailed per-surface UX specs stay in SPEC companions (form-fields.md, edit-page-chrome.md, slide-kinds.md) and are referenced, not duplicated.
- (decision) status set to 'final' (not 'as-built') on both spines deliberately: bmad-ux Create mode scans ux_output_path for folders whose DESIGN.md status is NOT final and offers to resume them. Leaving 'as-built' would make this completed run look unfinished and trigger a spurious resume offer on the next run. The as-built honesty note lives in the body instead.
- (assumption) NEW OPEN ITEM from the gate: computing the contrast ratios the rubric demands showed muted-foreground on muted is approx 4.4:1 -- BELOW WCAG AA (4.5:1) for normal text -- and on background approx 4.7:1, only narrowly passing. This is shadcn's default token and carries secondary text throughout. Values are Oklab-derived estimates (Y approx L^3), NOT tool-measured; both must be confirmed with a contrast checker before any accessibility claim. A single token darkening would fix it globally.
- (assumption) HONESTY BOUND on the state coverage added during gate fixes: per-surface states (lost sync, save rejected, last-admin refusal, empty plan) are the DESIGNED behavior distilled from the architecture invariants. Whether the shipped code implements each was NOT verified. Recorded as EXPERIENCE.md Open Item 6 rather than asserted as working; verification belongs to the implementation-readiness assessment.
- (question) OUTSTANDING: doc_standards polish passes (skill:bmad-editorial-review-structure then skill:bmad-editorial-review-prose) are part of the Finalize sequence and are NOT opt-in. Not yet run.
- (override) F4-1 FIXED (readiness assessment Step 4): Key Flows rewritten to use the PRD's protagonists verbatim per the bmad-ux mirror-source-names rule -- Sari, Bimo, Elen. The invented Yohana/Yosef are gone. 'Yosef' additionally had to go because it collided with a real predecessor's given name found during the PII sweep. Pronouns follow the PRD: Bimo he, Elen she, Sari she. Flows now carry explicit UJ labels.
- (change) Flow set expanded 5 -> 7 to close F4-3 and F4-4: UJ-5 (create by hand) promoted from a sub-branch to Flow 2; UJ-3 (Saturday Telegram correction) given Flow 4, scoped to the reviewer-facing half since the confirmation exchange belongs to the agent. UJ-1 documented as deliberately outside this product's surfaces -- Sari never opens the hub.
- (change) F4-5 FIXED: three behaviors whose FR/NFR already specified them had no UX owner. Added (a) FR-16 projector blanking as a full state -- black at any time and restore, deck position preserved, projector window not lost, scripture overlay undisturbed, blanked state indicated on the operator view, reload-while-blanked comes up blank -- plus Flow 3 step 5 and Branch 3a; (b) NFR-5 general unmapped-input channel as a cross-cutting state, explicitly not hymn-only; (c) NFR-2 deck-generation progress, with double-fire prevention.
- (change) Update run 2026-07-30 (bmad-ux Update mode). Trigger: Correct Course 2026-07-29 routed readiness findings F4-1/F4-3/F4-4/F4-5/F4-6 plus a dead-code correction here. SCOPE CORRECTED ON READING THIS MEMLOG: F4-1, F4-3, F4-4 and F4-5 were already closed by the 2026-07-29 run (entries above), and F4-7 was closed by Correct Course, which added EXPERIENCE.md to epics.md inputDocuments. Real remaining scope: the dark-mode dead-code claim, F4-6, and verifying the per-surface states this file admitted were unverified.
- (override) CORRECTION, AD-UX-4: the claim that the dark palette is dead code is FALSE and was carried by three artifacts (DESIGN.md frontmatter + Open Item 2, the readiness report, and this memlog entry of 2026-07-29). Verified against src/ at 2026-07-30: PresenterOperator.tsx:449 and SlideGridDialog.tsx:176 both pin className dark on their own wrappers, and globals.css:5 @custom-variant dark (&:is(.dark *)) matches any descendant, so the palette renders today in the two surfaces used while a service is running. What is actually missing is OPERATOR CHOICE, not reachability. The earlier grep looked only for a mounted ThemeProvider and concluded from its absence; that inference was wrong.
- (assumption) NEW OPEN ITEM, and a documentation defect this file caused: LOST SYNC IS SPECIFIED BUT NOT SHIPPED. EXPERIENCE.md carried it as a cross-cutting state and Flow 3 Branch 3a asserted the presenter surfaces lost sync immediately. Verified false at 2026-07-30: there is no liveness detection of any kind. BroadcastChannel is fire-and-forget with no delivery signal, present-channel.ts defines no heartbeat or pong message, and projectorRef.current.closed is consulted only inside openProjector, i.e. only if the operator happens to click Open projector again. The only surfaced projector state is projectorBlocked, which is the popup blocker, not a lost window. An operator can therefore advance the deck for the rest of a service with nothing on the second screen and no indication. Routed to a story rather than left as prose.
- (decision) AD-UX-5 [ADOPTED], closing F4-6: the congregation-facing deck gets a STATED boundary rather than a new specification. DESIGN.md owns operator chrome; the Artifact Registry payload plus its SPEC companions own slide interiors; both were already true and both are now written down as a boundary instead of appearing as a silence. What is NOT owned is named explicitly: is this readable from the pews. No artifact answers it, no test can, and the only control is the pre-launch projector inspection carried as an owner action item in sprint-status.yaml. Deliberately NOT invented here: minimum type sizes, contrast floors or a readability spec for projected slides. Per the bmad-ux capture rule, geometry that arrived from a real source deck is evidence and inventing numbers over it would replace evidence with taste.
- (decision) AD-UX-6 [ADOPTED]: every Open Item in both spines now names the story key that owns it, so the pair stops being an orphan defect list. DESIGN.md: muted-foreground to 17.2, dark-mode choice to 17.1, metadata to 17.3, dead chart/sidebar tokens left unowned on purpose as harmless. EXPERIENCE.md: unsaved canvas to 17.4, lost sync to NEW story 17.5, accessibility left unowned because it needs a scoping decision before a story can be written. This is the loop Correct Course opened on 2026-07-29 and did not close: findings were routed to skills, not to keys.
- (change) Update run 2026-07-30 (bmad-ux Update mode, second run today). Trigger: Reviewer Gate validation-2026-07-30-validate-b routed six items against EXPERIENCE.md (C7, F-11, cross-document F-2). Scope was reconciliation against ARCHITECTURE-SPINE.md only -- no redesign, no spine/SPEC/epics/sprint-status/src edits, and .work/inter-agent/** untouched per the AGENTS.md host-boundary rule for Claude Code. All six items closed.
- (override) CORRECTION, the CRITICAL item: EXPERIENCE.md Venue and Projection Constraints stated the global-and-immediate registry rule as current design and cited a bare AD-4 for its reversal. Two independent errors, both fixed. (a) TIMING: the reversal is decided, not scheduled -- AD-16 was recorded 2026-07-30 and is [TARGET], so the rule is settled while the code is not; the bullet now states both halves, shipped behavior first. (b) CITATION: AD-16 supersedes the global-across-services clause of AD-14, never AD-4, which is LiveServer durable paths. AD-14's own text warns against exactly this confusion and the wrong number survived two prior review rounds.
- (change) Flow 5 rewritten for AD-16. Climax now states both halves under the file's own marker: shipped Save reaches every service, and marked designed-not-shipped that it reaches the live registry only. New step 5 is the Sync Artifact beat carrying the three properties AD-16 fixes -- admin-only, updated_at precondition per AD-6, replaces structure and never the operator's entered data. New Branch 5b is the stale-snapshot state, with the matching per-surface state added under /services/[id]. Step 6 now names the two Reset surprises so they surface where Bimo meets them.
- (decision) AD-UX-7: the IA table keeps its ten route surfaces and gains two subsections rather than growing rows for things that are not routes. (1) /admin/artifacts is documented as three authoring surfaces in one route -- ordered registry list (CAP-2), free canvas for general rows only (CAP-3, AD-22), bounded configuration for songset-* rows with exactly two backgrounds plus font style and size and no canvas (AD-22) -- each with its own shipped/not-shipped status and owning story. The word global is retired per AD-16. (2) A Sub-surfaces table carries the three surfaces with behavior but no route: slide transition on /admin (shipped), the presenter live override (shipped), and the four per-slot hymnal bindings (AD-19, CAP-8, not shipped). This preserves the every-surface-is-landed-on-by-a-journey assertion, which adding route rows for non-routes would have broken.
- (change) Two shipped surfaces documented for the first time, both verified in src/ on 2026-07-30: admin/TransitionSettings.tsx (one app-wide settings.slide_transition, PUT /api/admin/settings, confirmation naming the projector and the next generated PPTX) and the presenter live override (badged Live only - not saved, broadcast over the AD-10 channel, with an in-words notice whenever live and saved styles differ). Landed in EXPERIENCE.md as IA sub-surface rows, Component Patterns rows, two states (/admin transition save failed, presenter live-differs-from-saved), and new Flow 8 so both have a journey; DESIGN.md gained the two matching Components rows to keep the paired-table contract true. Presenter state count in the per-surface table corrected four -> five.
- (change) Open Items 5 and 6 added, receiving the two affordance questions the spine routes to EXPERIENCE.md and its own Deferred says have not been received. 5: whether and how a stale snapshot is surfaced (owner Story 20.8), with the three constraints that bound the answer -- Sync is admin-only so an operator may see staleness and request a sync but never perform one, a stale service is correct rather than broken so it cannot borrow the destructive vocabulary, and 'nothing' is a legitimate answer only if chosen. 6: Reset reverts a rename (AD-17 label + AD-11 Reset), and its second face -- an administrator-authored row has no seed and exposes no Reset at all, so one list holds two rows with different verbs (owner Story 20.3).
- (assumption) NEW FINDING, out of the assigned six and against DESIGN.md rather than EXPERIENCE.md: amber is a second hue that ships with no token. Six warning affordances across five files use Tailwind amber utilities at five different shades -- date-collision warning and two more in CreateForm.tsx, hymn-lookup-unavailable in HymnNumberAutocomplete.tsx, the scripture badge in SlidePreviewList.tsx, the projector-blanked border, a scripture-lookup error, and the live-transition-override notice in PresenterOperator.tsx. DESIGN.md's Colors section claimed destructive was the only chromatic token and its Avoid list says the palette has no color to spare for encoding state, while 'warning' is already a semantic color in the shipped product. Found while documenting the presenter override, which is one of the six sites, so it could not be left unstated. Recorded as a Colors bullet plus DESIGN.md Open Item 4 with no owner and the reason why -- tokenize warning or remove it is a product decision -- and none of the six shades has been contrast-measured, two of them in the presenter surface Open Item 2 already flags as never measured. Existing chart/sidebar dead-token item renumbered 4 -> 5.
- (override) COUNT CORRECTION to the sweep entry above, appended rather than edited because this log is append-only. Measured after the edits: EXPERIENCE.md carries 71 AD citations over 17 distinct decisions (AD-1, 4, 5, 6, 7, 8, 10..17, 19, 22, 23 -- AD-4 appears once and only as the named correction), not 61 over 21. DESIGN.md now carries 5 over 4 decisions (AD-7, AD-15, AD-16, AD-20); the '2 in DESIGN.md' figure was the pre-edit count and the three additions are the forward note about AD-20 and AD-16 moving which-slides-exist. The sweep conclusion is unchanged: exactly one wrong number, the bare AD-4.
- (decision) (by bmad-ux-update, Gap 3 / R9) Registry row chip shows the KIND — [general], [song-set], [announcement] — never the entry key. Grounds, all pre-existing: AD-19 fixes that song-set names the kind and slot keys (songset-bt-open et al) are server-owned binding vocabulary; CAP-5 displays '[kind] label' and its success clause makes the label the only administrator-editable part; this file's own voice rule bars system vocabulary from administrator surfaces, and a raw hyphenated key in a chip is exactly that. Consequence recorded with the decision: the four SongSet rows share one chip and are told apart by label, and since rename is allowed, the bounded-configuration surface must state the row's slot identity read-only in worship vocabulary — otherwise a rename orphans the row's liturgical identity. Owners: Story 20.3 (list chip), Story 20.7 (read-only slot statement).
- (change) Update run 2026-08-01 (bmad-ux Update mode). Trigger: the epic-24 sprint action item, which had accumulated FOUR clauses against EXPERIENCE.md and asked for one run rather than four -- plus the epic-21 item that is its clause (1). Scope held to those four. No redesign, no spine/PRD/epics/src edits. Both spines updated; sprint-status.yaml closed in the same change set per the AGENTS.md rule.
- (override) CORRECTION found while closing clause 1, and the routing item did not know about it. The presenter state row said lookup is unavailable when the corpus was never imported AND that 'the presenter says so instead of returning empty results'. BOTH halves are now false. The ops step is gone -- Story 21.1 ships data/bible/kjv.json and seeds from zero on first boot (AD-25), so 'never imported' is not a state a fresh clone reaches; what survives is an unreadable/absent file or a translation installed holding no verses, which /api/scripture still answers deliberately with 503 naming the file and npm run corpus:verify (route.ts:18-27). And the presenter does NOT say so: PresenterOperator.tsx:432 collapses every non-404 into a flat 'Lookup failed', so the message reaches the operator on the two service forms (CreateForm.tsx:300, EditForm.tsx:316) and nowhere on the surface used during a service. One fault, two answers, and the room-facing half is the mute one. Row rewritten as 'Corpus empty' quoting the old text; owner Story 21.5.
- (decision) AD-UX-8 [ADOPTED], closing clause 2: NEITHER FR-24's corpus picker NOR FR-25's locale switcher is a route, so the IA table does not move. The sprint item posed this as the open question and nothing else could answer it. The two corpus defaults and ui_locale follow the shipped per-concern settings pattern (RetentionSettings.tsx, TransitionSettings.tsx) onto /admin; the translation control lives in the Presenter panel beside the lookup it scopes. Three rows join the Sub-surfaces table (count 3 -> 6). Recorded with the reason rather than the verdict: what WOULD have moved the IA table is a browse-the-corpora surface of its own, and FR-24 asks for the opposite -- reachability from where the choice is made, not a place to go first. Owners: 21.3, 22.3, 24.1.
- (change) Clause 3 rendered: 'Scripture reference field' added to Component Patterns beside HymnNumberAutocomplete, with a paired row in DESIGN.md Components to keep the two-tables-cover-one-set contract true. The single-field choice is recorded as load-bearing rather than cosmetic, per the routing item: a book picker was recommended and the owner chose one field, which keeps paste working and lets the rundown reuse the same matcher in the other scope (AD-28) instead of needing a second mechanism. Measured while writing it -- three plain inputs today and no component: PresenterOperator.tsx:696, CreateForm.tsx:584, EditForm.tsx:615. DESIGN.md's half adds one thing that is genuinely its own: this control renders on PresenterOperator's pinned-dark wrapper as often as on the light surface, so it must be measured on both, unlike every other row in that table.
- (assumption) Clause 4 received as EXPERIENCE.md Open Item 8, NO OWNER -- a question before it is a story, and it blocks neither Story 21.4 nor 21.5. Under AD-28 the rundown matcher spans every installed translation and an ambiguous reference is refused rather than guessed; the undesigned consequence is that adding a corpus is not purely additive on that surface. NFR-5 makes the unmapped line visible; the EXPLANATION is what is missing, and the two are different affordances -- a reviewer reads 'could not be mapped' as a typo, because that is what it has always meant. THREE BOUNDS ADDED that the routing item did not carry: (1) the ambiguity is INTRA-translation and live on the single corpus shipping today (Phil -> Philippians/Philemon, Jo -> five books), so an explanation written only for the cross-translation case would be absent exactly where it is already needed; (2) the colliding candidates are known at the moment of refusal, so naming them is cheap and is probably most of the answer; (3) the operator cannot act on the corpus -- install is not an operator surface -- so it may inform a choice but must not read as an instruction to fix an install.
- (change) Flow 9 added, and it is not decoration: this file asserts every surface is landed on by a journey, which is a testable IA invariant, so adding three Sub-surface rows without a flow would have broken it. Flow 9 lands the admin language settings and the ui_locale switcher; Flow 3 step 4 was rewritten to land the presenter translation control and the new reference field in the same beat Elen already had. Flow 9's climax is FR-24's own must-work case -- an Indonesian service singing one English hymn, crossing the language without changing a setting and without changing the default for anyone else. Branch 9a carries Open Item 8 so the un-resolving reference has a place an operator meets it. Every beat marked designed-not-shipped once at the top rather than per step. Like Flow 8 it has no PRD user journey, and says so.
- (override) PRE-EXISTING CITATION ROT repaired, found while renumbering: the Venue & Projection bullet cited 'Open Item 5' for the stale-snapshot affordance. It is Open Item 6 -- the 2026-07-31 insertion of the toast-channel item pushed it from 5 to 6 and this pointer was left behind. Item 5 is session revocation, so the citation resolved to a REAL entry about something else, which is the failure a dangling-reference check cannot catch and the class project-context.md warns about in both directions. Fixed in place with the reason stated. Related discipline held in the same edit: Open Item 7 (Reset reverts a rename) was NOT renumbered even though the new item wanted that slot, because ARCHITECTURE-SPINE.md:442 and Flow 5 step 6 both cite it by number; the new item took 8.
- (change) bmad-ux Update run, 2026-08-02, triggered by Story 24.1 shipping (PR #21). Four statements in these spines had become false about code that exists: EXPERIENCE.md:82 called the UI-locale switcher 'designed, not shipped' and cited 'lang="en" hard-coded at layout.tsx:31'; :81 said settings.ts holds RETENTION_KEY and SLIDE_TRANSITION_KEY only; Flow 9's preamble said 'every beat below is unbuilt'; and DESIGN.md's Components table had no row for the switcher while EXPERIENCE.md:95 states the two tables cover the same component set. All four corrected against the shipped code.
- (decision) Flow 9's per-beat status replaces the blanket marker. One marker covering five beats is how a document keeps claiming something after it has changed: step 2's interface half shipped while steps 1, 3, 4 stayed unbuilt, and a single preamble could not say that. Steps now carry their own marker; the heading names the split.
- (decision) CONFLICT SURFACED AND RESOLVED: Flow 9 step 2 read 'Language sits in its own block', which reads as one card holding both language axes -- and Story 24.1 AC-6 decided the opposite, that per-concern means one component per setting (UiLocaleSettings.tsx beside TransitionSettings.tsx), because a shared card is a file Stories 21.3 and 22.3 would each have to rewrite. The prior wording was never a decision for one card, but it would have been read as one by the next story. Both the step and the Language-settings sub-surface row now say adjacency, not one card, explicitly.
- (change) NEW FINDING, added to Accessibility Floor rather than found later: between Stories 24.1 and 24.2 the hub is bilingual in one admin block, so setting ui_locale=id makes <html lang> declare Indonesian while ~100-150 strings are still English literals. A screen reader then reads English text with Indonesian phonetics -- a sharper misstatement than the honest hard-coded 'en' it replaced, not a softer one. AC-12 already recorded the bilingual interim state as expected; its accessibility cost was recorded nowhere. It is the only line on that list with a dated owner: it closes when 24.2 lands.
- (decision) Reviewer Gate SKIPPED for this Update, owner's call. Reasoning offered and accepted: the change set is factual correction of four rows plus two paired component-table rows, not new design decisions, and the shipped behaviour behind it had already been verified independently in the same session (build route table, 416-test suite, tsc, eslint at baseline, and served markup checked with curl on /login, /, and the projector). The rubric walker and the adversarial lenses are built for runs that invent; this one ratifies.
