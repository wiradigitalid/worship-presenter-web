# Sprint Status — narrative record

Everything here was moved verbatim out of `sprint-status.yaml`, which BMad defines as a
flat `key: status` tracking file (`bmad-sprint-planning/sprint-status-template.yaml`).
No text was summarised, reworded, or dropped. This file is a record, not a contract:
nothing here binds an implementer. Where a statement here still binds, its home is the
architecture spine, a SPEC, or the story — not this file.

---

## Header changelog

The running commentary that had accumulated in the file header.

```text
(bc487b3) with both checks green. Epic 20 stays `in-progress`; Story 20.2 (the base_type
seven-to-three collapse) is the direct successor and is still `backlog`. What a reader of this
header needs: 20.1 did NOT do the base_type collapse — that stays in 20.2 — and 20.1 does NOT
close AD-20, because five transitional SongSet groups still originate outside AD-19's
ordered-entry model until Story 20.7 lands its closed set and form bindings; 20.7 also owns
deleting song1Number..song4Number in that same change set. The story's gate held:
ARCHITECTURE-SPINE.md was not edited and no [TARGET] tag was flipped from inside its change set,
so the four Deferred statements it staled still await a bmad-architecture Update run.
ONE THING THE CLOSE DOES NOT COVER, recorded because the owner closed with it stated rather than
hidden: the FINAL state after fix round 2 was verified only by the coordinator and never read by
an independent reviewer. See its row.
PRIOR ENTRY # 2026-08-06 (Story 17.6 is `done` — CLOSED BY THE OWNER this day. The
owner's combined inline + toast channel rule is recorded once in EXPERIENCE.md, the
two sonner rows and Open Item 4 read decided-not-wired, and the forward owner
17-9-toast-channel-wiring is registered here and in epics.md, dependent on an
operator-scoped route segment existing rather than on Story 17.7 delivering one.
AC-9 — the story's only blocking obligation — was CLOSED ON EVIDENCE by a
bmad-architecture Update run that repaired ARCHITECTURE-SPINE.md's stale Deferred
bullet in the same change set. All ten ACs carry a recorded disposition and none
is open. Committed as ba222af, PR #35, both checks green.
ONE THING THE CLOSE DOES NOT COVER, recorded because the owner closed with it
stated rather than hidden: the FINAL state of that change set was never read by an
independent reviewer. The trio reviewed the pre-fix tree; two fix rounds then closed
four findings AND found three sites no reviewer had flagged, and only the coordinator
verified the result. The coordinator recommended one more review pass; the owner
elected to close instead. If a later round finds something here, this is why.
See its row.
Also: Story 17.5 implemented, both code-review rounds closed,
sitting at `review` for the owner's `done` call; its row carries the review and
record-repair entries. Stories 17.3 and 17.4 are now `done`. Story 23.1 context
created and ready for development; Epic 23 active. Story 23.2 remains backlog,
waiting on Stories 23.1 and 22.3. This header had itself gone stale at 2026-08-03,
naming 17.3 as ready-for-dev and 17.4 as newly created — the pre-existing defect
Story 17.3's review deferred; repaired here rather than left to rot further, since
this change set edits the file it dates.)
Distinct from `done` (nothing shipped) and from `backlog` (nothing is waiting).
Added 2026-07-30 for Epic 19, whose goal architecture AD-20 met by another route.
Correct Course (sprint-change-proposal-2026-08-08.md) gave NFR-3 its first
owner. Story 20.1's authored seed shipped four liturgical lyric rows whose readability nothing
checks: the PPTX fit estimator pins contentWidth: 0 and counts only authored newlines, so
wrapping never forces a shrink there, while the web path measures glyphs and does. Live Preview
— the surface PRD §8 names as the Admin's eye — IS the web renderer, so it cannot reveal a
divergence in the PPTX that NFR-1 makes load-bearing. TWO STORIES ADDED, both backlog:
20-9-readability-guarantee and 20-10-font-set-is-closed (proposes AD-30). PRD amended at three
sites for NFR-3 and one for NFR-7; both epics.md NFR rows rewritten; the spine's Deferred bullet
corrected — it carried TWO DEAD CITATIONS (prd.md:563 and epics.md:52 are blank lines) and
claimed a hardcoded fontSize where a shrink-to-fit policy exists.
THE OWNER'S 2026-07-30 MANUAL-AUTHORING DECISION IS NOT REOPENED — what changed is that the
mechanism it named is now split by path, because no Admin authors the shipped seed.
ONE THING THIS DOES NOT COVER, recorded rather than hidden: no PPTX was generated and opened in
PowerPoint. The finding rests on the source (contentWidth: 0 is structural) and on the code's own
contradictory comments, render-model.ts:250 vs pptx.ts:236. Story 20.9 AC-3 settles which is true.
PRIOR ENTRY # 2026-08-07 Story 20.1 done — owner close after PR #37 merged (bc487b3), both checks
green; epic-20 stays in-progress and 20-2-three-slide-kinds is the next unit. The close carries
one stated gap (final post-fix state read only by the coordinator) and one forward debt (AD-20
still open, owned by Story 20.7) — both in its row.
PRIOR ENTRY # 2026-08-07 Story 20.1 context created (bmad-create-story) and Epic 20 opened.
20-1-ordered-registry was ready-for-dev; epic-20 backlog → in-progress. The story makes two
calls the epic left open and both are recorded in its row below: AD-21's data version counter
lands in 20.1, and the base_type seven-to-three collapse stays in 20.2. It also carries a GATE
— four spine statements go stale when it lands and repairing them (including any [TARGET] tag
flip) is a bmad-architecture Update run, never an edit from inside the story's change set.
AD-20 is explicitly NOT closed by 20.1: five transitional SongSet entries remain, owned by Story 20.7.
PRIOR ENTRY # 2026-08-05 Story 17.6 implemented (bmad-dev-story) same day, decision ratified,
forward owner 17-9-toast-channel-wiring registered, back to `review`; AC-9's spine repair
(bmad-architecture Update run) has since landed and was closed on evidence in a same-day
FIX round — see its row. Earlier the same day: Story 17.5
implemented (bmad-dev-story) and back to `review` — see its row for verification numbers and
the ten injected-defect proofs. Earlier the same day: its AD-29 gate cleared by a
`bmad-architecture` Update run (spine census 28 → 29, nothing renumbered), and before that,
Story 17.3 code review closed, status done.
```

---

## Story and epic rows

Keyed by the `development_status` key the narrative was attached to.

### `epic-14` — done

Closed 2026-07-29 by Correct Course: all six stories were already `done`, so `in-progress` contradicted this file's own definition ("done: All stories in epic completed"). Realizes FR-11 and FR-11b.

### `epic-16-retrospective` — done

Stories 16.2-16.5 shipped without story files, so no acceptance criteria were ever authored for them. The four keys are retired here rather than backfilled with AC written to match already-shipped code (AC that cannot fail is not a verification instrument). Delivery contract: spec-16-2-artifact-pipeline-completion.md (this folder) Scope narrative stays in ../planning-artifacts/epics.md "Epic 16". Decision: ../planning-artifacts/sprint-change-proposal-2026-07-29.md

### `epic-17` — in-progress

Created 2026-07-29 from the readiness assessment's product defects, through Epic → Story rather than inline patching. Repaired 2026-08-05, because the rows below had moved four times underneath it: Stories 17.1, 17.2, 17.3, 17.4 and 17.8 are `done`; 17.5 is `review` (implemented, two code-review rounds closed, awaiting the owner's `done` call — see its row). Re-repaired 2026-08-06, because 17.6 moved again the day after: 17.6 is now `done`, CLOSED BY THE OWNER (decision ratified, no code shipped, AC-9's spine repair landed and closed on evidence — see its row, which also records what the close did not cover); 17.7 remains `backlog`; 17.9 (registered by 17.6) is `backlog`, dependent on an operator-scoped route segment existing, which 17.7 does not itself promise.

### `17-1-reachable-dark-mode` — done

2026-08-01 CLOSED by the owner. Four review rounds, 15+19+25+12 patch items, and the one blocking decision item all closed; AC-4 scoped in writing to the token guarantee with its shell half owned by Story 17.7. The four guard narrownesses that had no owner are now Story 17.8, so deferred-work.md carries ZERO `owner: unassigned` entries — that, not the absence of latent holes, was the condition for closing. PRIOR ENTRY, at review # 2026-08-01 LATEST, bmad-architecture Update run (AD-24 closure-gate ceiling repair): **the round-4 decision item is CLOSED, so the stated blocker on `done` is gone — but the `done` call is the owner's, and this row does not make it.** The stale ceiling bullet and sub-bullet (ii) are rewritten, AD-24's rule clause is amended IN PLACE from "two sets" to the FOUR hardcoded lists the gate actually has, and the `projector/page.tsx:71` citation is repaired to `:85` at both sites. Nothing renumbered, no AD added; 24 AD headings before and after; lint_spine 0 findings; theme-chrome 48/48; tree clean (one measurement probe applied and reverted). **THREE OF THE FOUR THINGS THE OWNER'S SCOPE NOTE TOLD THIS RUN TO PRESERVE HAD GONE STALE** between the note being written and round 4's patches landing, so following it literally would have written two fresh falsehoods into the authority artifact: the `exportedProps` clause needed RETRACTING at the composed-type spelling rather than narrowing (P3 closed it); P5 closed the focusable guard's listed-files narrowing the same day; and (ii)'s arithmetic was wrong IN KIND — `FULL_SCREEN` feeds two loops, so the MEASURED result of adding both Server-Component shells is **52 tests / 4 failures**, not two, the extra pair being `exportedProps` (:927) refusing to read an `export default async function` at all. **The Reviewer Gate opened new findings for the FOURTH consecutive run, and for the second run running the headline findings were against the amendment ITSELF.** Three lenses converged on one: the spine had come to FORBID AND MANDATE THE SAME CHANGE — the new text called widening `FULL_SCREEN` "the widening to refuse" while AD-24:218 records the owner's ratified design as "`FULL_SCREEN` widened to it". Resolved by stating the implicit distinction: a route-group SEGMENT is the encoding (every page inherits, no registration step to forget); what must be refused is another LEAF FILE. **The gate also refuted this run's own headline claim** that round 4 broke the widen-the-list pattern. It did not: round 4 widened `LITERAL_OUTLINE_COLOUR` (:697) from four excluded spellings to nine after round 3 had named a positive colour vocabulary as the alternative — and that widened list is ALREADY DEFEATED in the same direction, reproduced against the shipped regex, `focus-visible:outline-[transparent]` / `outline-[inherit]` / `outline-[color:inherit]` accepted, none carrying a token name so no guard sees them. The pattern stays on the books in its general form. **FIVE code-owned findings filed in `deferred-work.md` under a new 2026-08-01 heading, not patched — an architecture Update run does not touch production code:** that focus-ring bypass; the `className` guard's index-signature hole plus its `.tsx`-only call-site belt (`React.createElement` from a `.ts` module is invisible); the EDGE-WIDTH GUARD NEVER RECEIVING THE TRANSITIVE SWEEP the token and focusable guards got (`EDGE_UTILITY` consumed at :631 and nowhere else, so a reachable `.tsx` is token- and focus-scanned but not edge-scanned); the four hardcoded room-facing lists where AD-24 claimed two; and the async default-export ceiling. **Three of those five are live narrowings of AC-4's own guard, all latent today** — same disposition round 4 gave DARK_VARIANT — which is the material argument for a round 5 and is the owner's call, not this run's. NOT DONE, reported rather than acted on: AD-24 grew again and the AD-25 split the 2026-07-31 rubric walker proposed is still unmade; the adversarial lens's proposal that AD-24 assert the PROPERTY (every room-facing URL resolves through a segment emitting the reset before first paint) rather than the mechanism spelling is a new invariant reaching into 17.7's design, so it is surfaced in the spine and left to the owner; and no story owns `PROJECTED`'s criterion. **The version/reality-check lens returned this run's own two passages CITATION-CLEAN** (all 11 line refs exact, 48-test baseline independently reproduced) and found the rot everywhere else — six spine-internal repairs applied, all verified by the parent first. **Two are claims about sibling artifacts that HAVE SINCE BEEN REPAIRED**, which is citation rot running in the direction that wastes work rather than hides it: the spine said the `EXPERIENCE.md` bare-`AD-4` repair *"is tracked nowhere"* while citing a BLANK LINE (`EXPERIENCE.md:153`) — `bmad-ux` had already made it, at `:185`, in its own voice; and a Deferred entry claimed two affordance handoffs to `EXPERIENCE.md` had *"not been received"* when both were received WITH OWNERS (Open Item 6 / Story 20.8, Open Item 7 / Story 20.3), the latter having found a second face the spine never named. Four more: `AD-23` cited `prd.md:305` (an offline-cache assumption) for FR-7, which is at `:179-180` with the cross-surface obligation verbatim at `:304`; NFR-3's binding cited `prd.md:550`, the Privacy paragraph, not `:563`; `SPEC.md:88`→`:89`; and **the Next security bullet's non-applicability argument was HALF WRONG** — Turbopack is the Next 16 default for `next dev`/`next build` and `next.config.ts` carries no opt-out, so this project DOES have that half and the CVE-2026-64642 dismissal now rests on the i18n leg alone. The conclusion survives, the stated reason does not — the third time this file has been bitten by a refutable reason, and it mattered because it handed a reader grounds to dismiss the bullet that also carries the SSRF bearing on AD-8 and `safeNextPath`. **Stack drift vs `package.json`: zero, row by row**; one undercount fixed (TypeScript is two majors behind, not one). PRIOR ENTRY, bmad-dev-story round-3 remediation: **all 18 remaining round-3 items closed; back to `review`.** The 19th patch item and the blocking decision were already closed 2026-07-31 by the bmad-architecture Update run, which is the channel AGENTS.md requires and not this workflow's to substitute for. Round 3 named its own shape — a rule applied too narrowly gets closed by widening the list instead of encoding the rule — so the remediation was written against the shape: three guard findings collapsed into ONE JSX walk (`walkJsx`/`openingTag`/`enclosingTag`) plus ONE shared lexical rule (`expressionPosition`, which answers the same question for `/` in `stripComments` and `<` in the walk), and the focusables, branch roots, header row and failure branches all ask that walk now instead of each carrying its own narrower regex. The badge guard's POLARITY IS INVERTED: universal over every `.tsx` and `.ts` under `src/`, per class value and per site, every chromatic hue at any shade, with 18 pinned exceptions each carrying a source citation and an owning key or AC, compared as a multiset in BOTH directions so an unlisted offender fails and a fixed-but-still-listed entry fails too. The closure walk enqueues every extension, reads `export … from` and bare side-effect imports, and asserts a floor of 27 reached modules against the 12 it was scanning. Two guards ended stricter than their findings asked (`fixed inset-0` required on the full-screen root; the reach floor). Code: the projector failure branch scrolls — MEASURED, the old shape hid 112px of error detail with no scrollbar and `scrollTop` pinned at 0; `header-chrome` splits into box + tone so the profile trigger stops hand-rolling the third copy, verified as NO VISUAL CHANGE by computed-style equality in both themes and by running the real `cn()` over the ThemeToggle merge path; the shell claim gains a floor and a `generation` token. Record: this file's superseded VERIFICATION GAP paragraph is DELETED and replaced by a record of what it claimed and which clauses retract it (that was round 3's only `high`, and it was this row); `epics.md` moved to `review`; `DESIGN.md`'s LogoutButton rows rewritten to the shipped `text-destructive`; `EXPERIENCE.md` scoped to the shells' own elements with all FOUR holes named; the story's own `layout.tsx:26` citation repaired. **The rules found three things the lists had not:** an unclassed context-provider root in `SlideGridDialog` (correcting AC-3's rule from *root* to *outermost classed*), 15 unwalked modules rather than the estimated 14, and a FOURTH copy of the header box at `AnnouncementsManager.tsx:275` — deliberately not absorbed into 17.1, because it is a page action button and not header chrome. Verification: full suite **386 tests / 385 pass / 0 fail / 1 skipped**; theme-chrome **47/47**; public-repo guard **4/4**; `tsc --noEmit` clean; `eslint src tests` **31 problems**, identical to HEAD measured by stashing this change set and re-running, so ZERO introduced; **24 defect injections, 23 react** — a property of those 24, NOT a coverage claim (round 4 found nine further defects that stay green, seven of them in guards this pass rewrote); the one that does not react is the shell-claim floor, which has no public path to reach once the generation token exists, stated in the source rather than covered by a source-text assertion. NOT VERIFIED IN THE RUNNING APP, and it is a real gap: the header row and the projector failure branch were never seen rendered — both are behind the session gate, the latter also needs a forced registry failure, and this workflow does not sign in; the two measurements above are of the CSS against the real compiled stylesheet, not of the rendered pages. PRIOR ENTRY, 2026-07-31, bmad-architecture Update run: **the BLOCKING prerequisite is now MET.** Both inline `ARCHITECTURE-SPINE.md` edits (`:218`, `:393`) are RATIFIED as amendments of that file rather than accepted as citation repairs — verified before ratifying, not rubber-stamped — and the stale `@/lib`/`componentImports` bullet at `:392` is repaired by the same hand, with the `.ts`-ceiling nuance the review asked to be carried. 24 AD headings before and after; nothing renumbered, no AD added; lint_spine 0 findings; theme-chrome 43/43. **The gate opened findings for a third consecutive time, and this time against the amendments themselves — three corrections landed on the run's own text:** (1) it replaced a refutable reason with a new refutable ABSOLUTE ("no browser-side mechanism can close the shell gap") which Next's own docs refute with a pre-paint inline `<script>` — the mechanism `next-themes` uses here, so the set handed to Story 17.7 had excluded the framework's answer; (2) the standing instruction "`FULL_SCREEN` gains both shells in the same change set" was UNSATISFIABLE — that set asserts every member calls `useProjectedShell()` and neither Server-Component shell can, so following it turns 43/43 into 2 failures; the closing change set must rewrite the assertion, not extend the list; (3) a route-segment stylesheet — which the earlier text MANDATED — has no release (Next does not remove stylesheets across client navigation), so a CSS-only closure strands the operator's whole hub at black with no gutter after Exit, failing on the opposite screen from the one being protected. AD-24's gap clause now states the constraint as a TIMING one with three pre-paint mechanisms separated by whether they release, and flags that a server-set class can only be encoded at the root layout, which couples AD-24's paint closure to AD-5's matcher. Also corrected while the spine was open, both pre-existing and both false: AD-23 still accused `docs/architecture.md:61` of hardcoding a crossfade after the 2026-07-30 reconciliation fixed it, and the Next security bullet claimed "Next has stated that earlier minors are not back-patched" — refuted, 15.5.21 was patched the same day. Retraction residue swept from three downstream projections (Structural Seed, the Client-state convention row, the mermaid edge). New at spine altitude: `projected-shell.ts:34-36`'s header comment tells a 17.7 implementer a Server Component can reach the reset without a hook — FALSE, filed in `deferred-work.md` under 17.7, not patched here; plus two Deferred entries (the shared reset's single colour is wrong for `PresenterOperator` as its queued third consumer; CI/release is the one dimension AD-21 binds by name and nothing owns). NOT done, reported to the owner instead: AD-24 is now ~2000 words and the rubric walker named a safe split into a new AD-25 — that is a restructuring pass the owner has not asked for and it moves a heavily-cited AD. PRIOR ENTRY, 2026-07-31, bmad-code-review round 3: back to `in-progress` with **19 patch action items**, 1 owner-resolved decision that BLOCKS closing, 1 deferred, 6 dismissed. Round 2's 25 items ARE closed — verified item by item against the code, none is a checkbox over nothing. The 19 are one pattern, now in its third round: a rule applied too narrowly gets closed by widening the list instead of encoding the rule. Nine of them are the AC-4 guard AD-24 names as its closure gate, all latent and all reproduced by injecting a defect and watching 43/43 stay green: the closure walk stops at the first `.ts` hop (so the removed `@/lib` directory exemption is now an extension exemption over the same directory) and `moduleImports` cannot see `export … from`, already spelled at `src/lib/parsed-fields.ts:432`; the `text-white` root guard reads `jsxReturnBranches(...)[0]`, the exact defect that function's own comment says it was written to remove, while the AC-3 guard 130 lines below iterates every branch; `jsxReturnBranches` matches only parenthesised returns though `SlideView.tsx:19` is paren-less; the focus-ring guard's `/focus-visible:outline-\w/` accepts `outline-none`, which deletes the ring it protects; "every projected focusable" is two hardcoded files and `<Link>` only, with `ProjectorClient` absent; the badge guard is a 4-file x 3-hue table satisfied file-wide by one match; `stripComments` has no regex-literal state and `\//` deletes the rest of a line from the scan (the false-pass direction); `exportedProps` is defeated by a named props type, which is house style at `Header.tsx:9`; `EDGE_END` misses `border-2${x}` and `border-2!`. Four code items: the projector's failure branch still cannot scroll after the same commit declared it and the slideshow's to be one failure and fixed one (`projector/page.tsx:71`); the `flex-wrap` assertion checks `<nav>` instead of the six-control row at `Header.tsx:79`; `header-chrome.ts`'s doc claims "every inactive control" while the profile button still hand-rolls the box; the shell claim counter has no floor and the exported test seam can drive it to -1. **The one `high` WAS this entry itself — see the `last_updated` note — and it was deliberately left unfixed by that round so the finding stayed visible. CLOSED 2026-08-01: the superseded paragraph is deleted and replaced by a record of what it claimed, further down this same row.** **BLOCKING: the owner ruled that the inline `ARCHITECTURE-SPINE.md` edit (`:218`, `:393`) goes through a `bmad-architecture` Update run before 17.1 can be `done`, and the stale `@/lib`/`componentImports` bullet at `:392` should be repaired by that same run rather than a different hand.** Patch items left as action items at the owner's direction, as in rounds 1 and 2. PRIOR ENTRY, 2026-07-31, bmad-dev-story round-2 remediation: **all 25 open review items addressed and verification is COMPLETE.** `npm run build` succeeds; full suite **380 tests / 379 pass / 0 fail / 1 skipped**; `tsc --noEmit` clean; theme-chrome 43/43; public-repo guard 4/4; `npm run lint` 31 problems against 32 before (the difference is the unused import this round deleted), with 4 in the 15 touched files, ALL pre-existing and verified individually against HEAD — zero introduced. Only a browser pass over the four new `dark:` sites is outstanding, and no new colour pair enters the product. **`tsc` caught one real defect that 43 green tests did not** — `ShellDocument` typed the claimed style as `Record<string, string>`, which a real `CSSStyleDeclaration` cannot satisfy, so `claimProjectedShell(document)` failed to compile while every stub-based behaviour test passed; fixed to a mapped type. **SEPARATE DEFECT FOUND AND REPAIRED, NOT THIS STORY'S: `npm ci` has been broken repo-wide since 2026-07-29.** `package-lock.json`'s `integrity` for `set-function-name@2.0.2` disagreed with the registry in one four-character substring — collateral damage from this project's own PII remediation, which replaced real names with invented ones across the whole tracked tree and rewrote one inside a base64 sha512. Nothing noticed for two days because npm is the only thing here that verifies a checksum. All 819 integrity values checked by base64 length (sha512 must be 88 chars; the corrupt one was 89): **exactly one affected** — but that detector CANNOT see a length-preserving rewrite, so the open question is what else that pass rewrote in non-prose data (asset-map, fixtures, any derived value). Repaired to the registry's authentic value; guard still 4/4; integrity checking never disabled. **This needs its own audit entry rather than attribution to Story 17.1.** NOTE, an earlier diagnosis recorded in the story was WRONG and is corrected there: the failure was first read as a live text substitution outside the repository; installing with a cache outside the default disproved that, because a freshly downloaded and hashed tarball produced the same digest — the stored expectation was the corrupt half. AC-4 is rescoped in the AC itself on two axes at the owner's direction (token guarantee = 17.1, shell guarantee = Story 17.7, and the word *previewed* removed because the Live Slide Preview is hub chrome and themes deliberately) — amending an AC is outside this workflow's normal edit surface and is flagged as owner-directed in both the AC and the Change Log. Landed: the four AC-6 sites the round-1 sweep missed; `text-destructive` replacing the hand-rolled red pair in two files (byte-identical to `red-600`/`red-400` in both themes) plus the unused `Button` import deleted; `ArtifactSlide`'s dead `className` parameter deleted so the AC-4 invariant is a compile error; `focus-visible:outline-white` on all three projected focusables (the ring painted from `--ring`, and the clearing measurement had been taken with nothing focused); a scrollable slideshow failure branch with the projector's headline; `ThemeToggle` on a shared `HEADER_CONTROL_BOX` (new `components/header-chrome.ts`) with `cursor-pointer` and `aria-disabled:pointer-events-none`, moved out of the `<nav>` landmark onto a `flex-wrap` row; `disableTransitionOnChange`; a **reference-counted** shell claim extracted to `src/lib/projected-shell.ts` and the cycle to `src/lib/theme-cycle.ts`, both so the restore path and the modulo are exercised as behaviour; the guard file rebuilt (scanner comment stripping, brace-balanced extraction, Tailwind 4 `bg-(--card)` shorthand, arbitrary + inline edge widths, per-branch AC-3, transitive closure with no exempt directory) — **28 -> 43 tests with 18 injected defects each confirmed to make the suite react**; `DESIGN.md` Open Items 4 and 6 repaired and the preview-list Components row corrected; `ARCHITECTURE-SPINE.md` naming **Story 17.7** as the key that closes AD-24's `[ADOPTED, partial]` tag and widening the gap from one hole to four. (A superseded VERIFICATION GAP paragraph was DELETED from this entry on 2026-08-01 — round 3 raised it as this story-round's only `high`. It claimed `npm test`, `tsc --noEmit` and `npm run lint` could not run, that the lockfile was intact, and that the cause was an environment fault for the maintainer. All three are retracted by the rest of this same entry: the runs completed, the lockfile was the corrupt half, and the fault was in the repository. It was a draft left inside the entry that replaced it, in this file, on the row that gates closing the story.) **TWO CLAIMS IN THE ENTRY BELOW WERE ALREADY STALE when this round started and are corrected here:** the "25-file working tree is STILL UNCOMMITTED" — it was committed as `3f210c7`, `git status` was clean; and "17.7 is NOT yet registered in this file or epics.md" — it was, by `116ba3d`, so patch item (b) was half-closed before this pass. PRIOR ENTRY, 2026-07-31, bmad-code-review round 2 (three parallel layers again, launched blind): back to `in-progress` with **24 patch action items left in the story**, 3 deferred, 2 dismissed. The owner resolved all 3 decision-needed items the same day: (1) the AC-4 shell holes are FOUR, not one — first paint on every projected load (a `useEffect` cannot fix it; the leaked paint is the server's), the two Server-Component error branches, `notFound()` at six reachable sites with NO not-found.tsx/error.tsx anywhere in `src/`, and `PROJECTED` never being closed upward — and they close with ONE route-group layout owning every room-facing URL, which is the scope of **new Story 17.7**, not a patch here; (2) 17.1 CLOSES once its 24 patches land, with AC-4 scoped in writing to the token guarantee and the shell guarantee spun out to 17.7 — **17.7 is NOT yet registered in this file or `epics.md`; that registration is patch item (b) and is the single most important one, because a decision recorded without a key is how this project has repeatedly lost findings**; (3) AC-4's contract sentence is amended to exclude operator-side preview, which `SlidePreviewList` now deliberately themes. Verified at review time: tsc clean, theme-chrome 28/28, suite 365/364/1-skipped, exactly one lint problem in any of the 13 changed files (pre-existing). The story's own `baseline_commit: acc8df0` was checked against HEAD — 16 commits apart, all `docs:`, none touching `src/`/`tests/`/`package.json` — so the uncommitted working tree IS 17.1's complete code delta; it is still uncommitted. PRIOR, 2026-07-31 bmad-architecture Update: AD-24 is IN THE SPINE, so all 12 patch findings are closed. Tagged [ADOPTED, partial], because that run's Reviewer Gate opened ONE NEW HIGH finding against this story's change set: the AC-4 shell reset reaches the two full-screen Clients and cannot reach the two room-facing route shells (projector/page.tsx:71, slideshow/page.tsx:76), which are Server Components — so html/body keep the themed background and the reserved scrollbar gutter on the branch a buildSlidePlan throw renders. Filed in the story's Review Findings with NO OWNER; needs a story key before Epic 17 closes. AC-4's token guarantee holds; its shell guarantee does not. PRIOR, 2026-07-31 bmad-dev-story remediation: 11 of the 12 patch findings closed and back to `review`. AC-4 now met — the projector's html/body reset extracted to src/lib/use-projected-shell.ts and called by the slideshow too, verified in the browser and with the defect reproduced first; the slideshow route shell repainted in literal colours and both shells added to the guarded set. Three sub-AA chromatic text pairs fixed (indigo 2.54→9.72, logout 3.76→6.21, emerald 4.23→10.56); the 29 newly-live `dark:` overrides — 18 sites, 8 files, not the 19/9 the review headlined — reviewed and measured, all pass; non-text contrast measured for the first time and filed as DESIGN.md Open Item 6 (--border 1.29:1 dark / 1.26:1 light, light focus ring 2.58:1, no owner). theme-chrome tests rebuilt with comment stripping and every guard negative-tested, 17→28. AC-5 stays structural per the owner and is now filed as EXPERIENCE.md Open Item 4 owned by the new story 17-6. Suite 365/364 pass/0 fail/1 skipped, tsc clean, guard 4/4. STILL OPEN: **AD-24** — the localStorage/root-client-boundary decision needs a `bmad-architecture` Update run, which this workflow is not allowed to substitute for # 2026-08-01 LATEST, bmad-code-review round 4 (three parallel layers, uncommitted tree = the round-3 remediation): **all 15 patch items applied in-session; 1 decision item OPEN, which is why this is `in-progress` and not `review`.** Owner's call on that item: route the stale `ARCHITECTURE-SPINE.md:392` ceiling bullet through a `bmad-architecture` Update run, and 17.1 does not go to `done` until it lands — the same refusal rounds 1 and 3 made, for the same reason. Round 3's own headline recurred a FOURTH time and inside the guards that round rewrote to encode rules: `openingTag` was written for the `<Link>` truncation and not called for the byte-identical one in the `className` guard; the focus-ring exclusion list grew by four spellings and still accepted `outline-transparent` (invisible) and `outline-inherit` (paints from `--ring`); the props reader followed a named type and then read only its first `{ … }`, so `React.ComponentProps<'div'> & { … }` restored a projected `className` with `tsc` clean. Fixed by encoding the rule each time: the props shape resolves transitively and fails loudly on anything not declared locally; `stripComments` models JSX children as a fourth quoting form (`walkJsx` already did, and it runs downstream of the lexer that did not — one URL in room-facing copy was hiding a live `bg-card` + `dark:` + `border-2` from four guards at once); one shared `projectedTree()` answers *what is projected* for both the token guard and the focusable sweep, which had answered it at different widths; the AC-6 pairing splits template arms in `classValues` so a ternary's two arms stop validating each other; the AC-3 surface root descends the single-element chain and reports ambiguity instead of taking the first classed element in source order. Nine defect injections, each green before and failing after, each reverted. Verification: theme-chrome **48/48** (47 → 48, the new test pins the shell reset), public-repo guard **4/4**, full suite **387 / 386 pass / 0 fail / 1 skipped**, `tsc` clean, lint **31** unchanged from baseline with **0** in any changed file. Record fixes: the pinned exception list is **18**, not the 20 claimed in three places; `DESIGN.md` rows 99 and 194; `EXPERIENCE.md` gains a projector State-Patterns row and its focusable claim resized to the assertion. `DARK_VARIANT`'s `dark:!…` / `dark:<digit>…` gap deferred, pre-existing, filed in deferred-work.md.

### `17-2-muted-foreground-contrast` — done

2026-08-03 Reviewed: fresh browser measurement and Node 22.23.2 full suite pass; oklch(0.543 0 0) clears all three light hosts; theme-chrome 57/57; DESIGN.md Open Item 1 closed.

### `17-3-app-metadata` — done

2026-08-05 Code review closed. Three docs patches resolved (AC-5 CI Node 22 evidence, stale References prose, baseline_commit wording); one pre-existing sprint-status header comment deferred — that deferral is now CLOSED, repaired 2026-08-05 by Story 17.5's record repair, which edited this same file (see deferred-work.md's 17.3 section).

### `17-4-canvas-dirty-state-guard` — done

2026-08-04 CODE REVIEW CLOSED. Three adversarial layers run on gemini-3.1-pro-high via the Antigravity CLI (agy) — a different model from the implementer, which is what the workflow asks for. NOT Orca-orchestrated: the Orca runtime answered runtime_unavailable and the fix is a restart of the user's live app, which was not taken unilaterally; the skill's own step-02 fallback (generate reviewer prompts, run each in a separate session, ideally a different LLM) was used instead, and this row says so rather than claiming provenance it does not have. 14 raised / 13 unique: 10 DISMISSED each against the source, not waved off — four were reviewer errors (inline text editing needs IText/Textbox and this editor builds FabricText; Ctrl/middle-click never reaches onNavigate because Next returns at isModifiedEvent first; the diff DOES contain EXPERIENCE.md and epics.md, the layer cited a path that does not exist; no async canvas.add exists so a seed image cannot false-dirty a mount). 2 DEFERRED to deferred-work.md with reasons: browser Back/Forward bypasses the guard (App Router ships no supported popstate hook; not a regression, nothing was guarded before) and a failed template load cannot be retried by re-clicking the same row (pre-existing; setSelectedId to the same value always bailed out). 1 REAL AND PATCHED: a drag landing between serializeCanvas and the save response set the flag but was discarded by the post-save remount, after which the flag reported clean over lost work — the exact failure this story exists to prevent. Owner chose to lock the canvas while busy; the [busy] effect now discards the active object and drops selection/selectable/evented. Verified: suite 28/28, four new injected-defect probes all red-on-break, browser re-verification 7/7 holding the PUT open via CDP Fetch interception with a self-validating idle-drag control. Acceptance Auditor: zero findings, all three disclosed deviations judged sound. PRECEDENT NOTE: per Story 17.1, the done call is the owner's — this row follows the workflow's status rule and does not substitute for that confirmation. # 2026-08-04 Implemented. In-memory dirty flag + visible indicator, beforeunload prompt, template-switch confirm, and a page-scoped onNavigate block on all five Header links (Next 16 pattern; provider on the page, NOT the root layout, per AD-24). Logic in src/lib/canvas-dirty-guard.ts so node:test calls it; wiring covered by TypeScript-AST assertions in the new tests/canvas-dirty-guard.test.mjs, registered in package.json scripts.test. All 13 injected-defect probes turned the suite red before it was trusted. Nothing persisted — AD-24 names this story as its live instance. EXPERIENCE.md Open Item 3 and the /admin/artifacts surface row closed; DESIGN.md untouched. THREE DEVIATIONS DISCLOSED IN THE STORY RECORD, none changing an AC outcome: the fresh-mount clear sits in loadTemplate rather than the mount effect (a synchronous setState there is a lint error, and loadTemplate is the more correct spot — a FAILED load must not clear a flag whose canvas is still mounted); navigation-blocker.tsx sits at src/components/ rather than admin/, because shared Header must not import an admin-scoped module; and one further EXPERIENCE.md row was corrected — the Component Patterns row still said "navigating away loses them silently", which this story makes false. Verified: npm test 468/467 pass 0 fail, tsc clean, public-repo-guard 5/5, lint 32 problems = re-measured clean-tree baseline. Node 24.18.0 in-worktree, not the 22.x CI runs — see the record. ALSO VERIFIED IN A REAL BROWSER (16/16): headless Chrome over the DevTools Protocol against next dev on a scratch DB_PATH, covering the half node:test cannot reach — Fabric object:added/object:modified raising the indicator, the native beforeunload prompt, and both confirm-gated exits declined AND accepted, plus the negative cases (clean canvas prompts nothing, re-clicking the active row prompts nothing, a read-only template arms nothing, no leak onto pages without the provider).

### `17-5-projector-liveness` — done

17.5 added 2026-07-30 by bmad-ux Update — the only finding of that run that was a code gap rather than a documentation defect. EXPERIENCE.md had specified "Lost sync" as a shipped state since 2026-07-19; no detection exists. 2026-08-06 CLOSED by the owner. Implemented, TWO CODE-REVIEW ROUNDS CLOSED, and MERGED to main as PR #33 with Node 22 CI green (run 31011350874, v22.23.1, 495/496 pass) — every AC has its evidence. See the CODE REVIEW and RECORD REPAIR entries at the end of this block. FIRST ENTRY, chronological: Story context created (bmad-create-story).

### `17-6-toast-channel-decision` — done

Nine AC. TWO OWNER DECISIONS were escalated during context creation and answered the same day, because epics.md:320 permits two mechanisms and picking one fixes the AC. (1) MECHANISM: an acknowledgement on present-channel.ts is the PRIMARY liveness signal and the retained-handle `closed` read is a corroborating FAST PATH inside the SAME evaluator — one predicate, not a second state machine — with the ack authoritative for `live` and the handle authoritative for immediate `lost`. The poll alone is structurally blind to three verified cases: a projector opened through the popup-blocked fallback anchor (no handle is retained), a Presenter remount or reload (the file's own comment at :105-111 names it), and a crashed/frozen/navigated-away window (`closed` stays false). The story's own words are "stops answering", which is an acknowledgement property. (2) WIRE PATH — GATE: the PresentMessage extension does not get written until a bmad-architecture Update run adds AD-29 (next unused; 28 headings verified, highest AD-28, nothing renumbered) ratifying the ack's shape, that only the projector sends it, and that the presenter remains the single authority. Paid up front deliberately: this is the same gate that blocked Story 17.1 from `done` twice. ARCHITECTURE-SPINE.md is untouched by the context change set. AD-10's [TARGET] plan-identity clause stays OUT of scope. No DB, schema, settings key, API route or IA-table row. EXPERIENCE.md is the authority home (it names 17.5 at :153, :240, :310); DESIGN.md names it nowhere and is touched only on a visual delta. Guard hazard recorded in the story: UNPAIRED_CHROMATIC_TEXT in tests/theme-chrome.test.mjs is a both-directions multiset, so a fifth unpaired chromatic text site in PresenterOperator.tsx fails the suite until it is pinned. 2026-08-05 LATEST — THE AC-2 GATE ABOVE IS CLEARED: the `bmad-architecture` Update run has landed AD-29 "The Projector Reports Its Own Liveness and Nothing Else, and One Predicate Decides It" [TARGET] at ARCHITECTURE-SPINE.md:262. Census 28 → 29 headings, nothing renumbered, no id reused, no existing tag flipped; AD-10 amended in place by one appended bullet (placed after its gap deliberately, so AD-10's own :118-122 range did not move). Beyond the three points AC-2 asked for, the run's Reviewer Gate closed two holes those three leave open: only the PROJECTOR may send the ack (AD-10's Binds already invites the web slideshow onto that channel, and a state-free message cannot say who sent it), and where the predicate cannot be certain it resolves to `lost` — a false `live` is unrecoverable within the service, a false `lost` self-clears and costs only credibility. Cadence numbers are deferred to this story with the throttling figures now web-verified in the spine's Deferred section. Status stays `ready-for-dev` — AC-1 may now be written. NOTE FOR THE DEV RUN: that run moved spine lines, so four of this story's citations need a mechanical repair — AD-24 :204-210/:210 → :205-211/:211, AD-28 :244 → :245, the plan-identity Deferred entry :429 → :447. AD-10's :118-122/:121/:122 are unchanged. AC-2's "28 headings, highest AD-28" is a DATED verification record of context creation and is left as written on purpose. lint_spine 0 findings; theme-chrome 58/58; public-repo-guard 5/5; zero code files touched. 2026-08-05 IMPLEMENTED (bmad-dev-story), back to `review`. All nine AC. `src/lib/projector-liveness.ts` (NEW): one pure evaluator, three states (never-opened/live/lost), a reducer over (state, event, nowMs) with three events (ack, handle-closed, tick) — no React, no window, no imports. Cadence pair exported once: PROJECTOR_HEARTBEAT_INTERVAL_MS=2000, LIVENESS_FRESHNESS_WINDOW_MS=6000 (3x). Sized against a STATED throttling assumption rather than left silent (mandatory per the validator): the projector is a popup window on a second screen, ordinarily visible, so it does not suffer ordinary background-tab timer throttling (~1/sec); the narrow real cases (operator minimises/obscures it, or the presenter's own tab is backgrounded) produce a false `lost`, which is the direction ARCHITECTURE-SPINE.md:448 (AD-29's own Deferred entry) already names as acceptable and self-clearing — cited rather than re-derived; no deferred-work.md entry needed since the assumption is stated and sourced. present-channel.ts: one new state-free `projector-alive` variant, citing AD-29 in its own voice; blankStateOf/liveTransitionOf already resolve it to null (pinned by two new tests in tests/present-channel.test.mjs). ProjectorClient.tsx: heartbeat on setInterval inside the effect already pinned to [serviceId] — not a new dependency, not a new effect. PresenterOperator.tsx: every inbound message (heartbeat + request-sync) dispatched as `ack` evidence in the existing listener without changing how request-sync answers; a 200ms poll feeds `handle-closed` only when the ref is truthy AND closed (never on null), else `tick`; the verdict renders as a persistent role="status" line in the header, independent of projectorBlocked, silent in never-opened, clearing on the next ack — reusing the existing pinned-dark text-amber-300 treatment (no new hue). tests/theme-chrome.test.mjs: UNPAIRED_CHROMATIC_TEXT's four PresenterOperator.tsx citations repaired to current lines and a fifth entry added for the reused site, owning key AC-5/Story 17.5. tests/projector-liveness.test.mjs (NEW, 17 assertions, registered in package.json scripts.test): the three states, both routes into lost, all AC-4 precedence rules including acks-beat-a-stale-closed-reading, never-opened silence, recovery on a bare ack, plus AST assertions (no second liveness state in the component, the heartbeat's interval and cleanup live inside the pinned effect, the handle-closed dispatch is guarded by a non-negated truthy check of the ref rather than a text match that an inverted `!ref || ref.closed` guard could satisfy — the test itself was tightened after that gap surfaced during defect injection). TEN defects injected against the evaluator and the wiring (ack-cannot-revive-from-lost, handle-closed-waits-out-the-window, never-opened-not-silent, inverted freshness comparison, null-treated-as-death, a heartbeat effect dependency, a state-carrying heartbeat, a second liveness flag, a wire type misresolving blank/transition, an unregistered chromatic site), all ten confirmed red before being reverted. EXPERIENCE.md: the Lost sync row (:153) now states shipped behaviour; Open Item 1 (:310-312) closed with evidence on Open Item 3's shape; Flow 3 Branch 3a (:240) states the shipped detection; the Interaction Primitives one-way wording (:157) corrected, citing AD-29, the plan-identity half left untouched. epics.md: Epic 17 summary line and the Story 17.5 label both read `review`; the Constraint paragraph states the resolved mechanism and points at AD-29. DESIGN.md: the amber inventory at :98 and Open Item 4's counted grep at :236-246 both re-measured and updated together (45→46 uses, 90→91 utilities — one new SITE, same shade, so only these two figures move) since the reused site is a new occurrence even though it introduces no new hue. ARCHITECTURE-SPINE.md carries no edit from this change set — confirmed, its only amendment is AC-2's prior Update run. No IA-table row moved. Citations repaired per the dev-run note above (AD-24 :205-211/:211, AD-28 :245, Deferred :447; AD-10's :118-122 verified unchanged). VERIFIED: tsc --noEmit clean; npm run lint 32 problems, IDENTICAL to the pre-change baseline measured by stashing this change set and re-running (0 introduced); full suite 489 tests / 488 pass / 0 fail / 1 skipped (the skip is pre-existing, in tests/registry-assets.test.mjs, unrelated) — 19 of the 489 are new (17 in tests/projector-liveness.test.mjs, 2 in tests/present-channel.test.mjs); theme-chrome 58/58; public-repo-guard 5/5; `npm run build` clean. Node 24.18.0 in-worktree — no Node 22 available on this machine, so this is disclosure rather than proof per Story 17.3's precedent; the PR's Node 22 CI run is this AC's evidence. Not run: a real browser pass (no DOM harness in this repo, per Testing Standards; the wiring gap is covered by the AST assertions instead). 2026-08-05 CODE REVIEW: TWO ROUNDS RUN, EVERY FINDING CLOSED. Status stays `review` — `done` is the owner's call (Story 17.1's row states that precedent) and neither the review nor this row makes it. Four independent reviewers across four model families (claude:sonnet@high, agy:gemini-3.1-pro-low, agy:gemini-3.6-flash-high, codex:gpt-5.6-terra@high) — the required trio plus one, with the third-family rule satisfied against a claude implementer. ROUND 1: three [High, blocking] defects, all fixed with fail-first proofs in a bmad-dev-story FIX round — (1) a second Presenter tab's own broadcasts were being recorded as projector acks, so the verdict could read `live` with no projector at all (fixed by `isProjectorMessage` in present-channel.ts, the wire's own owner of who-sends-what); (2) a projector that opened and never acked stayed `never-opened` forever instead of resolving to `lost`, contrary to AD-29's uncertainty-resolves-to-lost clause (fixed by a fourth reducer EVENT, `opened` — the verdict vocabulary stays three, so AC-3's one-predicate rule holds); (3) the advertised recovery only focused a frozen handle and could never reattach it (fixed by navigating it back to the projector route when the verdict is `lost`). One non-blocking render-churn item fixed; one claimed stale-liveness-across-serviceId gap investigated and confirmed NOT REACHABLE in this app rather than assumed away. ROUND 2: all three confirmed closed, plus one [Low, AC-7 accuracy] finding — the five UNPAIRED_CHROMATIC_TEXT citations for PresenterOperator.tsx had rotted a SECOND time under the fix round's own edits; repaired to :583/:604/:627/:709/:829. The guard cannot react to a stale bracket (it strips citation text before comparing), so this needed a human-shaped catch; that blind spot is now named in the story's Review Findings rather than left implicit, with no deferred-work.md entry filed because this file is held at zero `owner: unassigned` and picking an owner was not the review's call. 2026-08-05 RECORD REPAIR, and it is a finding in its own right: round 2 existed ONLY in commit 5d8100d's body for a day. This row still opened with "Story context created", and the story file's Change Log still ended on "pending re-review of this fix round" — so every artifact a reader would consult said a re-review was owed when in fact the review was complete. That is the same stale-story-prose defect Story 17.3 spent three review rounds on. Repaired in the story file (round-2 finding, round-2 Dev Agent Record section) and here. TWO FIGURES WERE ALSO WRONG and are corrected, not re-asserted: the fix round claimed projector-liveness.test.mjs at 30/30 and the focused trio at 103/103, having added its 7 new tests to the file's own total AND counted them in the breakdown. Re-measured on this tree: projector-liveness 23, present-channel 15, theme-chrome 58 = focused 96/96 pass / 0 fail (17 + 4 + 2 = 23 is the right arithmetic). The full-suite figure was re-run rather than trusted and stands as recorded: npm run build clean, then npm test 496 tests / 495 pass / 0 fail / 1 pre-existing skip. Documentation only — no source file touched. AC-9 CLOSED ON EVIDENCE in the same pass, not left outstanding: the Node 22 CI run this story deferred to already existed and was read — PR #33 (merged 2026-08-05T13:47:48Z), Node.js CI run 31011350874, step "Use Node.js 22" reporting node v22.23.1, npm test 496 tests / 495 pass / 0 fail / 1 skipped — the same four numbers as the local Node 24 run, so both supported versions agree. `test` check and Greptile review both passed. PR #33 IS ALREADY MERGED, so this story's code is on main and `git diff HEAD main` over the whole tree is empty; this repair travels as a separate docs: change set, per Story 17.3's precedent of recording a CI result in its own commit. Nothing now blocks `done` except the owner's call. 17.6 added 2026-07-31 during 17.1's review remediation, at the owner's direction: AC-5's structural-only status needed an owning key rather than a `last_updated` comment. `sonner` is installed, `Toaster` is mounted nowhere, `toast(` is called nowhere — and two artifacts describe toasts as shipped. Its first job is the decision (channel or no channel), not an implementation. Context created 2026-08-05 (bmad-create-story). The decision the story existed to ask is ALREADY MADE — the owner ratified a COMBINED inline + toast design on 2026-08-05, with the rule written NOW and the wiring built LATER, after Story 17.7's route group. So the story is documentation-and-registration only: it records the channel-split rule in EXPERIENCE.md, rewrites the two shipped-pattern rows in EXPERIENCE.md and DESIGN.md as decided-not-wired, answers Open Item 4, and registers the forward owner `17-9-toast-channel-wiring` here and in epics.md. It touches NO file under src/ or tests/; `sonner` stays installed, nothing is mounted, no `toast(` call is added. One blocking obligation the story cannot discharge itself: ARCHITECTURE-SPINE.md's Deferred bullet still calls the channel question live and names 17.6 as `(backlog)` owner, and repairing it routes through a `bmad-architecture` Update run (AGENTS.md) — the story's AC-9 blocks `done` on it. No new AD is needed: AD-24's narrowest-layout mount rule already decides this, because the toast provider's consumers are operator routes only, never every route. 2026-08-05 IMPLEMENTED (bmad-dev-story), back to `review` — the owner's `done` call is withheld deliberately (AC-9 below), not because anything here is unfinished on its own terms. Documentation-and-registration only, per the story's scope: no file under src/ or tests/ touched, sonner untouched, nothing mounted, no toast( call added. EXPERIENCE.md gained a dated decision block under Component Patterns carrying the three-clause combined inline + toast rule the owner ratified 2026-08-05; the sonner rows in EXPERIENCE.md and DESIGN.md and EXPERIENCE.md Open Item 4 were rewritten from decided-nothing to decided-not-wired, citing that block instead of restating it. epics.md: Story 17.6's own block rewritten to record the outcome (rule ratified, no surface shipped) rather than the question; Epic 17's status line moved 17.6 from the backlog group to the review group and added 17.9 to backlog; a new Story 17.9 block registered after 17.8 with the rule as its contract and an explicit Story 17.7 dependency, mirroring 17.8's own out-of-17.7 registration precedent. Forward owner 17-9-toast-channel-wiring added to this file, row above. Both UX artifacts' frontmatter `updated:` moved to 2026-08-05. AC-9 BLOCKS `done`: the stale ARCHITECTURE-SPINE.md:464 Deferred bullet still calls the channel question live and names 17.6 as its backlog owner — repairing it is reserved for a bmad-architecture Update run per AGENTS.md, not this workflow, and the dev agent did not touch that file. See Dev Agent Record for the five repair targets handed to that run and the full verification numbers. 2026-08-05 FIX ROUND (bmad-dev-story): three independent reviewers ran in parallel — codex, a Gemini Flash reviewer, and a Gemini Pro reviewer — and split: codex found four real defects, Gemini Flash found one of the four, Gemini Pro found none. A coordinator adjudicated the union against the files directly rather than trusting any reviewer's verdict; two of three said "fit to commit" and were wrong. FOUR CONFIRMED AND FIXED: (1) AC-1's stated-once property was violated by two epics.md restatements of the three-clause rule (`:329`, `:367`) — replaced with a named pointer to EXPERIENCE.md's decision block, and AC-1 reworded to state the property precisely against this story's own specification section, a spec of what to write rather than a second artifact claiming the contract; (2) AC-9's spine repair had already landed in this working tree (verified: one line replacing one line, AD census 29 before and after) but this story and this file still called it pending — both now record it CLOSED ON EVIDENCE, Status staying `review` for the owner's `done` call, same as Story 17.5's row; (3) citation rot this change set's own Task 2 introduced by inserting the decision block above the sonner row and Open Item 4 (shifting both, and everything below them in EXPERIENCE.md, by +8 lines) was left unrepaired at five sites in the story — corrected throughout, re-measured against final positions rather than trusted from either reviewer (one of whom reported the sonner row at `:119`, also wrong); (4) the mount rationale in EXPERIENCE.md, epics.md and this file overstated Story 17.7's registered contract (one route-group layout owning every room-facing URL) as an operator-facing one it has not promised — corrected everywhere to state the real precondition: an operator-scoped route segment existing, whoever creates it. REFUTED AND RECORDED rather than silently dropped: codex's blocking scope finding that the change set touches an unexpected seventh-plus file — the extras are the bmad-architecture Update run's own legitimate artifacts (deferred-work.md, architecture/.memlog.md, four files under architecture/reviews/), not scope creep, and the story's scope table is widened to say so rather than reverting anything; codex's claim that ARCHITECTURE-SPINE.md:465 is now stale — checked directly against the file and confirmed current, unchanged. No file under src/, tests/, package.json or package-lock.json touched by this FIX round; ARCHITECTURE-SPINE.md not opened. Full suite re-run: 496 tests / 495 pass / 0 fail / 1 pre-existing skip — identical to the baseline this same-day change set already measured, as expected since no code moved. Public-repo guard 5/5. Status stays `review`. 2026-08-05 SECOND FIX ROUND (bmad-dev-story): the coordinator verified the first round's four closures directly against the files and confirmed all four correct; two narrower items remained. F-A: R4's mount-rationale overstatement survived in this story's own body (Why the wiring cannot land now, and Dev Notes) because the first round's brief named artifact sites but not the story's own text — fixed at three sites, swept the whole file, no further live instances found. F-B, RECORDED HONESTLY AS A DEFECT THIS CHANGE SET INTRODUCED, NOT AN EXTERNAL FINDING: the 2026-07-31 owner declination (mounting <Toaster /> to make Story 17.1's AC-5 observable was refused as an unasked-for surface) used to live inside this story's own pre-rewrite epics.md Story 17.6 paragraph; this change set's own Task 5 deleted it when rewriting that paragraph to record the ratified outcome, without re-homing it anywhere — it survived nowhere in the tracked planning artifacts except as an unsourced paraphrase in this story's Dev Notes. Fixed by adding it to epics.md's Story 17.9 block (the story that now asks for the surface the declination said no story had asked for) and repairing the story's own citation to point there. Re-audited for the same class of defect (target text rewritten, not merely shifted) across the story and the four artifacts; nothing else rotted. One out-of-mandate finding reported, not fixed: stories/24-1-string-catalogue-switcher-and- lang.md:180 calls the AD-24 second-root-provider question "live," now stale since AC-9's spine repair — a different story, outside this dev-story's scope. No file under src/, tests/, package.json or package-lock.json touched; ARCHITECTURE-SPINE.md not opened. Full suite re-run: 496/495/0/1, unchanged; public-repo guard 5/5. Status stays `review`. 2026-08-05 THIRD PASS, RECORD-ONLY (bmad-dev-story): coordinator ruling on stories/24-1-string-catalogue-switcher-and-lang.md:180 — LEAVE THE SENTENCE, RECORD THE DISPOSITION, on consistency with the two dated 2026-07-31 architecture-review files this story's first FIX round already left alone for a stated reason. :180 calls the AD-24 second-root-provider question "live"; AC-9's repair of ARCHITECTURE-SPINE.md:464 made that stale. Left as-is because 24-1 is done (this file, above) and its story file is a dated delivery record, not a live pointer — editing a done story to track a later, unrelated story's spine repair would invent a sweep-every-prior-story rule this project does not have. Condition that would change the answer: a future reader of :180 as live guidance needs to re-check ARCHITECTURE-SPINE.md:464 first. Limit stated plainly: this and the prior round's search covered this story and the four artifacts it edits, NOT a systematic stories/** sweep for post-spine-repair staleness — :180 surfaced incidentally; whether that sweep is worth an owner is named as a coordinator call with a rough cost estimate in the story, not performed here. No file edited but this story and this row; 24-1-string-catalogue-switcher-and-lang.md and ARCHITECTURE-SPINE.md left untouched. No code/test change, so full suite not re-run — prior counts (496/495/0/1; guard 5/5) still current. Status stays `review`. 2026-08-06 CLOSED BY THE OWNER. All ten ACs carry a recorded disposition and none is open; AC-9, the only blocking one, was CLOSED ON EVIDENCE by the bmad-architecture Update run that repaired ARCHITECTURE-SPINE.md:464 in the same change set — the channel AGENTS.md reserves that edit for, and the gate the owner twice held Story 17.1 open over. Shipped as a `docs:` change set: ba222af, PR #35, `test` and `Greptile Review` both green; 496 tests / 495 pass / 0 fail / 1 pre-existing skip and public-repo guard 5/5, re-run by the coordinator rather than quoted; zero paths under src/, tests/, package.json or package-lock.json; AD heading census 29 before and after. **THE CLOSE IS RECORDED WITH ITS ONE GAP STATED RATHER THAN HIDDEN: the final state of this change set was never read by an independent reviewer.** The trio (codex gpt-5.6-terra high, agy gemini-3.1-pro-low, agy gemini-3.6-flash-high) reviewed the PRE-FIX tree and split badly — codex filed five blocking findings, Gemini Flash one should-fix, Gemini Pro ZERO on a tree that had four real defects, and both agy reviewers asserted "all citations resolve" when four had shifted. The coordinator adjudicated the union against the files: four confirmed, two refuted (one of them caused by a stale six-file figure in the coordinator's own review brief). Two fix rounds then closed all four AND FOUND THREE SITES NO REVIEWER HAD FLAGGED — including `story:63` still carrying verbatim the exact overstatement the review had rejected, missed because the first fix brief named artifact sites and not the story's own section. Only the coordinator verified the result. **The coordinator recommended one further independent pass over the fixed state, citing this project's own record that round-3 fixes produced round-4 headline findings; the owner elected to close instead.** If a later round finds something in this change set, that is the reason, and it was known at closing time. ONE DEFECT WAS INTRODUCED BY THIS CHANGE SET AND CLOSED INSIDE IT: rewriting the old epic block deleted the only home of the owner's 2026-07-31 refusal to mount `<Toaster />`, while another part of the same change set still cited it — that fact now lives in the Story 17.9 block, framed as the constraint it is rather than as a reversal. NOT SWEPT, stated so it is not read as a clean bill of health: `stories/**` was not audited for sentences AC-9 falsified; `24-1-string-catalogue-switcher-and-lang.md:180` surfaced incidentally and is deliberately left as a `done` story's dated record, on the same reasoning applied to two 2026-07-31 architecture-review files, and needs re-reading against the repaired spine if Epic 24 work resumes. Story 17.9 inherits the live questions: the consumer enumeration AD-24 asks for returns nothing today (`toast(` has zero call sites), the segment's edge is decided by `src/app/login/page.tsx` being ungated and not room-facing, and the half of AD-24 all of this rests on is asserted by nothing — filed in deferred-work.md with 17.9 as owner.

### `17-7-projected-shell-route-group` — backlog

17.7 added 2026-07-31 at the owner's direction, from round 2 of 17.1's code review. It owns AC-4's *shell* guarantee — `html`/`body`, which no component in the projected tree can see — after 17.1 was scoped in writing to AC-4's *token* guarantee. Four paths leak it and no hook can reach any of them: first paint on every projected load (the leaked paint is the server's, so `useLayoutEffect` is not a fix), the two Server-Component error branches, `notFound()` at six sites with no not-found.tsx/error.tsx anywhere in `src/`, and any future route shell. Owner chose one route-group layout over four point-fixes, because only the layout catches the fourth. This story is what takes AD-24 from [ADOPTED, partial] to [ADOPTED], so it carries an `EXPERIENCE.md` IA-table update and a `bmad-architecture` Update run in the same change set, per AGENTS.md.

### `17-8-guard-criteria-encoding` — done

2026-08-03 bmad-code-review: all six patch findings fixed; seventh combined injection compiled under tsc and made the fixed guard fail, then reverted; focused 54/54, public guard 5/5, tsc clean, lint 31 unchanged.

### `17-9-toast-channel-wiring` — backlog

17.9 added 2026-08-05 by Story 17.6, the story that ratified the combined inline + toast channel rule. Registered as a forward owner rather than folded into Story 17.7, on the Story 17.8 precedent (kept out of 17.7 on purpose so that story stays scoped to the shell closure and its own two AD-24 findings). Depends on Story 17.7 — more precisely, on an operator-scoped route segment existing, whoever creates it. AD-24's narrowest-layout mount rule makes that segment the toast provider's mount; the root layout is the wrong mount because the two room-facing routes inherit it too. Story 17.7's own registered contract creates the room-facing route group, not necessarily the operator-scoped one — if that split leaves the operator routes directly under src/app, this story creates the operator-scoped segment itself rather than wiring into one 17.7 built, and a per-page mount across the eight operator pages is the leaf-widening pattern Story 17.8 closed the guard against, not a substitute.

### `epic-18` — backlog

Realizes FR-18 + NFR-6. Kept separate from Epic 17 deliberately: one epic is what an operator sees, this one is what a visitor must never see. Pressure-test watch-list item L4, accepted un-actioned, now coming due.

### `epic-19` — retired

Created 2026-07-30 at the owner's direction, to give the song-title-skip item a tracked home instead of the bare "Consider" it had carried since 2026-07-29. NOT opened as a Story 15.2: Epic 15 is `done`, and reopening a closed epic is the contradiction Correct Course closed on Epic 14 the day before. MAY BE ABSORBED: spec-artifact-registry-authoring CAP-1 targets exactly the TypeScript plan constants this epic moves. If that SPEC is adopted, fold this in rather than deliver it twice. RETIRED 2026-07-30 by owner decision (AD-20). Goal met, method discarded: the three skipTitle songs (#671, #684, We Have This Hope) become hand-edited General registry entries, and a General generates no title slide — so skipTitle is DELETED, not moved into data. Nothing to migrate, no flag to store. Work happens in Story 20.1 seed.

### `epic-19-retrospective` — n/a

nothing was delivered to retro

### `epic-20` — in-progress

Created 2026-07-30. Owner decision: spec-artifact-registry-authoring is the final change, adopted whole. That SPEC is marked "Canonical contract", declares it supersedes Story 16.1 non-goals and that "this SPEC wins" over conflicting Epic 16 companions — and until now was referenced by NO epic, story or sprint key. An artifact claiming authority over shipped code with no delivery status is the inverse of the 2026-07-29 drift: there code ran ahead of docs, here docs ran ahead of code and nothing recorded that it was waiting.  TWO BREAKING CONSEQUENCES, both the SPEC's explicit instruction: (a) seven base types collapse to three kinds (General/SongSet/Announcement). SPEC Constraints retires TextPlaceholder/ImagePlaceholder/MixPlaceholder/ FullScreenImage by name. Placeholders stop being a kind and become elements inserted onto a General from the Placeholder Catalog (CAP-4). (b) CAP-6 REVERSES AD-14 (registry edits are global and immediate, no per-service override by design). Story 20.8 must not ship before the epic-16 architecture spine carries a new AD-n superseding AD-14 — never renumber — and EXPERIENCE.md Venue & Projection Constraints + Flow 5 are reconciled.  Epic 19 is a SUBSET: story 19-1 moves the skipTitle literals, and CAP-1 forbids exactly those "TypeScript plan constants". Deliver inside 20.1 or retire Epic 19. Migration is cheap only while no production exists — after deploy the same change needs a backfill over live artifact_templates rows and every service snapshot.  OPENED 2026-08-07 with Story 20.1's context. Two things the epic left open were answered there, because 20.1 is Epic 20's first release and both come due at it: (1) AD-21's global monotonic data version counter LANDS IN 20.1. The spine recorded the decision as made and its landing story as unassigned, arriving "with Epic 20's first release". Three reasons made deferring worse: AD-21 requires the counter to be stamped by the AD-17 bootstrap in the SAME TRANSACTION as the seeding marker, and 20.1 is the story that writes that transaction; AD-18 says the collapse "folds into production data version 1", which presupposes a version 1 exists for 20.2 to fold into; and 20.1's own ordering change needs the answer, since giving existing developer rows a position is a VALUE change. Under AD-21 the answer is "reset the developer database to the compacted version", enforceable only once a database reports a version. Safe without a row-by-row migration because AD-4 records that NO DEPLOYMENT EXISTS — a licence that EXPIRES AT FIRST DEPLOY, and 20.1's change set must say so rather than leave it to be re-derived. (2) The base_type seven-to-three collapse STAYS IN 20.2 and 20.1 does not touch ARTIFACT_BASE_TYPES or READ_ONLY_BASE_TYPES. They coexist because ordering is orthogonal to kind: the position is a column on every row whatever its base_type, and every row 20.1 authors uses a value that is already valid today (general for the four lyric pages, song-set for the four song positions). 20.2's collapse rewrites base_type and payload in one statement per AD-18 and must not touch the position, which it cannot — the position has one home and no payload copy.  WHAT 20.1 DOES NOT CLOSE, recorded here so a later reader does not assume otherwise: AD-20. The planner still renders four weekly SongSet groups plus an unbounded number of middle Divine Service songs. 20.1 gives the fixed four separately ordered transitional rows (`bt-opening-song`, `bt-closing-song`, `ds-opening-song`, `ds-closing-song`) but introduces NO AD-19 slot identity or binding key; it also retains `song-set` as the ds-middle layout source. Story 20.7 owns all four `songset-*` identities, replaces `song1Number..song4Number`, and removes or migrates these five rows with the closed set and unclaimed-hymn behaviour. Until then every slide EXCEPT those five SongSet groups originates from an ordered registry entry, and no [TARGET] tag may be flipped on that basis. 2026-08-08 two stories added by Correct Course (20.9, 20.10) — the epic now

### `20-1-ordered-registry` — done

carries NFR-3's owner as well as its capabilities. 20.2 is STILL the next unit to build; neither new story reorders it. PRIOR ENTRY # 2026-08-07 first story contexted; 20.1 done 2026-08-07 CLOSED BY THE OWNER. Merged as PR #37 (merge commit

### `20-2-three-slide-kinds` — in-progress

bc487b3), both checks green. All 13 ACs carry a recorded disposition and none is open. ONE THING THE CLOSE DOES NOT COVER, recorded because the owner closed with it stated rather than hidden: the FINAL state after fix round 2 was verified only by the coordinator (492 tests / 491 passed / 0 failed / 1 skipped, public-repo guard 9/9, forbidden-path audit clean) and was never read by an independent reviewer. Both round-2 changes are textual — one duplicate log line removed, one test comment corrected. This repository has seen a fix round surface the next round's headline finding; the owner elected to close anyway. If a later round finds something here, this is why. DEBT THIS STORY HANDS FORWARD: AD-20 is NOT closed (see the block above) — Story 20.7 owns replacing the five transitional SongSet rows with AD-19's four identities AND deleting song1Number..song4Number, in one change set. PRIOR ENTRY # 2026-08-07 implemented (bmad-dev-story): full suite green (npm run build && npm test, 489 passed / 1 skipped [gitignored source deck] / 0 failed), public-repo-guard green, skipTitle and songset- both zero occurrences in src/. Context below from bmad-create-story (2026-08-07 context created (bmad-create-story)). Scope: ordering column on artifact_templates; the ordered registry snapshot becomes the sequence source buildSlidePlan consumes for every registry-authored entry (AD-7 unchanged — only where the sequence comes from); skipTitle DELETED at all five sites (:140, :148, :438 intercessory-671, :460 intercessory-684, :550 hope — re-verified against the source, :460 is NOT the Special Song and no skipTitle site touches it); the three fixed liturgical songs become FOUR hand-authored General rows, one per lyric page (measured against the shipped corpus 2026-08-07: #671 → 1 page, #684 → 1, We Have This Hope fixed split → 2); the seed is re-authored as an ordered 38-row list, including four transitional SongSet rows but no `songset-*` identity; AD-17 bootstrap-once with the read-time gap-fill at registry-snapshot.ts:85-90 removed and tests/registry-reseed.test.mjs:337 INVERTED, not deleted; AD-21's counter. GATE — the story must not edit ARCHITECTURE-SPINE.md. Four Deferred statements go stale when it lands (the counter's unassigned owner, seed_hash becoming work without a job, the "no ordering column" surface, and the AD-20 cost of the three liturgical songs) and repairing them, including any tag flip, is a bmad-architecture Update run. 2026-08-08 contexted, validated (3 rounds, 0 blockers) and

### `20-3-add-delete-rename-reorder` — backlog

implemented; the two-reviewer panel ran THREE times across TWO fix rounds, every finding adjudicated against the cited lines rather than by vote. Seven base types collapse to three kinds; the 38-row seed is re-authored onto them and every row label rewritten into the operator vocabulary that `TEMPLATE_LABELS` held, because that map was consulted BEFORE the row's own label for 33 of 38 ids and made CAP-5's success clause structurally unreachable. Deleting it and re-authoring the labels ship together or every Presenter badge regresses. TWO FINDINGS DECIDED THE STORY AND NEITHER WAS IN THE ARTIFACTS: `TEMPLATE_LABELS` above, and that all eight rows moving onto `general` declare placeholders while the General case of `enforceBaseTypeRules` forbade exactly that (`validate.ts:366-368` AT THE 553a4c5 BASELINE — that branch is deleted by this story, so the citation is historical, not current) — a naive collapse throws inside the first-boot bootstrap. GATE — ARCHITECTURE-SPINE.md was NOT edited from inside this change set and no [TARGET] tag was flipped. A `bmad-architecture` Update run is OWED: AC-4 answers a schema question the spine's own Deferred section delegates to this story by number, and eight spine statements go stale — see the story's Gate for the enumerated list. DOC DEFECT RAISED, NOT FIXED HERE: `epics.md:432` maps `fullscreen-image → Announcement`, which contradicts AD-19, `slide-kinds.md`, `placeholder-catalog.md` and CAP-4. The code settles it — `sermon-flyer` is filled from `ctx.sermonGraphic` (`slide-plan.ts:551-560`), not `ctx.flyers` (`:698-706`). The story implements the SPEC (`general`) and leaves `epics.md` to its owner, because that file carries uncommitted Correct Course edits.

### `20-9-readability-guarantee` — backlog

2026-08-08 registered by Correct Course as NFR-3's FIRST

### `20-10-font-set-is-closed` — backlog

owner — epics.md carried "None" for this NFR from 2026-07-29 until today. AC-1 (build-time fit assertion over the shipped seed) is NOT gated on 20.10: the seed carries zero fontFamily overrides, so every element is Arial and the check runs today. AC-2 (the two renderers must agree) IS gated, or must be scoped in writing to the closed set as it then stands. Measured defect: estimateTextFitScale pins contentWidth: 0 and counts only authored newlines, so wrapping can never force a PPTX shrink — intercessory-671-lyric-1 (305 chars, zero line breaks) bakes scale 1.0 against a web-measured ~0.77. AC-3 resolves two code comments that contradict each other about whether PowerPoint's autofit covers the remainder. 2026-08-08 registered by Correct Course from the owner's

### `epic-20-retrospective` — optional

decision the same day: several fonts eventually, from a list fixed in code; target machines get them installed; an Admin cannot add one. CARRIES A GATE of the same shape Story 20.1 had — it fixes what data may exist, so ARCHITECTURE-SPINE.md must NOT be edited from inside its change set; AD-30 is written by a bmad-architecture Update run. Ships with one member (Arial). Today the rule is held ONLY by ArtifactEditor exposing no font control; validate.ts:135-140 accepts any non-empty string, while its neighbour aspectRatio is a literal that throws.

### `epic-21` — in-progress

Created 2026-08-01 by Correct Course. No architecture gate: it seeds empty tables from zero, so AD-21 does not reach it. No Epic 20 dependency either — startable immediately, in its own worktree. Realizes FR-19 (the last product Partial) and the new FR-22.

### `21-1-verse-database-ships` — done

done 2026-08-01: data/bible/kjv.json committed (4.36 MB) and seeded from zero on first boot. Completeness asserted structurally in tests/corpus.test.mjs — 66 books / 1,189 chapters / 31,102 verses — not sampled. The .work/tp_bible_* export was deleted only after that assertion was green, in that order. PATH SUPERSEDED 2026-08-01 by FR-24, after this story closed: the corpus moves to data/en/bible-translation/kjv.json. This story's AC names data/bible/kjv.json and is left standing as the record of what shipped — Story 21.2 performs the move. Superseded in writing (story file + epics.md), never silently rewritten.

### `21-2-translation-is-a-parameter` — done

AMENDED 2026-08-01 (FR-24): also performs the data/<locale>/bible-translation/ path move, the translation -> translation_code rename, and isKjvCorpusEmpty() -> isBibleTranslationEmpty(code). Never-filter rule binds: listing endpoints return every installed translation with its locale; no WHERE locale = <default> reaches the database. 'KJV' literal survives at scripture.ts :6 :106 :128 :144 (measured). STORY FILE 2026-08-01: stories/21-2-translation-is-a-parameter.md. Two things the epic text understates and the story now owns, both assigned by the spine's Deferred by story number: the AD-25 RECONCILE for the bible family (the seed's early return goes, removal arrives, a bad file reconciles nothing) and the GUARD that AD-25's closure and AD-26's never-filter rule both rest on. Two corrections against measurement: the 503 clause the epic asks for was already done by 21.1, and three operator-facing KJV labels were uncounted (PresenterOperator.tsx:692 is assigned to this story by EXPERIENCE.md:45; CreateForm.tsx:581 / EditForm.tsx:612 are a recorded scope call).

### `21-3-default-translation-and-presenter-choice` — backlog

AMENDED 2026-08-01 (FR-24): owns default_bible_translation AND the shared default_data_locale. The translation control opens on the default locale and carries an always-present way to browse the others. settings.ts is the append-shaped contact point with Story 22.3 — one const + getter/setter each.

### `21-4-book-names-belong-to-the-translation` — backlog

NEW 2026-08-01 (FR-24). Book names ship inside each translation's corpus. SUPERSEDED THE SAME DAY by the input-model Correct Course (third run of the day): input is NOT generous. Search is scoped to the CHOSEN translation on operator surfaces; only the rundown matches all installed translations, because a Telegram sender picks none. ONE matcher, scope as a parameter. Longest-prefix match over corpus-supplied names replaces the regex, so the two-word cap and the hyphen stop being concepts rather than being widened. BOOK_ALIASES is REPLACED, not deleted: non-prefix aliases (Jn, Mt) live in the matcher and BELONG TO A TRANSLATION, so Kej must not resolve under KJV. shortName dropped - 6 sites, measured. Crosses an epic boundary: parser.ts:152/:162 carry a second copy of scripture.ts:42's regex, and one rule may not have two implementations (spine Boundaries convention). The canonical-identity SHAPE is the bmad-architecture Update's, not this story's.

### `21-5-one-field-inline-autocomplete` — backlog

NEW 2026-08-01 (FR-24), the operator half of 21.4 - client only, so the two run in separate worktrees over an append-shaped contact point (the matcher's suggestion endpoint). One field with inline autocomplete, NOT a book picker plus chapter/verse fields: the owner chose this over the recommendation, and it is why paste survives and why the rundown needs no second mechanism. Accepting a suggestion leaves the FULL corpus name in the field, which is what pays for dropping shortName. Precedent HymnNumberAutocomplete.tsx (4 mounts in CreateForm.tsx, 4 in EditForm.tsx) - copy its debounce, settled cache and in-flight dedup, not merely its shape. No proxy.ts change: /api/scripture and /api/hymns carry no exclusion, so a sibling suggestion endpoint is gated by default and proxy-matcher.test.mjs is untouched.

### `epic-22` — in-progress

22-2 is BLOCKED on a bmad-architecture Update: no AD governs a shipped reference corpus, and correcting 695 persisted titles is AD-21 case work whose counter does not exist (spine :374). It also needs the owner-supplied number-to-title list — see action_items. 22-3 is GATED on Story 20.7: a per-song book qualifier hangs off the binding AD-19 governs, and AD-19 DELETES song1Number..song4Number rather than aliasing them, so building it earlier ships four fields Epic 20 then removes.

### `22-1-song-book-ships-with-book-code` — done

done 2026-08-01: corpus moved to data/song-book/sdah.json carrying book.code, book.attribution and the takedown statement; hymns rebuilt as UNIQUE(book_code, number) with a one-time boot migration recording existing rows as SDAH; import:hymnal RETIRED (its .work/lirik-lagu.json source does not exist), replaced by corpus:verify. The committed file is now the source of record. PATH + COLUMN SUPERSEDED 2026-08-01 by FR-24, after this story closed: corpus moves to data/en/song-book/sdah.json and book_code is renamed song_book_code. This story's AC names the old spellings and is left standing as the record of what shipped — Story 22.3 performs both. The UNIQUE(..., number) constraint, the attribution and the retired importer are unaffected.

### `22-2-hymn-title-is-a-title` — done

done 2026-08-01: all 695 titles replaced from the owner-supplied index, which arrived the same day and cleared the blocker action_items recorded. Fixed in the corpus, not hand-patched per row.  The architecture gate was NOT bypassed and is NOT closed. Story 22.2 shipped without an AD because it introduced no channel: upsertHymns already overwrote title and lyrics from the corpus on every boot, so corrected titles propagate through the path that was already there. What the bmad-architecture Update still owes is whether that every-boot channel should SURVIVE for a shipped reference corpus — see action_items. If the answer is AD-21's counter, the migration target is upsertHymns, not this data.

### `22-3-default-song-book-and-per-song-override` — backlog

AMENDED 2026-08-01 (FR-24), and the amendment is most of the story now: owns default_song_book AND reads the shared default_data_locale; performs the data/<locale>/song-book/ path move and the book_code -> song_book_code rename superseding Story 22.1; makes the EIGHT unqualified FROM hymns read sites book-aware (the ninth, db/index.ts:47, is the boot migration, not a read site). Never-filter case that must work: an Indonesian service singing one English hymn — no setting changes. STILL GATED on Story 20.7 (AD-19 slot identities).

### `epic-23` — in-progress

Neither corpus family owns this; kept separate so scripts/, tests/ and docs/ are not under two epics at once.  Sequence: 23.1 first. Story 23.2 then waits on both 23.1's demo seed and Story 22.3's move from data/song-book/ to data/<locale>/song-book/. Story 22.3 remains gated on Story 20.7.

### `23-2-fresh-clone-verified` — backlog

Partial work delivered 2026-08-01: tests/corpus.test.mjs rejects retired import commands, data/hymns.json, and data/bible/ in reader-facing tracked files. The FR-24 data/song-book/ criterion remains open until Story 22.3 performs that path move. The fresh-clone end-to-end assertion also remains open until Story 23.1 supplies npm run seed:demo. No Story 23.2 file exists, and no implementation is active.

### `epic-24` — in-progress

Created 2026-08-01 by the SECOND Correct Course of the day. Realizes FR-25 (UI Locale). Deliberately separate from Epics 21/22, which carry the DATA half (FR-24) of the same day's locale work: the two halves share a word and nothing else — no table, no module, no test, no file. This epic touches no corpus file and no corpus table; Epics 21-22 touch no .tsx string. Own worktree, no gate. Measured 2026-08-01: NO i18n infrastructure exists — lang="en" hard-coded at src/app/layout.tsx:31 is the entirety of it. 39 .tsx, 26 client components, ~55 JSX/attribute literals + ~158 src/lib and src/app/api messages (many dev-facing). Estimate 100-150 real translatable strings. in-progress 2026-08-01: first story of the epic written (bmad-create-story).

### `24-1-string-catalogue-switcher-and-lang` — done

Infrastructure half: string catalogue, ui_locale setting on the shipped per-concern pattern, switcher, <html lang> following the setting. An UNRESOLVED KEY MUST BE VISIBLE AS A DEFECT, never rendered blank. ui_locale is the fourth of FR-24's four settings keys and the only one this epic owns. STORY FILE 2026-08-01: stories/24-1-string-catalogue-switcher-and-lang.md. Four mechanism decisions 24.2 inherits 100-150 times, so they are ACs: the catalogue is CODE not database rows (AD-25 excludes it from the corpus channel by name); NO second root-level client provider (server-read state travels as an initial* prop, and AD-24's Deferred already holds that question open for Story 17.6); a missing key never falls back to English, with key-set parity asserted so the sweep adds keys in pairs; and the UI-locale vocabulary is NOT the data-locale vocabulary — catalogues ship with the build, corpora are installed. Own component, not a shared language card: EXPERIENCE.md:73 says per-concern, and a shared card is a file Stories 21.3/22.3 would each have to rewrite.

### `24-2-the-strings-move` — backlog

The sweep. Includes the slide-plan.ts operator-facing labels ('Welcome', 'Opening Song', 'Congregation, please stand', 'Prayer Partners', 'Break Time') — verified 2026-08-01 they populate LegacyProjection (slide-plan.ts:76), read ONLY by present/presenter-model.ts and SlidePreviewList.tsx; pptx.ts and the projector read it zero times. OUT OF SCOPE and stated so the drive-by fix is a decision: slide-plan.ts:261 hard-codes 'Bandung International Community', a church name in code — not a translatable string, different capability.

---

## Action items

Keyed by the `action` text as it now appears in `sprint-status.yaml`. An item appears here
when its action text was shortened, when its status carried a completion narrative, or both.

### Epic 16 — Inspect a generated deck and the projector BEFORE FIRST USE (registry now owns layout; only order/content are machine-verified).

- **owner:** "kodesh87"  |  **status:** `open`

**Full action text as originally written:**

> Inspect a generated deck and the projector BEFORE FIRST USE (registry now owns layout; only order/content are machine-verified). Reframed 2026-07-29: the owner confirmed there is no production deployment yet, so this is a pre-launch gate, not a pre-Sabbath one. The readiness report ranked it the single highest-consequence open item on the premise that a live service depended on it — that premise was wrong, and the urgency drops accordingly. The check itself does not: nobody has looked at a projector since Epic 16 moved layout ownership into the registry

### Epic 16 — Reset welcome, verse-reading, special-song, family-youth, bible-verse-contemplation on the FIRST DEPLOYED DB, if its rows predate the current seed.

- **owner:** "kodesh87"  |  **status:** `open`

**Full action text as originally written:**

> Reset welcome, verse-reading, special-song, family-youth, bible-verse-contemplation on the FIRST DEPLOYED DB, if its rows predate the current seed. Reframed 2026-07-29: the owner confirmed there is no production deployment, so there is no stale production row to reset today — this becomes a first-deploy check rather than an outstanding repair. Diagnose it with `npm run registry:doctor` against the deployed DB_PATH; it reports per template what the seeder would do. NOTE (corrected 2026-07-29): the 'missing-only seeding' premise is obsolete — src/lib/registry/seed.ts now self-heals at startup via reseedArtifactTemplateIfUntouched, and src/lib/db/index.ts backfills seed_hash once for pre-column rows. A rebuild+restart re-seeds any row still provably untouched; only rows reported 'kept (edited by an administrator)' or 'kept without a recorded seed hash' need a manual Reset. Read the [registry] seed lines in the startup log to see which. Manual path: /admin/artifacts editor Reset button, or POST /api/admin/artifacts/<id>/reset with {updatedAt}

### Epic 16 — Add a seed conformance test: every declared placeholder bound to exactly one element, every planner template id present

- **owner:** "Developer"  |  **status:** `done`

**Status narrative:**

2026-07-29 tests/registry-seed-conformance.test.mjs (6 assertions). Covers the direction validate.ts does not: a declared-but-unbound placeholder, whose value is computed then never rendered — the failure pptx-content.test.mjs records for the verse-reading citation, special-song performer and welcome date. Also asserts no layout binds one placeholder twice, and every literal templateId in slide-plan.ts exists in the seed

### Epic 16 — Add a ceiling assertion for deck bytes / generation time

- **owner:** "Developer"  |  **status:** `done`

**Status narrative:**

2026-07-29 tests/pptx-ceiling.test.mjs. Measured baseline: 53 slides, ~4.2s, 29.99 MB. Ceilings 120 slides / 30s / 45 MB — regression signals with headroom, not NFR-2 breaches. Actuals print on every run so drift is visible before the assertion trips. NOTE: 30 MB is dominated by full-bleed background images, not slide count; it is the file an operator downloads before a service (NFR-1)

### Epic 16 — Repair the two pre-existing stale checks in scripts/smoke-deck-fidelity.mjs left from the Epic 14 field renames

- **owner:** "Developer"  |  **status:** `not-reproducible`

**Status narrative:**

2026-07-29 ran npm run build + node scripts/smoke-deck-fidelity.mjs: 30 checks, 30 PASS, 0 FAIL, exit 0. Includes the seven static source greps and the EditForm field check the Epic 14 renames would have broken. Either the checks were repaired before this run or the readiness-report finding was wrong. Nothing was changed — there was no failing check to repair. Re-open with the failing check name if it resurfaces

### Epic 15 — Consider moving the hardcoded song-title skip rules out of slide-plan.ts now that the Artifact Registry exists

- **owner:** "Developer"  |  **status:** `done`

**Status narrative:**

2026-07-30 at the owner's direction: converted from a "Consider" into Epic 19 / story 19-1-song-title-suppression-as-data, with the three slide-plan.ts call sites and their liturgical reasons tabulated in epics.md. The item is closed as an ACTION ITEM, not as work — a bare "Consider" owned by "Developer" is how a decision stays unmade, so it now lives where sprint planning can see it. NOT reopened as Story 15.2: Epic 15 is done

### Epic 16 — Operator tooling: scripts/registry-doctor.mjs (npm run registry:doctor) reports what the seeder would do to every template on a given DB_PATH, and applies only the resets startup will not do itself.

- **owner:** "Developer"  |  **status:** `done`

**Full action text as originally written:**

> Operator tooling: scripts/registry-doctor.mjs (npm run registry:doctor) reports what the seeder would do to every template on a given DB_PATH, and applies only the resets startup will not do itself. Reuses the app's own serializeTemplate/hashTemplatePayload/resetArtifactTemplate so it cannot drift from the behaviour it describes

**Status narrative:**

2026-07-29 user-requested tooling. Exists because the 'reset five template rows' item was written against insert-missing-only seeding and could not tell an admin-edited row (must not be reset blindly) from an untouched one (startup fixes it)

### Epic 14 — Assert that the create/edit form preserves operator-chosen Announcement List order (FR-11b). The date-collision override is already covered by tests/services-lib.test.mjs:161

- **owner:** "Developer"  |  **status:** `done`

**Status narrative:**

2026-07-29 tests/services-create.test.mjs — 'announcement sync preserves operator-chosen order, including a reorder'. Asserts the reorder case the prior coverage missed (its inputs were already in stored order, so a position-ignoring sync stayed green), plus distinct ascending sort_order so slide order cannot be arbitrary. Ties FR-11b to FR-6 'in list order'

### Epic null — Route UX findings F4-1 (protagonists contradict PRD: Sari/Bimo/Elen are authoritative), F4-3 (UJ-3 has no flow), F4-4 (UJ-5 demoted to a branch), F4-5 (FR-16 blanking / NFR-5 unmapped-input channel / …

- **owner:** "kodesh87"  |  **status:** `done`

**Full action text as originally written:**

> Route UX findings F4-1 (protagonists contradict PRD: Sari/Bimo/Elen are authoritative), F4-3 (UJ-3 has no flow), F4-4 (UJ-5 demoted to a branch), F4-5 (FR-16 blanking / NFR-5 unmapped-input channel / NFR-2 progress state have no UX owner), F4-6 (congregation-facing deck has no UX document) through bmad-ux Update. ADDED 2026-07-29: DESIGN.md open item 2 states the dark palette is unreachable dead code. Verified false — PresenterOperator.tsx:449 and SlideGridDialog.tsx:176 pin the class themselves, so the palette renders today in the two surfaces used during a service. Left for bmad-ux rather than corrected in place, since that skill owns the file; the correction is recorded in story 17-1 so the implementer is not misled meanwhile

**Status narrative:**

2026-07-30 bmad-ux Update ran. SCOPE WAS WRONG WHEN WRITTEN: the UX .memlog.md already recorded F4-1, F4-3, F4-4 and F4-5 as closed by the 2026-07-29 run (protagonists renamed to Sari/Bimo/Elen with UJ labels; flows expanded 5→7; FR-16 blanking + NFR-5 + NFR-2 added as states), and Correct Course itself closed F4-7 by adding EXPERIENCE.md to epics.md inputDocuments. This item re-listed work already done because it was written from the readiness report rather than from the memlog. Actually delivered 2026-07-30: dead-code claim corrected in DESIGN.md; F4-6 closed as a stated ownership boundary in both spines (no readability standard invented); all four per-surface states verified against src/ — three shipped, "empty plan" unreachable by construction, "lost sync" NOT shipped → new story 17-5; every Open Item in both spines now names its owning story key

### Epic null — Close or waive the three unrecorded Phase-1 go/no-go spikes (PRD §6): font proven on a clean machine, church fidelity sign-off on a sample slide set, 5-10 historical Rundown corpus

- **owner:** "kodesh87"  |  **status:** `partially-done`

**Status narrative:**

2026-07-29 owner decision: the two gates requiring the church are WAIVED and will not be sought. Recorded in PRD §6 with the risks carried rather than closed — fidelity is unvalidated by the people who will see it (an adoption risk no test can detect; the pre-launch projector inspection is the compensating control), and the parser stays fit to one sample, so the first unfamiliar rundown is where real variance gets measured. The font-on-a-clean-machine gate is technical and remains open with the maintainer

### Epic null — Create the epic and stories for the readiness-report product defects

- **owner:** "kodesh87"  |  **status:** `done`

**Status narrative:**

2026-07-29 owner asked for the dark-mode work to go through an epic/story. Delivered as TWO epics rather than one, because C5-1's remediation applies the user-value standard to new epics: Epic 17 is what an operator sees (dark mode, muted-foreground contrast, app metadata, canvas dirty-state guard), Epic 18 is what a visitor must never see (in-route authorization for the nine proxy-only routes; FR-18 + NFR-6). Stories 17.1 and 17.8 have full story files; 17.8 is now at `review` and the remaining planned stories are `backlog`. Epic 17 records its own requirement ancestry as a DECISION — operator-chrome visual identity is DESIGN.md-governed, not FR-governed — rather than leaving the silence FR-20 had to be added to fix

### Epic null — SUPERSEDED 2026-07-29 — merged into the single purge item below. The history rewrite covered the F4-2 names and the second-instance names in one operation, and one Support ticket covering all eight pre-rewrite SHAs settles both.

- **owner:** "kodesh87"  |  **status:** `done`

**Full action text as originally written:**

> SUPERSEDED 2026-07-29 — merged into the single purge item below. The history rewrite covered the F4-2 names and the second-instance names in one operation, and one Support ticket covering all eight pre-rewrite SHAs settles both. Two separate items would have implied two tickets

### Epic null — SECOND PII instance, found 2026-07-29 by widening the F4-2 sweep from _bmad-output/** to the whole tracked tree: ten real names across data/asset-map.json (slide-56 evidence — a family surname, three …

- **owner:** "kodesh87"  |  **status:** `done`

**Full action text as originally written:**

> SECOND PII instance, found 2026-07-29 by widening the F4-2 sweep from _bmad-output/** to the whole tracked tree: ten real names across data/asset-map.json (slide-56 evidence — a family surname, three given names AND their prayer request, verbatim from the source deck), tests/fixtures/sample-rundown.txt (seven role-holders), docs/QUICKSTART.md, docs/picoclaw-webhook.md, scripts/smoke-deck-fidelity.mjs, tests/parser.test.mjs, tests/pptx-content.test.mjs. Working tree remediated: all replaced with invented names, prayer-request body redacted, twelve fingerprints added to FORBIDDEN_NAME_HASHES. The count grew from ten to twelve during verification: one given name turned out to belong to a real predecessor rather than to the invented brainstorming persona it resembled, and the sermon speaker's full name was found glued into a single token in asset-map.json. Three names are deliberately left unblocked, for the reason the guard already documents: one is a word in 182 hymns and in 'Amazing Grace', one is a figure in the Bible, and one predecessor's given name is also a book of the Bible — that person's surname is what identified them, and the surname is blocked. Verified: git grep -w finds none, guard 4/4, npm test 332 pass / 0 fail, smoke 30/30

**Status narrative:**

working tree only

### Epic null — RESIDUAL of the second PII instance: history rewrite.

- **owner:** "kodesh87"  |  **status:** `done`

**Full action text as originally written:**

> RESIDUAL of the second PII instance: history rewrite. DONE 2026-07-29 — all 12 commits rewritten with git filter-branch (filter-repo is not installed and no external tool was downloaded) and force-pushed with --force-with-lease. origin/main moved 2dd8069 -> a9277ff; every SHA changed. Four passes were needed, and the reason each was needed is the finding: pass 1 the ten known names; pass 2 two more REAL role-holders — the volunteer who built the deck and one predecessor — still present in Initial commit even though the F4-2 remediation reported the history clean, one of them also surviving at HEAD in the brainstorming transcript where it had been mistaken for an invented persona; pass 3 the family surname quoted in abbreviated form in deferred-work.md, glued into one token so no word-boundary sweep can see it; pass 4 the sermon speaker's real FULL name, glued as <Given><Surname> in asset-map.json slides 40 and 50 — a fourth real person, found only while verifying. Verified by replaying the guard's own fingerprint logic over all 603 unique text blobs reachable from main: zero hits on any of its 26 fingerprints. No tracked document cited any of the eight rewritten SHAs, so no reference repair was needed

### Epic null — Residual of the history rewrite: GitHub keeps the pre-rewrite objects and serves them by direct SHA until it garbage-collects on its own schedule

- **owner:** "kodesh87"  |  **status:** `waived`

**Status narrative:**

2026-07-29 owner decision: no Support purge ticket will be filed; GitHub's own garbage collection is accepted as sufficient. What that accepts, stated plainly so it is a decision and not an oversight: the eight pre-rewrite commit SHAs were public while they were reachable, so anyone who noted one can still fetch that commit — including the family name and prayer request — until GC runs, and GitHub does not publish that timing. A clone or fork made before the rewrite is out of reach either way. The local rollback (refs/original plus the verified 36 MB bundle in the session scratchpad) can be dropped whenever you are satisfied: `git gc --prune=now` and delete the bundle

### Epic null — Re-infection vector: scripts/extract-pptx-assets.mjs build-map regenerates data/asset-map.json from the real deck, and its `evidence` field is the slide's own text runs — so re-running it reintroduced real member names and prayer requests.

- **owner:** "Developer"  |  **status:** `done`

**Full action text as originally written:**

> Re-infection vector: scripts/extract-pptx-assets.mjs build-map regenerates data/asset-map.json from the real deck, and its `evidence` field is the slide's own text runs — so re-running it reintroduced real member names and prayer requests. Closed by filtering at the generator rather than by blocking afterwards

**Status narrative:**

2026-07-29. `evidenceFor` now reads the distinction out of the registry instead of a hand-kept list: a template with no placeholders is chrome and its runs stay (that is how the Part C plates were audited); a template that declares placeholders may contribute only runs matching copy it declares in an element `content`. Matching is whitespace-collapsed, case-insensitive, substring-wise, because one declared two-line label arrives as two separate runs. Withheld runs are COUNTED, not silently dropped, so a filtered row stays distinguishable from an unchecked one. Applied to the committed map offline (the deck is gitignored, so build-map cannot run in CI): 7 rows changed — family-youth keeps its two labels and withholds the name plus prayer body, closing-prayer-ds keeps Closing/Prayer and withholds the speaker, sermon/song-set/verse-reading/bible-verse-contemplation withhold everything. Asserted by tests/asset-map-evidence.test.mjs (4 assertions, stated independently of the generator so it cannot agree with the bug). Negative-tested: injecting one payload run fails two of the four. Rule written into AGENTS.md, .agents/AGENTS.md, .cursorrules and .constitution/public-repository.md — the three agent files verified still identical in their shared blocks

### Epic 20 — BLOCKS Story 20.8.

- **owner:** "kodesh87"  |  **status:** `partially-done`

**Full action text as originally written:**

> BLOCKS Story 20.8. Amend the epic-16 architecture spine via bmad-architecture Update: CAP-6 reverses AD-14, which this item called AD-4 because the Epic 16 child spine still existed (registry edits are global and immediate; no per-service override, by design) because a service must hold a cloned snapshot that only Sync Artifact refreshes. Add the next AD-n superseding it — never renumber an existing one — then reconcile EXPERIENCE.md (Venue & Projection Constraints states the global-and-immediate rule, and Flow 5's climax turns on it) in the same change set

**Status narrative:**

2026-07-30 bmad-architecture Update ran on architecture-epic-16. SPINE HALF DONE, EXPERIENCE.md HALF STILL OPEN — the item asked for both in one change set and only one landed, so it is not closed. Delivered: AD-16 supersedes ONLY the "global across services" clause of AD-14 (admin-only authorization and read-through-server-side-modules clauses stand; AD-4 LiveServer durable paths is a different decision and untouched). Owner decided Sync is permitted on any service including one already presented, and CORRECTED the rationale on 2026-07-30: the freeze event is service CREATION (a service is self-contained against the registry from adoption), and what is frozen is the supporting data entry — sermon speaker, sermon flyer, hymnal number per slot — not the generated deck. So there is no historical-fidelity cost to accept; three deliberate non-goals follow instead: Sync is unrestricted, Announcement membership stays live from the master list, and a later structural change need not keep an old snapshot renderable. Three more decisions landed because the gate found them: AD-17 (seed is bootstrap-once — resolves the previously unrecorded AD-11 vs CAP-2 collision where a deleted row left no evidence and was resurrected every boot via insertArtifactTemplateIfMissing), AD-18 (value migrations are explicit and marker-gated, precedent artifact_seed_hash_backfilled; the seven-to-three base_type collapse is a total replacement with no incremental migration under an owner waiver that EXPIRES AT FIRST DEPLOY), AD-19 (a cross-boundary key is a server-owned value the administrator cannot edit — closes the SongSet-slot-to-hymn-number binding and the Placeholder Catalog key set, which both reviewer lenses found independently). AD-19's SongSet half uses the OWNER'S mechanism, decided 2026-07-30 and simpler than the reviewer's proposal: the binding key is the slot's own immutable identity carried by the type vocabulary (songset-bt-open / -bt-close / -ds-open / -ds-close), so identity is immutable by construction and there is no separate identity mechanism. Kind vocabulary stays exactly the three the SPEC fixes (general, song-set, announcement); text-placeholder / image-placeholder / mix-placeholder / fullscreen-image are GONE not renamed, and only song-set expands. Whether the slot identities live in the base_type column or a discriminator beside it is DELIBERATELY LEFT to Story 20.2/20.7 — the spine fixes the invariant, not the schema. Consequence for gap 4: authoring-boundaries.md line 18 offers "Edit Label (and optionally baseType)", which AD-19 forbids for a slot-carrying type — a contradiction inside the new SPEC itself. This also removes Story 20.7's stated "central design decision" from story altitude. Also fixed: AD-6 (optimistic concurrency) was missing from the Inherited Invariants table while Epic 16 already shipped expectedUpdatedAt/RegistryStaleError, and AD-16 made that omission load-bearing since Sync is a service mutation. Reviewer Gate ran sequentially, reviews in archived/architecture-epic-16/reviews/*-epic-20.md. TWO ESCALATIONS TO THE PARENT INITIATIVE SPINE REMAIN, both one-line fixes awaiting the owner: (1) its State convention "services are immutable once presented" MISLOCATES THE FREEZE and is factually wrong, not merely narrow — the owner corrected this on 2026-07-30 and the epic-16 Inherited Invariants table now carries the correction as an escalation row; (2) AD-9's Binds names the bootstrap path where AD-18 operates, so AD-18 could be read as a child decision on parent-owned ground. Item (2) also wants a sync of project-context.md line 85. ARCHITECTURE CONSOLIDATED 2026-07-30 by owner directive, after these notes were written: BMad's default is one spine per project, so the Epic 16 child spine was FOLDED INTO the project spine and its AD-1..AD-9 renumbered to AD-11..AD-19 (AD map published in both files; AGENTS.md never-renumber rule waived once by the owner, recorded as a non-precedent in all three gate files; 36 live citations repaired). The two escalations above therefore stopped being cross-document conflicts and are now Open items INSIDE the one spine, which is where they get settled. Every AD number cited in this file is the post-merge number. SUPERSEDED IN ONE DETAIL 2026-07-30 (later the same day): where this note says AD-18 makes value migrations "marker-gated, precedent artifact_seed_hash_backfilled", that mechanism is gone. AD-21 replaces the per-change boolean with ONE global monotonic data version counter in `settings`, declared while the change is coded as version n -> n+1; unreleased transitions compact into a single transition before release and developer databases are reset to it, while a released version is frozen. Read AD-21 before writing any migration. Also added the same day: AD-22 (free canvas is General's alone; a songset row gets a bounded configuration surface) and the closed six-key recognized set in AD-19

### Epic 20 — Decide Epic 19's fate when Story 20.1 is planned: deliver story 19-1 inside 20.1 or retire Epic 19.

- **owner:** "kodesh87"  |  **status:** `done`

**Full action text as originally written:**

> Decide Epic 19's fate when Story 20.1 is planned: deliver story 19-1 inside 20.1 or retire Epic 19. CAP-1's success criterion forbids exactly the TypeScript plan constants 19-1 moves, so shipping both means doing the same work twice with two owners

**Status narrative:**

2026-07-30 RETIRED — and by a route neither option in this item anticipated. The owner decided the three suppressed songs (#671, #684, We Have This Hope) become hand-edited General registry entries rather than data-driven song entries. A General generates no title slide, so skipTitle is REMOVED not migrated: there is no flag left to store anywhere, so there was never a second implementation to avoid. Recorded as architecture AD-20 — the planner injects nothing the registry did not ask for; buildSlidePlan applies rules and does not hold them. Epic 19 goal met, method discarded; epic-19 and 19-1 set to retired. ALSO CORRECTED the same day while grounding the decision in source: epics.md named slide-plan.ts:460 as "Around the Special Song" — the source shows group intercessory-684, and NO skipTitle site touches the Special Song at all. That table was the only record of these three sites and Story 20.1 was told to rely on it

### Epic 20 — Gap 4 — bmad-spec Update.

- **owner:** "kodesh87"  |  **status:** `done`

**Full action text as originally written:**

> Gap 4 — bmad-spec Update. Three notes AD-19 shed when the architecture spine was trimmed to Binds/Prevents/Rule on 2026-07-30; an initiative-altitude spine does not carry story-level reconciliations, but nobody should have to rediscover them: (1) authoring-boundaries.md offers 'Edit Label (and optionally baseType) in the slide inspector', which AD-19 forbids for a slot-carrying identity — a contradiction inside the new SPEC itself; (2) slide-kinds.md describes SongSet as 'One row per song slot' without saying that the slot's IDENTITY is what the hymnal binding hangs on; (3) placeholder-catalog.md defers key spelling ('Exact key names follow implementation naming') and lists a mix of sermon.speaker, familyPrayerRequest, serviceDate — free while the catalog was a UI list, but under AD-19 the key is server-enforced and persisted into saved layouts, so it is chosen once (Story 20.5) and is an AD-18 / AD-21 versioned-migration concern after that. TWO MORE for the same pass, added by the 2026-07-30 owner session: AD-19 now states the recognized entry set is CLOSED at six keys over three kinds (general, songset-bt-open, songset-bt-close, songset-ds-open, songset-ds-close, announcement) and that bare song-set names the kind but is never an entry key — so any SPEC text implying a single SongSet row needs reconciling, and the seed's one song-set row becomes four; and AD-22 is new, fixing that free canvas is General's alone while a songset row gets a bounded configuration surface (two background images, font style, font size) with layout composition developer-owned

**Status narrative:**

2026-07-31 bmad-spec Update on spec-artifact-registry-authoring. All five routed notes closed, plus THREE cross-document findings the 2026-07-30 review explicitly folded into this same pass and this item did not name — F-7 (placeholder-catalog.md:8 admitted catalog placeholders on ANY slide, contradicting both AD-22 and its own :34), F-24 (authoring-boundaries.md gave the SongSet surface as the bare word "backgrounds", so Story 20.7 would have shipped with no font controls; and claimed Presenter/PPTX read a service snapshot directly, which AD-12/AD-16 forbid), F-25 (slide-kinds.md left Announcement membership per-service where AD-16 keeps the master list live). Eight items, not five. Landed: authoring-boundaries.md drops the editable baseType from the inspector step and gains a "What the administrator may not edit at all" section — kind, slot identity and catalog key are server-owned, set at creation, closed at six keys over three kinds; slide-kinds.md now leads with the four slot IDENTITIES as the binding key and names the three consequences (reorder and rename cannot touch a binding, delete leaves it inert with the entered hymn number surviving per AD-16); placeholder-catalog.md retires the "exact key names follow implementation naming" deferral (chosen once in Story 20.5, an AD-18/AD-21 migration concern after that) and narrows insert to General rows only. SPEC.md kernel took three minimal edits — CAP-2 success gains the claim-an-unclaimed-slot clause, CAP-5 success gains "the label is the only part an administrator edits", one new Constraint states the cross-boundary-key rule; Constraints 8 -> 9, CAP-1..8 unchanged and nothing renumbered. TWO THINGS WORTH KNOWING. (1) One consequence the review did not name: narrowing :8 to General-only would have left hymnNumber / songTitle / lyric slots / announcement image slots standing in an ENFORCED admitted key set that only a General may insert and no General can carry — they are removed as system-expansion internals rather than left as a new contradiction. (2) One DERIVED reading needs owner ratification: AD-19 fixes that the set is closed, that at most one row carries each slot identity, and that a deleted slot leaves an inert binding — it does NOT say in so many words that adding a SongSet row means claiming an unclaimed identity. That is the only reading under which CAP-2 keeps its add verb and AD-19 keeps its closed set, so it is written into SPEC.md and both companions and logged as an assumption at .memlog.md, flagged rather than quoted as spine text. Self-Validate PASS both passes. STILL OPEN and now the blocking pair for the same corpus, both outside this spec folder: review findings F-4 and F-10 against ../spec-slide-artifact-model/SPEC.md — an ADOPTED COMPANION of the spec just fixed, which Stories 20.1 and 20.3 read — where :64 still presents the missing-only seed as live (AD-17 reversed it), :65 still presents templates as global across services (AD-16 reversed it), CAP-8 still requires a TextPlaceholder (AD-19 abolished the kind), and the supersession note at :16 enumerates only two reversals, which implies the rest still binds. That is a second bmad-spec Update, on a different spec folder, and it is not tracked as an item anywhere yet"

### Epic 20 — Gap 4b — bmad-spec Update on spec-slide-artifact-model, opened 2026-07-31 by the Gap 4 pass because the findings had a fix route and no tracker.

- **owner:** "kodesh87"  |  **status:** `done`

**Full action text as originally written:**

> Gap 4b — bmad-spec Update on spec-slide-artifact-model, opened 2026-07-31 by the Gap 4 pass because the findings had a fix route and no tracker. Review findings F-4 and F-10 in planning-artifacts/architecture/architecture-bic-pptx-workflow-2026-07-10/reviews/review-cross-document-coverage-2026-07-30-validate-b.md. This file is an ADOPTED COMPANION of spec-artifact-registry-authoring, so Stories 20.1 and 20.3 read it as contract: SPEC.md:64 still presents data/default-registry.json as a seed that inserts missing template IDs only — AD-17's whole Prevents is that a gap-filler resurrects a deleted row on every boot, forever; SPEC.md:65 still presents artifact templates as global across services — AD-16 reversed it; and CAP-8's success still requires BibleVerseContemplation to remain a TextPlaceholder, a kind AD-19 abolished (gone rather than renamed), with deferred-work.md:83 compounding it by recording welcome's change to text-placeholder as current state. The trap is the supersession note at :16, which says 'Two reversals matter to anyone reading this file directly' and enumerates the seven base types and the create/delete/reorder boundary — affirmatively implying everything else still binds. Fix: extend :16 from two reversals to four naming AD-17 against :64 and AD-16 against :65, mark both constraint bullets superseded in place, extend the note to CAP-8 and state the successor (a General carrying a Placeholder Catalog text element)

**Status narrative:**

2026-07-31 bmad-spec Update. Delivered exactly the named fix plus one step for coherence: banner :16 now enumerates four reversals with CAP-8's successor stated; both Constraints bullets carry their superseding AD in place (the clauses that still bind — SQLite SSOT, admin-only with DB role re-check — stand unmarked); registry-contract.md:5's own banner had the same two-item trap the review did not name and was matched to the same four. The bullets sit at ~:72/:73 after the banner grew; the only citations to the old :64/:65 are review records, verified by grep. CAP text and IDs untouched — rewriting the seven-type table stays Story 20-2's delivery. deferred-work.md:83 not edited (implementation artifact, outside bmad-spec's write surface); logged in the spec memlog. Self-Validate PASS both passes; memlog at 65 entries

### Epic 20 — Gap 3 — one finding to fold into the bmad-ux pass, NOT the whole gap-3 scope (see the 2026-07-30 handover for that): CAP-5 shows a registry entry as '[kind] label', and AD-19 now fixes six recognized …

- **owner:** "kodesh87"  |  **status:** `done`

**Full action text as originally written:**

> Gap 3 — one finding to fold into the bmad-ux pass, NOT the whole gap-3 scope (see the 2026-07-30 handover for that): CAP-5 shows a registry entry as '[kind] label', and AD-19 now fixes six recognized keys over three kinds with song-set naming the kind but never an entry. So for a songset-bt-open row, does the chip read [song-set] or [songset-bt-open]? AD-19 implies the kind, but the spine does not say it and the display is EXPERIENCE.md's to own. Raised by the rubric-walker lens (R9) in reviews/review-rubric-walker-2026-07-30-post-trim.md and deliberately routed rather than decided in the spine

**Status narrative:**

2026-07-31 bmad-ux Update, scope held to R9 alone per this item's own instruction. Decided in EXPERIENCE.md (Inside /admin/artifacts, 'Row display'): the chip reads the KIND, never the entry key. Three grounds, all pre-existing: AD-19 makes slot keys server-owned binding vocabulary; CAP-5's success makes the label the only administrator-editable part; EXPERIENCE.md's own voice rule bars system vocabulary from human surfaces, and a raw hyphenated key in a chip is exactly that. Recorded with its consequence rather than as a bare verdict: four SongSet rows share one chip and are told apart by editable labels, so the bounded-configuration surface must state the row's slot identity read-only in worship vocabulary — a rename must not orphan the row's liturgical identity. Owners in place: Story 20.3 (chip), Story 20.7 (slot statement). DESIGN.md untouched — display content, not visual identity

### Epic 20 — Story 20.1 seed authoring — the SongSet layout specification the owner gave on 2026-07-30.

- **owner:** "Developer"  |  **status:** `open`

**Full action text as originally written:**

> Story 20.1 seed authoring — the SongSet layout specification the owner gave on 2026-07-30. Recorded here rather than in the architecture spine deliberately: it is one seed with one answer, so no two units can choose it incompatibly, and AD-22 fixes only that the layout is developer-owned seed data. TITLE slide: follow the sample slide as closely as possible; full-screen background image. LYRIC slide: the verse marker at the top, the lyric text below it filling roughly the top two thirds, sized to be read from the back of the room. Verse and refrain SHARE ONE lyric layout and ONE background image — the only difference is the label value, so do not author a third layout. What the owner's description did not cover and the seed author must handle: src/lib/lyrics.ts emits 'n/total' for a verse (line 384), 'Reff' (386) AND 'Chorus' (388), and for lyrics with no headers it emits an EMPTY label (413) or '1/1' (452) — so the label element must tolerate an empty value. It is required:false in the seed today; keep it that way

### Epic null — COLLATERAL DAMAGE of the 2026-07-29 PII remediation, in a class none of the items above anticipated: the text substitution that replaced real names with invented ones ran over the whole tracked tree, …

- **owner:** "kodesh87"  |  **status:** `done`

**Full action text as originally written:**

> COLLATERAL DAMAGE of the 2026-07-29 PII remediation, in a class none of the items above anticipated: the text substitution that replaced real names with invented ones ran over the whole tracked tree, including files where a name is not prose. A four-character real name occurred as a substring inside the base64 sha512 of set-function-name@2.0.2 in package-lock.json, and the five-character replacement went in with it. The digest became 89 base64 characters where a sha512 is always 88, and `npm ci` failed with EINTEGRITY repo-wide, on every machine, for two days. Nothing caught it because npm is the only thing in this repository that verifies a checksum, and npm only speaks when someone installs — `npm test` never looked at a derived value at all. FIXED in 488eb19 by restoring the registry's authentic digest (an upstream checksum, not congregation data; the substring is not fingerprinted and the public-repo guard passes 4/4 with it). Integrity verification was NOT disabled to get past it

### Epic null — RESIDUAL of the lockfile corruption: measure the real blast radius.

- **owner:** "kodesh87"  |  **status:** `done`

**Full action text as originally written:**

> RESIDUAL of the lockfile corruption: measure the real blast radius. The 2026-07-29 detector was base64 LENGTH, which by construction only sees a substitution that changes length — an equal-length rewrite decodes to the right byte count and is invisible to it. So `exactly one affected` was an unproven claim when it was made

**Status narrative:**

2026-07-31. Measured, not assumed, and by a method that does not depend on length. (1) LOCKFILE: all 819 registry-resolved integrity values compared against the registry's own dist.integrity via the per-version manifest endpoint — 0 mismatches, 0 unverifiable. That is exhaustive and settles the length-preserving case for package-lock.json. (2) WHOLE TREE: 973 tracked files swept with two independent detectors — name-targeted (an invented name glued inside a long mixed alphanumeric token, which is the exact signature of the damage) and name-agnostic (SRI size, base64 decodability and canonical round-trip, hex fields carrying non-hex letters). Every hit was a benign coincidence: `ada` is itself valid hex and occurs naturally in sha256 digests, `adver-sari-al` in a review filename, `re-ada-ble` in a package name, and several natural base64 runs inside digests already registry-verified. (3) OTHER DERIVED VALUES: _bmad/_config/files-manifest.csv recomputed — of 275 rows, 262 point at files not tracked here, 12 match their committed blob exactly, and the single mismatch (cis/module-help.csv) is a CRLF-vs-LF artifact in vendor content last touched 2026-07-28, before the remediation, containing no invented name. (4) data/asset-map.json has zero invented-name hits in any field, consistent with acc8df0 having withheld payload runs, and tests/fixtures/ contains only the prose rundown. CONCLUSION: the lockfile was the only casualty

### Epic null — RESIDUAL of the lockfile corruption: recover the substitution pairs the 2026-07-29 pass applied, so the sweep above could be aimed.

- **owner:** "kodesh87"  |  **status:** `done`

**Full action text as originally written:**

> RESIDUAL of the lockfile corruption: recover the substitution pairs the 2026-07-29 pass applied, so the sweep above could be aimed. NOT FULLY POSSIBLE and that is by design — the history rewrite worked. refs/original is gone, `git fsck` finds no dangling commits, the 36 MB rollback bundle is not on this machine, and the local clone postdates the rewrite (`main@{6}: clone`), so the real names are unrecoverable from git here. What WAS recovered is the pair that mattered: the damage itself carries it. The corrupt digest and the authentic one differ in exactly one substring, which names both halves of one pair, and the invented half is traceable in the tree to a predecessor operator in the brief's tool-churn history. The remaining invented cast was enumerated from the tree the remediation left behind. NOTE FOR ANYONE EXTENDING THE NAME GUARD: the real name in that pair is a substring of an authentic upstream sha512 that must stay in package-lock.json. Adding it to FORBIDDEN_NAME_HASHES would fail the guard on a legitimate file and pressure someone into weakening the guard or corrupting the lockfile again — the exact trap that produced this incident. It is deliberately not fingerprinted

### Epic null — Close the recurrence gap the lockfile corruption exposed: nothing in `npm test` verified any derived value, so a rewritten checksum could only be found by installing

- **owner:** "kodesh87"  |  **status:** `done`

**Status narrative:**

2026-07-31. tests/lockfile-integrity.test.mjs added and wired into the package.json test list. Asserts every integrity value is <algorithm>-<base64> with a known algorithm, strict base64 charset, the exact character count and decoded byte count for that algorithm, and canonical base64 round-trip. Offline and deterministic — no network, so it runs in CI. NEGATIVE-TESTED both ways in a window that reverts: reinjecting the exact 2026-07-29 damage fails it with `sha512 payload is 89 base64 characters, must be 88`, naming the package; and an equal-length substitution PASSES, which is the documented limit stated in the file header rather than left for someone to discover. The verdict for a length-preserving rewrite remains `npm ci` against a clean cache or a direct dist.integrity comparison; the offline guard is a filter, not a proof

### Epic 22 — STILL OPEN, and re-scoped 2026-08-01: Story 22.2 shipped without waiting on this, which needs stating precisely so the gate is not quietly treated as closed.

- **owner:** "kodesh87"  |  **status:** `done`

**Full action text as originally written:**

> STILL OPEN, and re-scoped 2026-08-01: Story 22.2 shipped without waiting on this, which needs stating precisely so the gate is not quietly treated as closed. It shipped because it introduced NO new channel — upsertHymns already overwrote title and lyrics from the corpus on every boot, so the corrected titles reached the table through the path that was already there, and no schema or seeding channel was added for them. What this run still owes is the forward question, unchanged: SHOULD a shipped reference corpus keep an every-boot overwrite? If the answer is AD-21's counter, the migration target is upsertHymns in src/lib/db/index.ts, not the corpus data. Note the new bible seeder deliberately does NOT use that channel: seedBibleCorpus only fills a translation holding zero verses, so it never overwrites a persisted value — one precedent for the decision, not the decision itself. ORIGINAL TEXT FOLLOWS. bmad-architecture Update: no AD governs a SHIPPED REFERENCE CORPUS at all. AD-11 and AD-17 cover the registry seed only, while the hymnal is upserted on every boot (src/lib/db/index.ts:262), overwriting title and lyrics — the boot-time value-change channel AD-17 removed for the registry and AD-21 routes through a declared transition n to n+1. Correcting 695 persisted titles is exactly that case, and the spine records at :370 that AD-21 counter does not exist and no story owns introducing it. Add the next AD-n — never renumber — settling whether the shipped corpora ride the boot upsert or the counter arrives here. The same run amends the Structural Seed tree (:315 names data/ hymnal corpus (hymns.json); :322 the scripts list) and clears both Deferred bullets (:360 FR-19 corpus ops, :363 SDAH licence). Line numbers re-measured after merging origin/main on 2026-08-01, which moved all five by four

**Status narrative:**

2026-08-01, settled in ONE run with the epic-21 item below. ANSWERED AS A THIRD OPTION rather than inside the binary the item posed: neither the boot upsert nor AD-21's counter. AD-25 [ADOPTED, partial] — a shipped reference corpus is a PROJECTION of its committed file; the file is authoritative and the table is reconciled to it, completely within the corpus code it names. The premise both offered answers shared is false: AD-17 exists because an administrator owns registry rows, and AD-21 counts transitions over values somebody persisted deliberately, and neither is true of a corpus table — MEASURED, zero INSERT/UPDATE/DELETE against hymns, bible_verses or bible_books outside src/lib/db/index.ts. So correcting 695 titles was never AD-21's case, and the counter's landing story stays unassigned at Epic 20's first release (spine Deferred amended to say so). The two shipped shapes are wrong in OPPOSITE directions and both change: upsertHymns removes nothing, seedBibleCorpus early-returns so a correction never arrives. The owner also chose the reconcile to be COMPLETE — rows the corpus no longer holds are removed, bounded to that corpus code — which is free today only under AD-4's no-deployment date and expires with AD-18's licence on the same event. Structural Seed tree repaired (the data/ and scripts/ lines) and both Deferred bullets struck as closed with their record kept: FR-19 corpus ops (KJV committed, import:kjv retired, verified structurally) and the SDAH licence (an accepted risk, not a cleared one — attribution and takedown statement verified inside the file, guarded by verify-corpora.mjs:133-138 and tests/corpus.test.mjs:102-104)

### Epic 21 — bmad-ux Update: EXPERIENCE.md:143 states, as a shipped state, that lookup is unavailable when the corpus was never imported (an ops step). Once the corpus ships the default flips, and that message survives only as the unreadable-file path.

- **owner:** "kodesh87"  |  **status:** `done`

**Full action text as originally written:**

> bmad-ux Update: EXPERIENCE.md:143 states, as a shipped state, that lookup is unavailable when the corpus was never imported (an ops step). Once the corpus ships the default flips, and that message survives only as the unreadable-file path. One State Patterns row; no route changes, so the IA table is untouched

**Status narrative:**

2026-08-01 bmad-ux Update. This is clause (1) of the epic-24 item below and was closed in that same run, as that item asked. THE ROW WAS FALSE IN A SECOND WAY THIS ITEM DID NOT KNOW ABOUT, found by reading src/ rather than the row: it also claimed 'the presenter says so instead of returning empty results'. /api/scripture does say so — 503 naming the file and npm run corpus:verify (route.ts:18-27) — but PresenterOperator.tsx:432 collapses every non-404 status into a flat 'Lookup failed', so that message reaches the operator on the two service forms (CreateForm.tsx:300, EditForm.tsx:316) and NOWHERE on the surface used during a service. One product, one fault, two answers, and the mute one is the room-facing half. Row rewritten as 'Corpus empty' with both corrections stated and the old text quoted; routed to Story 21.5, which already owns this field on all three surfaces and carries 'an unresolved reference is visible as a defect'

### Epic 22 — Supply the number-to-title list for the 695 SDAH hymns.

- **owner:** "kodesh87"  |  **status:** `done`

**Full action text as originally written:**

> Supply the number-to-title list for the 695 SDAH hymns. Story 22.2 cannot start without it: the source dump carried no title column, deriveTitle() takes the first lyric line by design, and .work/lirik-lagu.json is gone — so there is nothing in the repository to derive a real title from. Owner chose to supply the list rather than have an agent source one

**Status narrative:**

2026-08-01 the owner supplied all 695, complete and contiguous 1..695 with no duplicates. Applied verbatim into data/song-book/sdah.json apart from whitespace collapse (two entries carried a double space). Longest title is now 39 chars, against 40 titles over 45 chars before. NOT applied: four entries the corpus lyrics contradict, left as supplied because the owner index is authoritative — see the follow-up item below

### Epic 22 — Decide four SDAH titles the owner index spells differently from the lyrics beneath them, left VERBATIM rather than silently corrected: #81 'Thou I Speak With Tongues' (lyric: 'Though I speak with …

- **owner:** "kodesh87"  |  **status:** `dropped`

**Full action text as originally written:**

> Decide four SDAH titles the owner index spells differently from the lyrics beneath them, left VERBATIM rather than silently corrected: #81 'Thou I Speak With Tongues' (lyric: 'Though I speak with tongues'); #231 'Bless Be the King' (lyric agrees, but the hymn is 'Blest Be the King Whose Coming' — both the index and the lyric carry it, suggesting one upstream source); #234 'Christ Is the Worlds Light' (lyric: "Christ is the world's Light" — missing apostrophe); #356 'All Who Love and Serve Youre City' (lyric: 'All who love and serve your city'). Each projects onto a slide as written. One TSV-shaped correction fixes all four; nothing else depends on it

**Status narrative:**

2026-08-01 raised to the owner and EXPLICITLY DROPPED. The owner-supplied index is authoritative and stays verbatim; all four project as written. Closed by the second Correct Course of the day. Do not re-raise — a later pass rediscovering these four spellings should read this line and stop, which is the whole reason the finding is recorded rather than deleted

### Epic 21 — Delete .work/tp_bible_book_translations_*.json and .work/tp_bible_verses_*.json from the main checkout AFTER Story 21.1 completeness assertion is green (66 books / 1,189 chapters / 31,102 verses).

- **owner:** "kodesh87"  |  **status:** `done`

**Full action text as originally written:**

> Delete .work/tp_bible_book_translations_*.json and .work/tp_bible_verses_*.json from the main checkout AFTER Story 21.1 completeness assertion is green (66 books / 1,189 chapters / 31,102 verses). Not before: they are the only copy of the source, and the hymnal corpus is already in exactly the state this ordering exists to avoid

**Status narrative:**

2026-08-01, in that order. The assertion went green first (tests/corpus.test.mjs + npm run corpus:verify), then a full round-trip comparison confirmed all 31,102 verses and all 66 book name/shortName pairs reproduce byte-for-byte from the committed file, and only then were both exports deleted. No tp_bible_* file remains anywhere under the project root. data/bible/kjv.json is now the only copy, which is why its completeness is asserted structurally on every test run rather than sampled

### Epic 21 — bmad-architecture Update — SCOPE WIDENED 2026-08-01 by the second Correct Course of the day; this EXTENDS the still-open epic-22 architecture item above rather than replacing it, and both should be settled in ONE run.

- **owner:** "Developer"  |  **status:** `done`

**Full action text as originally written:**

> bmad-architecture Update — SCOPE WIDENED 2026-08-01 by the second Correct Course of the day; this EXTENDS the still-open epic-22 architecture item above rather than replacing it, and both should be settled in ONE run. What the locale axis adds to that run: a bible_translations registry and a song_books registry, each carrying locale, name, licence and provenance; per-translation book names as a first-class table; a canonical book identity stable across translations, so 'same passage, another translation' stays one query; and the renames the axis implies — bible_verses.translation to translation_code, hymns.book_code to song_book_code, isKjvCorpusEmpty() to isBibleTranslationEmpty(code). The target shape was deliberately NOT decided by Correct Course; it is named there only so the stories can cite something. Never renumber an existing AD-n — add the next one. NOT part of this run and NOT an open item: per-locale template identity on artifact_templates. Rejecting projection_locale makes one global template set a recorded accepted trade (PRD 4.12), not work awaiting a decision

**Status narrative:**

2026-08-01, same run as the epic-22 item above. AD-26 [TARGET] — a corpus is a REGISTERED ENTITY: one row per corpus in bible_translations or song_books carrying code, name, locale, licence and provenance. The corpus CODE is globally unique across locales and is the cross-boundary key (AD-19's class); LOCALE IS AN ATTRIBUTE, never part of a key and never a predicate — which is what makes FR-24's never-filter rule STRUCTURAL rather than disciplinary, since no key holds a locale so no read path can need one. The file declares and the path merely locates: the loader already refuses a declared code disagreeing with the loaded code (corpus.ts:91, :176) and refuses a declared locale disagreeing with its directory on the same terms; two files declaring one code refuse the boot; a default_* naming an uninstalled corpus is INERT, not an error, and is not rewritten. All three renames carried, and under AD-25 they are rebuilds rather than migrations because the tables are projections. AD-27 [TARGET] — book identity is canonical, translation-independent and carries NO display text; every name belongs to a translation; a corpus MAPS onto the identity and a book outside the canon is refused with the book named. SPELLING SETTLED (owner): the declared field is `locale`, not `language` — both committed files ship `language` today and are corrected in the same change set. Story 21.4 is unblocked and cites AD-27. FOUND WHILE SIZING IT, filed in the spine's Deferred rather than fixed here (an architecture run does not patch production code): the SHIPPED DEFAULT CORPUS MISNAMES A BOOK, and the alias map is what caught it. BOOK_ALIASES targets "Song of Solomon" for book 22 while data/bible/kjv.json says "Song of Songs", so three entries resolve to nothing — but the KJV's own title IS "Song of Solomon" (verified against Project Gutenberg's KJV: the book heading reads Song of Solomon, and "the song of songs" is a phrase inside verse 1). "Song of Songs" is the Hebrew-derived title other translations use and it came in through the SQL export the corpus was normalised from. So the CORPUS is wrong, it is a live AD-27 output-is-exact violation on the default translation, and it is a one-word data fix; checked across all 66 books, exactly one mismatch. The parser is the harder half and the corpus fix does not reach it: parseScriptureRef caps a book name at two words and ASCII, so "Song of Solomon 2:1" is rejected exactly as "Song of Songs 2:1" is — book 22 is the only book whose name exceeds two words, which is why the cap hid this long — and "Kisah Para Rasul 1:8" is what FR-24's generous input walks into

### Epic 24 — bmad-ux Update — EXPERIENCE.md is now stale in TWO ways and one run should close both.

- **owner:** "Developer"  |  **status:** `done`

**Full action text as originally written:**

> bmad-ux Update — EXPERIENCE.md is now stale in TWO ways and one run should close both. (1) The pre-existing item: the State Patterns row at EXPERIENCE.md:143 states, as a shipped state, that lookup is unavailable when the corpus was never imported (an ops step). The corpus now ships, so the default flips and that message survives only as the unreadable-file path. (2) NEW 2026-08-01: FR-24 adds a corpus picker with an always-present control for browsing locales outside the default, and FR-25 adds a locale switcher — both are UX surfaces no artifact describes. The never-filter rule is what the picker must make visible: an Operator has to be able to SEE that other locales are reachable, or a default that only filters the view will read to them as one that filters the data. Whether either adds a route determines if the IA table moves; the pre-existing item alone did not. (3) NEW 2026-08-01, from the input-model Correct Course, and it lands in the SAME file so the same run should take it: the scripture reference input becomes ONE FIELD WITH INLINE AUTOCOMPLETE serving every operator surface (Presenter Mode lookup plus the create/edit service forms), on the HymnNumberAutocomplete precedent. That is a component-inventory row beside HymnNumberAutocomplete at EXPERIENCE.md:97, not a route — so on its own it leaves the IA table alone. Worth stating in the row because the choice is load-bearing rather than cosmetic: a book PICKER was recommended and the owner chose the single field, which is what keeps paste working and what lets the rundown reuse the same matcher instead of needing a second one. (4) NEW 2026-08-01, from the AD-28 Reviewer Gate, and it lands in the SAME file for the same reason - take it in the same run. Under AD-28 the rundown matcher spans EVERY INSTALLED TRANSLATION and an ambiguous reference is refused rather than guessed, which is correct and fail-closed. The consequence nothing describes: ADDING A CORPUS IS THEREFORE NOT PURELY ADDITIVE ON THAT SURFACE - a reference that resolved unambiguously against one translation can become ambiguous against two and start arriving as unmapped input. NFR-5 already makes unmapped input VISIBLE; what is missing is the EXPLANATION. An operator sees a reference that was fine last Sabbath arrive unmapped, with nothing connecting it to an install nobody associated with the rundown, and this surfaces on a Sabbath morning on a rundown they did not edit - AD-1 shape. An affordance question on the same footing as the stale-snapshot and Reset-reverts-a-rename items the spine already handed to EXPERIENCE.md. It blocks neither Story 21.4 nor 21.5

**Status narrative:**

2026-08-01 bmad-ux Update, ALL FOUR CLAUSES CLOSED IN ONE RUN as this item asked, plus the epic-21 item above which is clause (1). (1) Row rewritten as 'Corpus empty' — see that item for the second falsehood it turned out to carry. (2) THE ROUTE QUESTION IS ANSWERED, NO: neither the picker nor the switcher is a route, so the IA table does not move and three rows join the Sub-surfaces table instead — translation control in the Presenter panel (21.3), the four language settings on /admin (21.3, 22.3, 24.1), the ui_locale switcher on /admin (24.1). Reasoning recorded in the file rather than only here: what WOULD have moved the IA table is a browse-the-corpora surface of its own, and FR-24 asks for the opposite — reachability from where the choice is made. Corpus picker added as a Component Patterns row whose stated job is making the never-filter rule VISIBLE, because the failure is silent: an operator cannot tell a view filter from a data filter, and one who believes the English hymnal is unreachable never asks for it. (3) Scripture reference field added beside HymnNumberAutocomplete, with the single-field choice recorded as load-bearing rather than cosmetic. Measured while writing it: three plain inputs today, no component — PresenterOperator.tsx:696, CreateForm.tsx:584, EditForm.tsx:615. (4) Open Item 8, no owner, on the footing this item named. It gained three bounds the item did not have: the ambiguity is INTRA-translation and live on the single corpus shipping today (Phil, Jo), so an explanation written only for the cross-translation case is missing where it is already needed; the colliding candidates are known at the moment of refusal, so naming them is cheap; and the operator cannot act on the corpus, so it must not read as an instruction to fix an install. ALSO IN THIS RUN: Flow 9 added so the three new sub-surfaces are landed on by a journey — the every-surface-is-landed-on assertion is testable and adding rows without it would have broken it — and one PRE-EXISTING CITATION ROT repaired, the Venue bullet pointing at 'Open Item 5' for the stale-snapshot affordance when the 2026-07-31 toast insertion had pushed it to 6; item 5 is session revocation, so it resolved to a real entry about something else. DEVIATIONS: Reviewer Gate not offered and not run (session instruction forbids subagents unless asked; every lens is one), and doc_standards run as a targeted self-check against prior findings rather than as full passes — same two deviations as the 2026-07-30 runs, and a full pass can be requested

### Epic 21 — bmad-architecture Update — the owner direction REVERSES a clause AD-27 shipped hours earlier, so it needs a NEW AD rather than an edit to that one. NEVER RENUMBER: add AD-28. What it must settle.

- **owner:** "Developer"  |  **status:** `done`

**Full action text as originally written:**

> bmad-architecture Update — the owner direction REVERSES a clause AD-27 shipped hours earlier, so it needs a NEW AD rather than an edit to that one. NEVER RENUMBER: add AD-28. What it must settle. (1) AD-27's 'input tolerance is the matcher's concern, never the corpus's' SURVIVES, but its 'one SHARED server-side matcher' does not: the matcher is one implementation carrying a SCOPE — the chosen translation on operator surfaces, all installed translations on the rundown, because a Telegram sender picks none. (2) AD-27 enumerates tolerance as 'case, punctuation, abbreviation, singular against plural' and treats the four as uniform across translations. THREE OF THEM ARE; ABBREVIATION IS NOT. 'Jn' is English tolerance, so an unscoped alias list reintroduces exactly the cross-language input this direction removes — through the matcher rather than through a corpus file, which is the door AD-27 was watching. Aliases therefore BELONG TO A TRANSLATION while the comparison rules do not, and the enumeration has to be split rather than kept as one list. (3) A tolerance collision between two translations in rundown scope is surfaced as unmapped input (NFR-5) and NEVER guessed — the fail-closed posture AD-5, AD-8 and AD-17 already take. (4) shortName leaves AD-27's Rule and the bible_books column; under AD-25 that is a rebuild rather than a migration, because the table is a projection. (5) The Boundaries convention is ALREADY violated on shipped code — scripture.ts:42 and parser.ts:152/:162 carry the same regex — and the one-matcher rule is what closes it, which also means Story 21.4 legitimately reaches into Epic 2/5 code. What SURVIVES of AD-27 and must not be re-litigated: the canonical translation-independent identity, names owned by the translation, output exact, and refusal with the book named

**Status narrative:**

2026-08-01, bmad-architecture Update. AD-28 [TARGET] added as the next id; NOTHING RENUMBERED and AD-27 keeps every clause that survives, with the two reversed ones struck IN PLACE and pointed at AD-28 rather than deleted, because AD-27 is [TARGET] and unbuilt - which is exactly when a half-reversed rule reads as a whole one. All five points settled. The Reviewer Gate then found FOUR THINGS THE FIVE POINTS DID NOT COVER, all closed in-run. (1) NOBODY SAID WHO SUPPLIES THE SCOPE: Story 21.5 could reasonably send none and expect the server to read default_bible_translation while 21.4 could reasonably read an absent scope as ALL INSTALLED - the other scope the same decision names - which compiles, passes both stories AC, and REINSTATES CROSS-LANGUAGE OPERATOR INPUT THROUGH A DEFAULT. Now a required, registry-validated corpus code; absent or unrecognised is REFUSED; all-installed is a property of the rundown surface, never a fallback. (2) LONGEST-PREFIX MATCH NEVER SAID WHICH SIDE THE PREFIX WAS ON, and both directions are required by the decision own examples - corpus-name-prefixes-input to find where a book name ends, typed-text-prefixes-corpus-name for Ps to reach Psalms, which is the whole case for dropping shortName. Implement only the first and the server refuses Ps 23:1 while the autocomplete beside it offers Psalms, ON PASTE. Now stated in both directions with a disambiguation rule. (3) THE COLLISION CLAUSE WAS WRITTEN FOR TWO TRANSLATIONS COLLIDING and the measured ambiguity is INTRA-translation and live on the single corpus that ships today - Phil matches Philippians and Philemon, Jo matches five books - so a builder in a single-corpus install would reasonably conclude the rule did not apply yet. Generalised. (4) STORY 21.2 translation parameter and the matcher SCOPE are the same value on an operator surface and nothing said so, so two parameters that must always agree could land separately in the same module. Stated. ALSO FOUND, and filed in the spine Deferred rather than fixed because an architecture run does not patch production code: A SECOND LIVE output-is-exact VIOLATION distinct from the Song of Songs corpus misnaming - scripture.ts:139-142 echoes the reference from the OPERATOR TYPED STRING, so ps 23:1 comes back as ps 23:1, and AD-28 makes it worse because every tolerance it grants is another abbreviation that can reach a reference the congregation sees. Owned by Story 21.4. Story 21.4 and 21.5 are UNBLOCKED. SEQUENCING WARNING CARRIED OUT OF THIS RUN: Story 21.2 and 21.4 both rewrite src/lib/scripture.ts - 21.2 removes the KJV literals at :6 :106 :128 :144 while 21.4 replaces :42, :65 and :87-88 - different lines, same file, and 21.4 replaces the whole resolution path, so run 21.2 THEN 21.4 rather than in parallel worktrees

### Epic 20 — Gap 3 CLOSE-OUT — bmad-spec Update on spec-artifact-registry-authoring, opened 2026-08-07 by the owner.

- **owner:** "kodesh87"  |  **status:** `done`

**Full action text as originally written:**

> Gap 3 CLOSE-OUT — bmad-spec Update on spec-artifact-registry-authoring, opened 2026-08-07 by the owner. The Gap 3 item above is done and the chip WAS decided in EXPERIENCE.md on 2026-07-31, but slide-kinds.md 'Badge display' still recorded the [song-set]-vs-[songset-bt-open] question as OPEN and routed a builder to EXPERIENCE.md to answer it. Pure ordering artifact: the Gap 4 bmad-spec pass ran FIRST that day and deliberately flagged the question open with its owner named; the bmad-ux pass answered it hours later and nothing came back to close the loop in the spec folder. Story 20.3 owns the chip. No tracker item existed for this because closing it was nobody's registered deliverable

**Status narrative:**

2026-08-07 bmad-spec Update. Delivered the named fix plus two steps for coherence. NAMED: slide-kinds.md Badge display now records the decision — the chip names the KIND, never the entry key, so a songset-bt-open row shows [song-set] — with the three standing grounds (AD-19 server-owned binding vocabulary, CAP-5 label-only, EXPERIENCE.md's voice rule) and owner Story 20.3; the stale "Tracked as Gap 3" pointer is dropped, since aiming a builder at a closed tracker row is worse than silence. COHERENCE 1: EXPERIENCE.md ADOPTED into companions: — same class of finding this spec already hit once with ARCHITECTURE-SPINE.md, a live decision authority reachable only by narrative reference, and it is now the sole source of both the chip rule and Story 20.7's read-only slot obligation. COHERENCE 2, the one that was a build defect rather than wording — the decision's SECOND half (the AD-22 bounded surface must state the row's slot read-only in worship vocabulary, because four SongSet rows share one chip and labels are renameable) was FORBIDDEN by three standing statements: CAP-5's success said "no surface exposes the row's kind or its SongSet slot identity", a DISPLAY ban where AD-19 says only "never administrator-editable"; and authoring-boundaries.md's "The SongSet bounded surface, in full" ended "Nothing else.", read as exhaustive over CONTENT. A Story 20.7 builder reading the spec alone would have refused the slot statement as widening AD-22 — the same shape as F-24, which nearly shipped that surface with no font controls. Repaired, not re-decided: AD-19's own words bar editing only and EXPERIENCE.md owns display, so no live authority was overruled. CAP-5 narrows "exposes" to "exposes FOR EDITING" and names what IS shown; authoring-boundaries.md gains a "Not editable is not the same as not shown" paragraph and "Nothing else is CONFIGURABLE — a rule about controls, not content". One pre-existing self-contradiction closed as a side effect: CAP-5's intent already displayed a row as "[kind] label" while its success banned exposing the kind. CAP IDs, CAP count (8), Constraints (9) and Non-goals (5) all untouched. Self-Validate PASS both passes; memlog at 89 entries

### Epic 20 — OWNER RATIFICATION of the CAP-2 add-verb assumption, given 2026-08-07 in the same session as the Gap 3 close-out.

- **owner:** "kodesh87"  |  **status:** `done`

**Full action text as originally written:**

> OWNER RATIFICATION of the CAP-2 add-verb assumption, given 2026-08-07 in the same session as the Gap 3 close-out. The assumption had stood in SPEC.md flagged 'Needs owner ratification' since 2026-07-31: that 'add a SongSet entry' means claiming a slot identity not currently in the order, derived from AD-19 rather than stated by it. It gates Story 20.3's Add menu and its delete verb, and 20.3 has no story file yet

**Status narrative:**

2026-08-07 bmad-spec Update. RATIFIED, and the owner sharpened it with a distinction the spec never made: TWO LEVELS, not one. (A) the PREDEFINED SET the system offers — the four songset-* identities and announcement — is permanent, and no administrator action removes anything from it, ever. (B) MEMBERSHIP IN THE ORDERED REGISTRY is what an administrator changes. "Delete" only ever meant (B). Membership caps differ per kind and are NOT interchangeable: a songset-* identity sits in the order AT MOST ONCE and may be absent (valid configuration, not an error), and removing it is reversible by the administrator because the identity returns to the available set; ANNOUNCEMENT may appear MORE THAN ONCE with no cap — the owner's "normally one, three would still be reasonable" is recorded as judgment, deliberately NOT encoded as a limit. That corrects three statements that read as an enforced cap and were only ever describing the common case — CAP-7's "a single registry entry", slide-kinds.md's "One row [Announcement]" and its "Insert one Announcement entry" — and it is consistent with AD-19, which states at-most-one for SLOT IDENTITIES and never for announcement. No spine change needed and none made. Landed: new Constraint (predefined set vs order, 9 -> 10), CAP-2 success and CAP-7 both amended, slide-kinds.md and authoring-boundaries.md carry the two-level rule, and "deleting a slot" is replaced by "removal from the order" at both sites. Assumptions 4 -> 3, the ratified one retired with a visible marker rather than dropped. CAP IDs and count (8) untouched; Non-goals 5 unchanged. ONE OPEN QUESTION OPENED, routed to Story 20.6 and not decided: what a SECOND Announcement row is FOR. Under CAP-7 and AD-16 membership is the live master list with no per-row subset selection, so two rows expand the SAME images twice rather than splitting them early/late — if splitting was the intent it needs per-row membership, which no capability, companion or AD provides and which CAP-7 actively rules out. A builder must not invent a subset mechanism to give the second row meaning. Self-Validate PASS both passes; memlog at 95 entries

### Epic 20 — The Open Question the ratification above opened — what a SECOND Announcement row is for, given each row expands the whole live list. Routed to Story 20.6

- **owner:** "kodesh87"  |  **status:** `done`

**Status narrative:**

2026-08-07, answered by the owner the same day, hours after it was opened. Several Announcement rows mean REPEATING the identical set at the start, middle and end of one service — never splitting the list across positions. The contract as it stood was therefore already correct and needed no new mechanism, only its INTENT recorded. That matters in the OTHER direction: a builder or reviewer seeing the same images render three times would reasonably read it as a bug and add dedup, a collapse, or an operator warning. CAP-7 success and slide-kinds.md now state that repetition is the intended use and that nothing deduplicates, collapses, or warns; per-row membership stays explicitly unwanted, so Story 20.6 must not build one. SPEC.md Open Questions back to none and Assumptions hold at 3 with none flagged for ratification. Self-Validate PASS both passes; memlog at 96 entries
