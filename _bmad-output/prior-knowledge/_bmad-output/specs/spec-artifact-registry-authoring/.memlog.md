---
topic: Ordered Artifact Registry authoring: central placeholders, full canvas editor, label boundary, optional registry clone/freeze
updated: 2026-08-07T21:31
---

- (direction) Correct Course: Artifact Registry must become the operator-facing ordered deck definition, not only an alphabetical template catalog.
- (direction) User accepts that past worship services need not be strongly preserved; optional registry clone/freeze is enough if historical fidelity is needed.
- (direction) Full canvas authoring vision is in scope: add/delete/reorder slides; change layout; insert/reposition/resize background, images, text, and text areas with font color/size/style including bold italic underline.
- (direction) Placeholders are a central predefined catalog; operators insert any predefined placeholder into any slide template and style it; adding a new placeholder kind still requires code.
- (direction) Presenter labels like Bible Talk Sequence must have an explicit edit boundary: operator can set base type display and the slide name/label in the registry.
- (decision) CAP-1: Ordered Artifact Registry is the SSOT for which slides exist and their sequence for live presentation generation.
- (decision) CAP-2: Administrators can create, delete, rename, and reorder slides in the ordered registry.
- (decision) CAP-3: Full canvas authoring on ordered slides — background, insert image/text/text-area, drag position, resize, font color/size/style (bold/italic/underline), Save required.
- (decision) CAP-4: Central predefined Placeholder Catalog; insert into any slide; style locally; new placeholder kinds are code-gated.
- (decision) CAP-5: Each slide has editable baseType (from the allowed set) and editable label; Presenter/list badges show [baseType] label.
- (decision) CAP-6: Optional Registry Clone/Freeze produces an immutable snapshot for historical services when an operator chooses to freeze.
- (decision) Weekly placeholder VALUES still come from worship-service intake (form/rundown/webhook); the ordered registry owns structure/layout/labels, not weekly hymn numbers or sermon titles.
- (constraint) buildSlidePlan (or its successor) must consume the ordered registry as the sequence source rather than a hard-coded Part A/B/C template list.
- (constraint) Adding a new predefined placeholder kind requires a code change; operators cannot invent arbitrary placeholder keys in the UI.
- (constraint) Public-repo rules unchanged: no congregation PII in git; private overrides stay in data/local/.
- (constraint) SongSet and Announcement expansion rules remain system-controlled children unless explicitly redesigned later.
- (non-goal) Per-service forked live editing of layouts during the service without Save.
- (assumption) Default shipped ordered registry mirrors today's buildSlidePlan sequence so existing Sabbath output is reproducible after migration.
- (assumption) Legacy services without a clone continue to render from stored parsed_data + then-current registry until an operator freezes a clone.
- (question) Should registry clone be automatic on first PPTX/present of a service date, or only on explicit Freeze action?
- (question) Do SongSet/Announcement remain non-canvas system expanders in v1 of this course, or must they become fully canvas-authored too?
- (note) Wrapper-only: screenshot UI chrome and BIC branding examples not duplicated into kernel.
- (decision) Q resolved: Creating a new worship service clones the full ordered Artifact Registry (order, labels, layouts, placeholder bindings) into a service-bound snapshot that is frozen for that service.
- (decision) CAP-6 refined: Explicit Sync Artifact on a service re-clones from the live Artifact Registry and replaces that service snapshot; without Sync, later live registry edits do not affect the service.
- (decision) Q SongSet/Announcement: Announcement slides remain full-screen image presentation (list-driven); not a freeform multi-element canvas. SongSet remains system-expanded title+lyric children from hymnal rules; operators author SongSet shell/cue templates in registry but do not hand-layout every lyric page.
- (note) Clarification for operators: 'canvas-penuh' meant freeform add text/shapes/images on every slide type; Announcement full-bleed is still correct UX and is not the same as unrestricted canvas chrome.
- (decision) Announcement: one ordered-registry entry [Announcement]; no canvas editor; expands to full-screen images driven solely by the Announcements list menu/uploads.
- (decision) SongSet: special ordered-registry entry with system behavior (title + verse/refrain lyric expansion from hymnal); not freeform canvas pages per lyric; treated as a first-class special slide kind alongside Announcement.
- (decision) Sermon flyer/graphic and Family/Youth are General canvas slides: operator inserts predefined image/text placeholders (sermonGraphic, familyPhoto, youthPhoto, familyPrayerRequest, youthPrayerRequest) and styles them; weekly worship service fills those bindings.
- (decision) Correct Course taxonomy collapse: Epic 16 seven base types reduce to three slide kinds — General (free canvas + optional catalog placeholders), SongSet (system expander), Announcement (system expander). TextPlaceholder/ImagePlaceholder/MixPlaceholder/FullScreenImage are retired as distinct kinds because placeholders are centrally insertable onto General.
- (assumption) Display badge [General]/[SongSet]/[Announcement] uses these three kinds; operator-facing label remains editable on each registry entry.
- (question) Does SongSet registry entry expose any limited shell settings (e.g. title/lyric background defaults) without full canvas, or zero visual settings beyond order/label?
- (decision) SongSet open question resolved: each SongSet registry entry can set background(s) for its expanded title/lyric presentation without a freeform multi-element canvas.
- (decision) Four predefined SongSet slots ship in the ordered registry: bible-talk opening song, bible-talk closing song, divine-service opening song, divine-service closing song; admins reorder them among other entries; each remains expandable to title+lyrics.
- (decision) Worship service settings bind a hymnal number to each of those four SongSet slots independently (BT open, BT close, DS open, DS close).
- (assumption) SongSet entries use stable slot identities so form fields and planner can map hymn numbers even if labels/order change.
- (decision by owner-2026-07-30) ADOPTION (backfilled by validate; originally hand-edited into SPEC.md at commit 24b1d3a, citation repaired at c04fea0, neither logged). This SPEC is adopted whole and tracked as Epic 20 in planning-artifacts/epics.md, one story per capability, 20-1..20-8 in sprint-status.yaml, all backlog. Before that date the supersession claim over shipped code had no epic, story or sprint key, so it was unenforceable and unscheduled while artifact_templates still had no ordering column and the admin API no create/delete/reorder verb. Two adoption consequences live in the epic, not here: the base_type migration implied by Constraints (seven types to three kinds), and the fact that CAP-6 REVERSES architecture decision AD-14, which blocks Story 20.8 until the spine is amended. Recorded so the next derive reproduces the Delivery status paragraph instead of silently deleting it.
- (decision by bmad-spec-update) BACKFILL 1 of 2 found by the derive-convergence check: the non-goal 'Video elements or full Canva-suite parity beyond General CAP-3' stands in SPEC.md with no entry in this log. It is a real boundary - CAP-3 grants background, images, text, text areas, drag, resize and font styling, and nothing more - so it is recorded here rather than allowed to vanish on the next derive.
- (decision by bmad-spec-update) BACKFILL 2 of 2: the constraint in SPEC.md reads 'Placeholder Catalog extensions require code + tests', while this log only ever said a new placeholder kind requires a CODE change. The '+ tests' half is correct under the repository testing rules in project-context.md but was never logged. Recorded so the constraint survives the next derive at full strength.
- (decision) (bmad-spec-update, validate fix) Rewrote the Delivery-status banner's second adoption-consequence sentence: AD-14/CAP-6 reversal is no longer 'blocking Story 20.8 until the spine is amended' since AD-16 now records it; Story 20.8's only remaining open item is where the snapshot lives physically. The base_type migration consequence sentence is unchanged (still accurate).
- (decision by bmad-spec-update) BACKFILL 3 of 3 (constraint): 'Explicit Save for registry/canvas mutations; no autosave' stands in SPEC.md Constraints with no prior entry here. It is a real boundary inherited from CAP-3's 'Save required' and the adopted spec-slide-artifact-model companion's autosave exclusion, generalized to all registry/canvas mutations in this Correct Course. Recorded so it survives the next derive at full strength.
- (decision by bmad-spec-update) BACKFILL 4 of 4 (non-goal): 'Work in the frozen bic-pptx-workflow repository' stands in SPEC.md Non-goals with no prior entry here. It restates the AGENTS.md active-vs-frozen-repository rule as a boundary of this delivery unit specifically - Epic 20 work happens only in worship-presenter-web. Recorded so it survives the next derive at full strength.
- (decision) (bmad-spec validate, fix) Banner amended: Story 20.8 is unblocked 'at spine altitude' and BOTH remaining items are named - physical snapshot location (ARCHITECTURE-SPINE.md) and the EXPERIENCE.md Venue & Projection global-and-immediate rule per epics.md:374. Corrects an over-correction: the prior sweep (this log) fixed an over-claim that AD-14 still blocked 20.8, and swung past the residual UX-spine item.
- (decision) (bmad-spec validate, fix) ARCHITECTURE-SPINE.md adopted as a companion (adopted, not spec-authored - bmad-architecture owns it, bmad-spec does not edit it). The SongSet constraint now reads 'bounded configuration surface' and CITES AD-22 for its exact extent rather than restating it, per the Companions rule that adopted content is referenced, never duplicated into the kernel.
- (note) (bmad-spec validate) CROSS-ARTIFACT FINDING, not this skill's file to fix - route to bmad-ux. EXPERIENCE.md:153 says the Epic 20 CAP-6 per-service clone 'supersedes AD-4'. Wrong decision: AD-4 is LiveServer durable paths; the superseded clause belongs to AD-14, and AD-16 is what supersedes it. The spine's own AD-14 entry warns against exactly this confusion ('Not to be confused with AD-4 ... which is a different decision and is not affected'). EXPERIENCE.md is owned by bmad-ux and is not a companion of this SPEC, so it is reported rather than edited here.
- (decision) Gap 4 item 1 / review F-3: baseType is REMOVED from the authoring-boundaries slide-inspector step. Kind and SongSet slot identity are server-owned and set at row creation; only the label is administrator-editable (AD-19). An admin retype control would let one row be retyped into songset-ds-open while another already holds it, breaking the one-row-per-slot uniqueness the hymnal binding depends on. Supersedes .memlog.md:15 (CAP-5 as originally logged with an editable baseType), which is where the contradiction entered.
- (constraint) Gap 4 item 4: the recognized entry set is CLOSED at six keys over three kinds — general, songset-bt-open, songset-bt-close, songset-ds-open, songset-ds-close, announcement — and bare song-set names the KIND, never an entry (AD-19). CAP-2 "inserting SongSet entries" is reconciled rather than deleted: adding a SongSet row means claiming one of the four slot identities that no row currently holds; a fifth slot is a code-plus-tests change, never administrator configuration.
- (assumption) That claim-an-unclaimed-identity reading is DERIVED, not verbatim in AD-19. The spine fixes three things — the set is closed, at most one row carries each slot identity, and a deleted slot leaves an inert binding rather than an error — and this is the only reading under which CAP-2 keeps its add verb and AD-19 keeps its closed set. Flagged for owner ratification rather than presented as quoted spine text.
- (decision) Gap 4 item 2 / slide-kinds.md: a SongSet row is described by its SLOT IDENTITY, and that identity — not its label, not its position in the order — is what worship-service settings bind a hymnal number to (AD-19). Reordering rows changes the presented sequence without touching a binding, renaming a label cannot touch one either, and deleting a slot row makes its binding inert rather than an error while the entered hymnal number survives in the service data (AD-16). "One row per song slot" said none of this.
- (decision) Gap 4 item 3 / placeholder-catalog.md: the deferral "Exact key names follow implementation naming" is RETIRED. Under AD-19 the catalog is one server-side module holding both the admitted key and its resolver from the parsed rundown, enforced on every write path; the spelling is chosen ONCE in Story 20.5 and is persisted into saved layouts, so after that it is an AD-18 / AD-21 versioned-migration concern. The v1 table stays intent-level but is now labelled the coverage floor for that one-time choice, not licence to name keys per surface.
- (decision) Consequence the review did not name but the F-7 narrowing creates: the admitted-catalog table rows for SongSet expansion values (hymnNumber, songTitle, lyric slots) and Announcement image slots are REMOVED from the catalog key set. They are system-expansion internals, and an admitted key that only a General may insert but no General can meaningfully carry is exactly the "key nothing can fill" hazard AD-19 names from the other direction. Recorded so narrowing :8 does not leave a new contradiction behind it.
- (decision) Gap 4 item 5 / review F-7: placeholder-catalog.md:8 narrows from "Any ordered-registry slide may insert" to "Any GENERAL row may insert", citing AD-22 (free canvas is Generals alone) and AD-19 (a placeholder becomes an element inserted onto a General). This also closes the companions self-contradiction against its own :34, which already said SongSet and Announcement have no placeholder insert — a builder reading :8 built the affordance and a builder reading :34 built the refusal.
- (decision) Review F-24 first half: authoring-boundaries.md now states AD-22s exact SongSet extent instead of the bare word "backgrounds" — two background images, one for the title layout and one for the lyric layout that verse and refrain share, PLUS font style and font size. Also records that administrator-configured values persist as an override record keyed by row and field OUTSIDE the developer-owned layout, and that Reset restores the layout and leaves the override record untouched. A surface built from "backgrounds" alone would ship Story 20.7 with no font controls.
- (decision) Review F-24 second half: the freeze tables "Presenter / PPTX for a service always read that services snapshot" is REWORDED to route through the plan. The snapshot is buildSlidePlans input; buildSlidePlan stays the single source of order and layout and no renderer reads a snapshot directly (AD-12, AD-16). Two AD-16 facts added in the same table while it was being corrected: the clone carries the AD-22 override records, and Sync Artifact is a structural write and therefore admin-only — an operator may see staleness and request a sync, never perform one.
- (decision) Review F-25: slide-kinds.md drops "for that service" from Announcement membership. The Announcements master list stays LIVE and reaches an existing service at render time rather than being cloned into the snapshot (AD-16, CAP-7) — it is nonetheless scoped per service, so "not cloned" means this weeks flyers may still change after the structure is frozen, never that one list is shared across services.
- (note) Recorded in slide-kinds.md rather than decided here: the shipped seed data/default-registry.json holds ONE song-set row and the four slot identities need four, which is Story 20.1 seed work. And Gap 3 stays open and is EXPERIENCE.mds to answer — whether the chip on a songset-bt-open row reads [song-set] or [songset-bt-open]. The companion now says the question is open and names its owner, so a builder does not silently pick one.
- (decision) SPEC.md kernel edits from this pass, kept minimal because the contradiction lived in the companions: CAP-2s success gains the closed-set and one-row-per-slot clause so the add verb cannot be read as inventing a fifth SongSet; CAP-5s success gains that kind and slot identity are not administrator-editable, replacing the editable-baseType reading logged at .memlog.md:15; one new Constraint states the cross-boundary-key rule and cites AD-19. CAP-1..CAP-8 unchanged in count and numbering, nothing renumbered, nothing retired.
- (note) REPORTED, NOT FIXED — outside Gap 4 and outside this spec folder. Review findings F-4 and F-10 target ../spec-slide-artifact-model/SPEC.md, which THIS spec lists as an adopted companion, so Stories 20.1 and 20.3 read it: :64 still presents the missing-only seed as live (reversed by AD-17), :65 still presents templates as global across services (reversed by AD-16), and CAP-8 still requires BibleVerseContemplation to remain a TextPlaceholder (a kind AD-19 abolished). Its supersession note at :16 enumerates only two reversals, which affirmatively implies the rest still binds. That is a second bmad-spec Update on a different spec folder, not this one.
- (decision) Gap 3 close-out, named item: slide-kinds.md Badge display now RECORDS the decision instead of flagging it open. The chip names the KIND — [general], [song-set], [announcement] — never the entry key, so a songset-bt-open row shows [song-set] (EXPERIENCE.md, Row display, 2026-07-31). Owner Story 20.3. The stale 'Tracked as Gap 3 in sprint-status.yaml' pointer is dropped: that item is done and pointing a builder at a closed tracker row is worse than silence. Supersedes this log's own last note, which recorded the question as open and EXPERIENCE.md's to answer.
- (decision) Coherence step 1: EXPERIENCE.md ADOPTED as a companion (adopted, not spec-authored — bmad-ux owns it, bmad-spec does not edit it). Same class of finding this log already recorded once for ARCHITECTURE-SPINE.md: a live decision authority that downstream must read was reachable only by narrative reference. Concretely load-bearing now — the Row-display clause is the only statement of what the chip shows AND the only source of Story 20.7's read-only slot obligation, and Stories 20.3 and 20.7 build from companions:. Not adopting it would leave slide-kinds.md citing a file the contract never declares.
- (constraint) FINDING the named item did not reach, fixed in the same pass because it is a build defect not a wording taste: the 2026-07-31 decision has a SECOND half — the AD-22 bounded surface MUST state the row's slot read-only in worship vocabulary — and three standing statements forbade it. (1) CAP-5 success said 'no surface exposes the row's kind or its SongSet slot identity', which is a DISPLAY ban; AD-19 says only 'never administrator-editable'. CAP-5 therefore also contradicted its own intent, which already displays the kind as '[kind] label'. (2) authoring-boundaries.md:16 was already correct ('exposes them for editing') but said nothing about display. (3) authoring-boundaries.md 'The SongSet bounded surface, in full' ended 'Nothing else.', read as exhaustive over the surface's CONTENT. A Story 20.7 builder reading the spec alone would have refused the read-only slot statement as a widening of AD-22.
- (decision) Resolution of that finding, and why it is a REPAIR rather than a new decision: AD-19's own words bar editing only, and EXPERIENCE.md is the owner of display and decided on 2026-07-31 with the owner's Gap 3 item closing done — so no live authority is being overruled and no either/or is being picked here. The spec text simply over-stated AD-19 into a display ban. Landed: CAP-5 success narrows 'exposes' to 'exposes FOR EDITING' and names what IS shown (kind as chip, slot read-only in worship vocabulary, raw songset-* key on no human surface); authoring-boundaries.md gains a 'Not editable is not the same as not shown' paragraph in the may-not-edit section; and its 'Nothing else.' becomes 'Nothing else is CONFIGURABLE' plus the one required read-only display. CAP IDs, CAP count, Constraints and Non-goals all untouched.
- (decision) OWNER RATIFICATION 2026-08-07 of the CAP-2 add-verb assumption, and the owner sharpened it with a distinction THIS SPEC NEVER MADE: there are two levels, not one. (A) The PREDEFINED SET — the special kinds/identities the system offers: the four songset-* identities and announcement. Permanent. No administrator action removes anything from it, ever. (B) MEMBERSHIP IN THE ORDERED REGISTRY — which rows are actually in the deck. 'Delete' has only ever meant removing from (B). Deleting from (A) is not an administrator operation at all. AD-19's 'a deleted slot row leaves an inert binding' is (B) and stays exactly as written.
- (decision) SongSet membership rule, owner-ratified: all four songset-* identities are permanently available to add. Each may be in the ordered registry AT MOST ONCE — an identity already in the order cannot be added again. Each may be absent, which is a legitimate configuration and not an error. Removing one from the order is allowed and is reversible by the administrator: the identity returns to the available set and can be re-added. The derived reading logged earlier is therefore CONFIRMED, and the assumption is retired from SPEC.md rather than left flagged. What changes beyond ratification is the vocabulary: the spec said 'delete a slot row', which reads as destroying the slot; it is REMOVAL FROM THE ORDER.
- (decision) Announcement membership rule, owner-ratified and DELIBERATELY UNLIKE SongSet: announcement is a predefined special kind, permanently available, but it carries NO uniqueness rule — it may be inserted into the ordered registry MORE THAN ONCE. The owner's words: normally one, three would still be reasonable, and the system imposes no cap; a limit would be judgment, not contract. It may also be absent, and it may be removed from the order. It can never be removed from the predefined kind set. This corrects CAP-7 ('a single registry entry'), slide-kinds.md's 'One row [Announcement]' and its 'Insert one Announcement entry' — three statements that read as an enforced cap and were only ever describing the common case. Consistent with AD-19, which states at-most-one for SLOT IDENTITIES and never for announcement.
- (question) CONSEQUENCE of allowing more than one Announcement row, raised because the contract answers it one way and an operator would probably expect the other. CAP-7 and AD-16 put announcement membership in the LIVE Announcements master list with no per-row selection of any kind, so under the contract as it stands each announcement row expands to the WHOLE list — two rows means the same images shown twice in one deck, not a split. If the intent behind 'sometimes three' was to place SOME announcements early and OTHERS late, that needs per-row membership, which no capability, companion or AD currently provides and which CAP-7's live-master-list rule actively rules out. Recorded as an open question for the owner and routed to Story 20.6 (announcement expands), not decided here.
- (decision) OPEN QUESTION ANSWERED by the owner the same day it was opened (2026-08-07): several Announcement rows mean REPEATING the identical set at the start, middle and end of one service — never splitting the list across positions. So the contract as it stood was already correct and needed no mechanism, only its INTENT written down. What that buys is protection in the other direction: a builder or reviewer seeing the same images render three times would reasonably read it as a bug and add dedup, a collapse, or an operator warning. CAP-7 success and slide-kinds.md now state that repetition is the intended use and that nothing deduplicates, collapses or warns. Per-row membership stays explicitly unwanted. Open Questions 1 -> 0.
