---
topic: Wave W1 x Registry — ship FR-21: Admin delete and reorder over HTTP (UC-15), and Sync Artifact onto a service-bound snapshot (UC-16)
updated: 2026-08-19T15:37
---

- (direction by owner) Slug w1-registry, one run per (wave x Product Component); folder _bmad-output/specs/spec-w1-registry. Wave W1 is not yet in waves.yaml (waves: []) — the coordinator opens it in wdi-build Phase 1.
- (direction by owner) Mode resolved headless-express: the dispatch pre-supplied every input and asked for artifact paths back. Story Breakdown normally never runs headless; it runs here because the owner pre-chose both stories and their scope in the dispatch, so the human judgment the conversation exists to capture is already supplied.
- (direction by owner) Scope is FR-21 only, projected from .what/registry (SRS, UC-15, UC-16, BR-8..BR-11, SCN-5, domain, state machine), .how/registry (SDD, 01-artifacts contract, 00-inventory, LC-15, data-model, delete-template flow), .how/_platform/ARCHITECTURE-SPINE.md (AD-16, AD-17, AD-19, AD-22 and the AD rows the SDD inherits), and .what/_prd/offline-deck/prd.md FR-21 proof of done.
- (direction by owner) _bmad-output/prior-knowledge/_bmad-output/specs/spec-artifact-registry-authoring/ read as feeding context only. Its own CAP-1..CAP-8 series is a different numbering from requirements.yaml and MUST NOT leak into this spec; nothing was lifted from it into SPEC.md.
- (decision by agent) CAP-9 (Artifact Registry — Deck layout and order) is the one capability in scope, allocated from .control/registry/requirements.yaml where FR-20 and FR-21 both hang off it. No new CAP is minted: the registry is the only allocator.
- (decision by agent) FR-20 (UC-14 layout editing) is already as-built and is a Non-goal of this wave even though it shares CAP-9. This wave's slice of CAP-9 is FR-21 alone.
- (decision by agent) UC-15 and UC-16 detail (flows, alternates, failure rows, BR bindings) goes to companion use-case-contracts.md rather than the kernel: two multi-row flow tables outgrow a kernel-shape line (Spec Law rule 8, companion rule 1).
- (decision by agent) The AD rows that bend this wave go to companion architecture-constraints.md, quoted from the spine and the SDD Inherited Constraints table. The kernel Constraints section holds only the decision-benders that are one line each.
- (decision by agent) stack.md, conventions.md and brownfield.md are written as companions because .constitution/project/codebase-{stack,conventions,brownfield}-guide.md each records that the first wave's bmad-spec companion of that name is promoted into it before the wave closes, and W1 is the first wave. They are distilled from real code, which is what those guides require and what makes them ratifiable.
- (constraint by agent) Every FR-21 write carries the client's updated_at as a precondition and answers 409 on a stale value (AD-6). This binds delete, reorder, and Sync alike; no write path bypasses it.
- (constraint by agent) Delete is terminal. gone has no transition back to live; Reset is live-to-live on a still-live seed row and is not undelete (state-machines.md, OQ-24, OQ-15).
- (constraint by agent) artifact_templates.position stays unique and sequential 0..N-1 with no gap after a delete (data-model.md Invariants). The SQL-delete proof path may leave gaps today (OQ-31); the FR-21 verb compacts.
- (constraint by agent) The seeder does not fill the gap a delete leaves, on this or any later boot, and no read path substitutes seed content for an absent or unparsable row (AD-17). BR-9 and SCN-5 are the behaviour this protects.
- (constraint by agent) Deleting a songset-* row is allowed; the Hub hymn binding for that slot stays stored and inert, not an error (AD-19, UC-15 alternate). Deleting the last live row is allowed and yields an N=0 Deck (AD-17, UC-15 alternate).
- (constraint by agent) Sync Artifact is a Hub Service surface, Admin-only, re-checking the role from SQLite in-route with requireAdminSession even though src/proxy.ts gates that Service route for any signed-in account, and it ships with its tests/proxy-matcher.test.mjs assertion (AD-16, AD-14, AD-5). An Operator may see a stale snapshot and request a Sync; they may not perform one.
- (constraint by agent) The clone carries order, kinds, labels, layouts, placeholder bindings and the AD-22 administrator override records; kinds travel by carrying the entry keys they are derived from, and no snapshot gains a slot/songset_slot/kind column of its own (AD-16, AD-19).
- (constraint by agent) Announcement membership is not cloned: the Announcements master list stays live and reaches an existing Service at render time, and each announcement row still expands that whole live list, repeats intended (AD-16, BR-11, FR-21 proof).
- (constraint by agent) Sync replaces the snapshot destructively and may not alter the Service's entered data (AD-16, BR-10). Weekly payload survives; a field whose slot went away stays stored and inert; a new slot starts empty (UC-16 alternate).
- (constraint by agent) The snapshot table arrives through the app's startup DDL on the getDb path (AD-9) under the single monotonic data_version in settings (AD-21). It is not a re-seed (AD-17, AD-18).
- (constraint by agent) A live Registry row whose payload will not parse is omitted from the Sync and logged with id and reason, exactly as the plan read does; an unrenderable snapshot is not frozen (OQ-32, UC-16 alternate, AD-17 fail-closed).
- (constraint by agent) buildSlidePlan stays the single source of slide order and content, and no renderer reads a snapshot directly any more than it read the registry (AD-7, AD-12, AD-16). The clone and re-clone validate under AD-15 like every other write.
- (constraint by agent) Public repository: no congregation data, .env, data/local, data/uploads, rendered slides or source decks reach a tracked file, and tests/public-repo-guard.test.mjs runs before every commit.
- (note by agent) Non-goal check against as-built: src/lib/registry/store.ts exports list/get/update/reset/insertIfMissing only, and updateArtifactTemplate never touches position — so reorder is not hiding inside the shipped PUT and this wave adds verbs rather than renaming one.
- (question by agent) OQ-A — the exact HTTP verb and path for Admin delete and reorder. .how/registry/02-contracts/01-artifacts.md marks them [MISSING] and says do not invent a path. Bounded by the SDD Inherited Constraints row for AD-5: they sit under /api/admin/artifacts/**, already inside the proxy matcher. The chosen shape MUST land in 01-artifacts.md and 00-inventory.md before the wave closes.
- (question by agent) OQ-B — the exact HTTP path for Sync Artifact. AD-16 places it on a Service route and the SDD forbids inventing a Registry route, so its inventory row belongs to Hub, not to .how/registry/02-contracts/00-inventory.md. The chosen shape MUST land in the Hub contracts before the wave closes.
- (question by agent) OQ-C — whether this wave closes AD-16's pre-existing-Service exception by cloning a snapshot for every existing Service in the AD-21 transition. AD-16 names that the preferred landing, not a decision; without it, a Service created before the model renders live until its first Sync.
- (question by agent) OQ-D — whether this wave makes announcement_items.service_id enforce the scoped reading AD-16 picks. The column is nullable today, so both readings are expressible; the corpus schedules no change to it.
- (question by agent) OQ-E — whether AD-17's not-yet-closed per-row seed-origin column stays deferred. It becomes reachable only with a create verb, and create is a Non-goal here; tracked in _bmad-output/implementation-artifacts/deferred-work.md.
- (assumption by agent) Reorder is expressed as one ordered write over the whole live list rather than a per-row move: position must be contiguous 0..N-1 on the same write (data-model.md Invariants) and AD-6 gives one precondition token per mutation. Not stated in the SDD.
- (assumption by agent) The two test filenames the coordinator recorded — tests/registry-reorder-delete-http.test.mjs and tests/registry-sync-artifact.test.mjs — are added to the npm test script list in package.json, which is how this repo registers a test file. Read from package.json, not from the corpus.
- (decision by owner) Story 1-1 ships UC-15 over HTTP: delete plus reorder, position compacted, BR-8 and BR-9, SCN-5 restart proof. Create/add of a new Artifact Template stays out (SRS Non-Goal; AD-17's origin column ships with the create verb later).
- (decision by owner) Story 1-2 ships UC-16: the AD-16 service-bound snapshot table, the Admin-only Sync Artifact surface on the Hub Service route, BR-8, BR-10, BR-11, and the OQ-32 omit-and-log for a corrupt live row. It runs after 1-1.
- (decision by agent) spec_checkpoint is true on both stories and done_checkpoint is true on both: UC-15 is the only critical UC in the component and its delete is irreversible, and 1-2 puts a new table on the durable DB_PATH through startup DDL. Both are the owner's call to override in stories.yaml.
- (decision by agent) depends_on: ["1-1"] is carried on story 1-2 as a caller-only field, alongside spec_checkpoint and done_checkpoint. wdi-build Phase 1 also records wave-level and story-level depends_on in waves.yaml, which stays the registry home for it.
- (note by agent) Brownfield finding for story 1-1: ARTIFACT_ENTRY_KEYS in src/lib/registry/types.ts is still the three-key set ['general','song-set','announcement']. The four songset-* slot identities AD-19 closes the vocabulary at are Story 20.7's work and not persistable yet, so UC-15's songset-delete case can only be exercised against the single song-set key. Recorded in brownfield.md; not an invitation to widen the set here.
- (event by agent) Self-Validate pass 1 (Coherence, Spec Law 1-6 and 8): PASS. CAP-9 carries intent and success; the intent states WHAT; every Constraint rules something out; eight Non-goals; the success signal is a runnable demonstration; CAP-9 is allocated from requirements.yaml and not renumbered.
- (event by agent) Self-Validate pass 2 (Preservation): PASS after eight repairs. Eight load-bearing source claims had not landed on the first render and were added: the SRS/SDD two-surfaces rule; that UC-15 is a screen action and /admin/artifacts gains the delete and reorder controls; UC-15's failed-save flow (the old order survives); AD-11/AD-4's durable DB_PATH on the snapshot table; that no new LC is minted (LC-11, LC-15, and a Hub services gateway for Sync); the NFR-3 lyric-readability floor from SRS Risks; OQ-A extended to demand the five lanes 01-artifacts.md leaves 'not applicable until FR-21'; and new OQ-F, that no inventory-screen row lists UC-16 at all.
- (event by agent) Wrapper-only content, dropped on the record rather than silently: SRS Gate Checklist, Design Reference, Slots and Open Items index lines; SDD Slots, Evidence and Robustness table scaffolding. Deliberately not projected, with a reason: deck-frame.md geometry and macro-sections (frame context, not FR-21 behaviour); domain-model.md's Placeholder Catalog coverage floor (FR-20 / Story 20.5); AD-13 canvas wrapper and AD-8 image helpers (no canvas or image path is touched); UC-14 and UC-20 flows (FR-20 as-built, and Operator-side plan consume).
- (event by agent) stories.yaml checked against assets/stories-schema.md: two entries, ids '1-1' and '1-2' quoted strings of letters/digits/dashes, unique and prefix-free (no bare '1' exists to collide), no status field, required id/title/description present on both, execution order is list order. VERDICT PASS.
- (decision by owner) Coordinator answered the story-file question: stop at stories.yaml. The story spec files at stories/{id}-{slug}.md stay bmad-build-auto's to write in wdi-build Phase 3 Step 1, because step-01-clarify-and-route.md branches on whether one already exists and a pre-written file would misroute it into resuming a plan nobody made.
- (event by agent) Spec finalized for wave W1 x Registry. Files: SPEC.md, stack.md, conventions.md, brownfield.md, stories.yaml, .memlog.md. Six open questions (OQ-A..OQ-F) and three assumptions stand; none of them blocks Phase 3, and each names where its answer must land before the wave closes.
