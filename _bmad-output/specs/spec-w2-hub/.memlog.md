---
topic: Wave W2 x Hub — ship FR-14 on the DEC-003 topology (Go API, SPA, on-demand PPTX worker)
updated: 2026-08-19T19:45
---

- (direction by owner) Hold G3/G4 on the DEC-003 delta; proceed wdi-build automatically ("oke, kerjakan aja semua otomatis saya setuju"). Isolation: this checkout is main (same override as W1).
- (direction by owner) Slug w2-hub — one SPEC per wave, Hub PC because FR-14 / UC-18 is Hub. Folder _bmad-output/specs/spec-w2-hub.
- (decision by agent) CAP-5 from requirements.yaml (FR-14). No new FR/UC/AD in this SPEC.
- (decision by agent) Three stories: 2-1 Go gate+SQLite, 2-2 PPTX worker, 2-3 Go GET pptx with in-process Go planner.
- (constraint by agent) Worker MUST NOT open SQLite. Planner MUST run in Go, not a Node child that calls getDb/buildSlidePlan.
- (decision by agent) Stories 2-1, 2-2, 2-3 marked done. Absence-guard for worker SQLite import proved by injecting `getDb` into workers/pptx/draw.mjs (failed on that needle), then reverted. GET pptx FR-14 proven by tests/pptx-go-http.test.mjs.
- (note by agent) Wave W2 stays open: Operator chrome is still as-built Next; remaining JSON verbs are SPEC non-goals. Retrospective waits until the wave closes (size L).
