# Issue 02 — Admin maintains background library and global default background

**Status:** done

## Summary
Satisfies UC-25 (FR-31). Admin maintains an image-only background library and sets the global default background for Verse/Reff slides.

## Tests
- `tests/background-library.test.mjs`
