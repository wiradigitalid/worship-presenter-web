# Issue 01 — Admin configures the song-set list in the Artifact Registry

**Status:** done

## Summary
Satisfies UC-24 (FR-29). Admin can configure the song-set list in the Artifact Registry, managing multiple dynamic song sets rather than a fixed four.

## Tests
- `tests/registry.test.mjs`
- `tests/registry-go-http.test.mjs`
