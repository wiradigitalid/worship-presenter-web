---
topic: W3 x Registry — FR-20/FR-21 completion: the three canvas gaps, preview titles, and Sync Artifact
updated: 2026-08-21T21:35
---

- (decision) Wave W3 scoped to the Registry Product Component alone, so wdi-build's one-spec-per-wave rule and this project's w<N>-<pc> slug override both hold; the Hub half is W4.
- (capability) CAP-9 (Artifact Registry - Deck layout and order) is the only capability; FR-20 and FR-21 both hang off it in requirements.yaml. No new CAP allocated.
- (constraint) SPEC is a projection of .what/registry and .how/registry onto W3; it introduces no FR, UC, BR or AD. A gap found while building is landed by the layer's owning skill, never patched into the spec.
- (decision) Story B (canvas gaps) covers insert image, explicit layer order, bold and italic only. Underline is out: textDecoration is absent from allowedStyleKeys in internal/plan/validate_artifact.go, so it is not persistable and MUST be documented rather than faked.
- (constraint) Persistence for story B already exists - imageRef and zIndex in allowedElementKeys, fontWeight and fontStyle in allowedStyleKeys. Only the UI is missing, so no payload-shape change is in scope.
- (constraint) A seed layout opened and saved without touching a style control MUST produce no new style keys. ArtifactEditor.tsx ~197 records that Fabric dies in Cache.getFontCache when fontStyle is undefined and every shipped element omits it; writing fontStyle unconditionally would rewrite every seed payload on first save.
- (constraint) zIndex MUST stay a dense deterministic ordering: both the PPTX exporter and the web slideshow paint in zIndex order, so this control changes what the congregation sees.
- (constraint) Deleting a canvas element MUST NOT delete the uploaded file. Images are shared by reference per DEC-004 Copy/paste and tests/copy-paste-share-by-reference.test.mjs pins it.
- (decision) Story D rewrites Sync Artifact's confirm copy because it is now false: it promises 'Announcement flyers stay this Service's list' and FR-3 retired today, deleting the five /api/announcements routes.
- (constraint) Story D's preview refresh MUST NOT use router.refresh() or navigate(0); tests/no-router-refresh-guard.test.mjs enforces it and it exists because navigate(0) remounts the route and flashes the page.
- (question) npm test does not glob tests/ - it names each file explicitly, and three guards added 2026-08-21 (no-router-refresh-guard, announcements-screen-guard, operator-i18n-guard) are unregistered and therefore never run in CI. Registering them touches no FR, so it is fastpath work outside this wave, but every story's definition of green depends on it.
- (event) Owner rulings 2026-08-21: all coding goes through wdi-build; no reviewer panel - the coordinator tests and reviews OpenCode's output. Cross-family (builder OpenCode, reviewer Claude), so wdi-build Step 3 is satisfied as a coordinator review rather than dropped.
- (note) check
- (note) The preceding one-word 'check' entry was a tooling probe confirming the append script's exit shape, not a decision. Recorded rather than removed: the memlog is append-only.
- (note) Assumption checked and found FALSE: preview-model.ts documents grouping as 'Present only on members of a group (currently SongSets)', and slide-plan.ts types group.role as 'title' | 'lyric'. Announcement Sets expand flat at slide-plan.ts ~791 with no group wrapper.
- (decision) Announcement Set group chrome is OUT of W3. DEC-004 says groups MAY appear as operator chrome - permissive, not normative - and adding it means widening a group role union that the presenter also consumes. Story C keeps only the normative half: prefer the registry label or kind over 'Untitled Slide'.
- (event) Self-validate pass 1 (coherence): CAP-9 carries intent and success; intent is WHAT not HOW; every constraint rules something out; four non-goals present; success signal is demonstrable. Pass.
- (event) Self-validate pass 2 (preservation): every load-bearing claim from DEC-004 Registry-UI, Live-Preview and UC-16, plus the stack guide's npm-test-does-not-glob fact, lands in SPEC.md constraints or non-goals. Wrapper-only content dropped: none. Pass.
- (note) x
- (note) The one-character 'x' entry above was a second tooling probe, same as the earlier 'check'. Both are recorded rather than removed because this log is append-only; neither is a decision. No further probes.
- (decision) Story breakdown: three stories under CAP-9 -- 1 canvas gaps (insert image, layer order, bold/italic), 2 preview titles, 3 Sync Artifact. Ordered biggest-blocker-first per wdi-build's parallel-stories guidance; all three run serially anyway because each touches the three i18n catalogue files and a concurrent write there already destroyed a neighbour's work once today.
- (direction) Owner set spec_checkpoint false and done_checkpoint false on all three stories: the coordinator reviews each story's output and one PR lands at the end, rather than pausing for a human between planning and build or between stories.
- (event) stories.yaml checked against assets/stories-schema.md validity rules 1-4: three entries, ids unique and prefix-free, no status field, ids quoted strings of digits only, title and description present on each. Pass.
- (event) wdi-review ran the lens set for risk_accepted medium (structure, prose, edge-case-hunter) at 91fddf0. 15 findings. Reader type calibrated to llm, not humans: wdi-build states SPEC and story files are machine contracts, so persuasion and orientation count as decoration.
- (decision) Structure findings resolved: the 'second independent reviewer' non-goal is removed as a process note that belongs on the wave record (already there); Why is condensed from persuasion to the three factual gaps plus the frozen-per-service reason; the Announcement Set group chrome non-goal is PRESERVED because its file-level evidence is what stops a builder re-attempting the widening.
- (decision) Prose findings resolved: title made a plain name rather than an argument; 'the room' and 'the congregation' collapsed to one term throughout.
- (decision) Edge-case findings with behaviour already decided in code are now stated in the SPEC: a whitespace-only registry label falls through to the next title source because the resolver trims; and zIndex on insert is stated once return trip 2 lands.
- (question) Edge-case findings genuinely undecided are deferred as OQ rather than invented: an upload whose slide is never saved leaves an unreferenced file with no owner (DEC-004 defers the orphan-purge tool); emphasis on a token-bound text element; and a preview refresh that fails after a successful sync, which the implementation currently swallows.
- (decision) Last review finding resolved: the zIndex-on-insert branch is now stated as its own constraint. The edge-case lens found that isOrderModified was hasReorderedExisting || hasAddedElements, so any insert renumbered every element including the 40 non-dense seed layouts -- the same silent seed mutation return trip 1 fixed, reached by a different trigger, and AC-06 did not cover it. Fixed in code at return trip 2 with AC-07 covering all three insert controls; proved by restoring the defective condition and watching AC-07 fail while AC-06 still passed.
