---
topic: W4 x Hub — FR-6: Family/Youth name inputs and the S6 closing-prayer checkbox
updated: 2026-08-21T20:42
---

- (capability) CAP-1 (Rundown to Service) is the capability; FR-6 hangs off CAP-2 in requirements.yaml and UC-20 satisfies FR-4/5/6 -- confirm the CAP before rendering, do not allocate a new one.
- (constraint) SPEC is a projection of .what/hub and .how/hub onto W4; it introduces no FR, UC, BR or AD.
- (decision) Storage for familyName and youthName already landed in f393bfd -- WorshipFormFields, structuredKeys, both parsed-data structs, both hydrate paths. W4 adds only the inputs, so the story is UI plus request-body wiring.
- (constraint) The values MUST reach create, update AND the preview request body. Missing the preview call means the preview lies about the deck the operator is about to generate.
- (decision) S6 replaces today's silent auto-fill. CreateForm ~367 and EditForm ~442 overwrite closingPrayerPerson whenever the sermon speaker changes, which can destroy a name the operator typed. S6 asks for a deliberate checkbox instead.
- (constraint) On the edit form the checkbox's initial state MUST be inferred from stored values -- checked only when the stored closing-prayer person already equals the stored sermon speaker. The both-empty case is a judgement call the builder must state.
- (constraint) Per S1 both name fields start empty on a parsed rundown: the legacy combined familyYouth text cannot be split automatically. No backfill, no migration, no guessing a name out of the parse.
- (constraint) Every acceptance criterion needs a test that can fail. An absence criterion needs a guard that scans what it claims, proved by injecting the defect -- W3 was returned twice for exactly this class of gap.
- (event) Self-validate pass 1 (coherence) and pass 2 (preservation): CAP-2 carries intent and success; every constraint rules something out; four non-goals; success signal demonstrable; every load-bearing S1/S6 claim and the form-fields.md card order land in the SPEC. Pass.
- (event) stories.yaml checked against assets/stories-schema.md rules 1-4: one entry, id unique and prefix-free, no status field, quoted string id, title and description present. Pass.
