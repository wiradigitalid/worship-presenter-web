---
topic: BIC Worship Presentation Automation
updated: 2026-07-30T22:04
---

- (decision) Intent=Create PRD via Fast path; stakes read as internal-tool (solo dev, real weekly use, target: sticks >=1 quarter); output language English, conversation Bahasa Indonesia
- (decision) Web render scope v1 = PPTX download AND in-browser web slideshow replicating dual-screen presenter mode (projector full-screen + laptop current/next + run-sheet). Not PPTX-only.
- (decision) Revision flow v1 = BOTH web-form edit per week AND re-send corrected text to Telegram; either triggers regenerate.
- (decision) Auth v1 = per-person accounts (individual login), not single shared password.
- (decision) Fidelity v1 = rebuild clean master template (real layouts/placeholders) with close-but-not-pixel-perfect resemblance; safe font substitution acceptable (avoid commercial Cooper BT licensing/headless risk).
- (change) PRD restructured into delivery phases. Phase1(MVP)=PPTX automation: Telegram->picoclaw->API assembly, offline PPTX download, single-screen full-screen Web Slideshow (NO presenter view), hub(list/preview/run-sheet/manual delete), auth. Phase2=edit(web form)+Telegram correction+regenerate+auto retention cleanup. Phase3=dual-screen presenter view. Phase4=standalone scripture display.
- (decision) OQ1 Scripture display = standalone special feature, decoupled from PPTX workflow, lives in web app, from user-provided Verse Database. Deferred to Phase4.
- (decision) OQ2 confirmed: unknown SDAH Number does NOT block Service creation (flag-and-continue).
- (decision) OQ3 Telegram correction targeting: app exposes API to list + get-detail of worship Services queryable by text; picoclaw uses it to identify which Service a correction targets and confirms.
- (decision) OQ4 Web Slideshow: requires online for initial load, usable after loaded; data operations may show connection errors offline. PPTX remains sole hard offline guarantee.
- (decision) OQ5 Auto-cleanup = configurable retention (default ~2 months) that deletes ONLY generated PPTX + uploaded videos. Posters, participant text, worship rows persist and are manual-delete only.
- (decision) OQ6 Roles required: Admin, Operator (Multimedia Team), Events Department. Supersedes earlier 'all operators equal / no role tiers'.
- (decision) OQ7 Presenter view operator screen also shows participant list to help operator (in addition to current/next slide + run-sheet).
- (decision) OQ8 Theme verse is part of the Telegram Weekly Data Payload (variable), not the fixed Template Skeleton.
- (decision) OQ9 Concurrency web-vs-Telegram = first-save-wins (optimistic lock); the second save fails with an error.
- (change) Events Department elevated from Telegram-only non-user to an app role (with access), alongside Admin and Operator.
- (decision) Phases resequenced to 6: P1 MVP=Telegram intake+hymn-validating input API+announcement-list API+deck generator+offline PPTX download+service list+run-sheet+web-form edit+regenerate+auth(2 roles). P2=single-screen web slideshow(+slide preview FR-9). P3=Telegram correction FR-12+first-save-wins FR-13b. P4=retention auto-delete of generated PPTX only FR-10b. P5=dual-screen presenter mode FR-16. P6=scripture display FR-19. Phases 2-6 nice-to-have, contingent on P1 proving valuable.
- (decision) FR-2 reshaped: picoclaw does NOT resolve hymns; one app input API validates SDAH numbers against Hymnal Database and resolves lyrics server-side; invalid number flagged in response, non-blocking.
- (decision) FR-3 reshaped: announcements are IMAGES ONLY - MP4/video upload out of scope entirely; persistent ordered announcement list with recurring items; picoclaw instructs keep/replace/add/remove+order via API; one-off flyers are occasional (special reports), not weekly.
- (decision) FR-5 expanded: lyric slides governed by readability, not just pagination - each verse and each reff starts a new slide; overflow splits across slides; songs may have no reff; slide count = f(verses, reff availability, readability).
- (decision) FR-6: theme verse text is sender-supplied via picoclaw (translation choice matters; owned Verse DB is KJV-only and not used for deck slides); when absent, standing skeleton theme verse is used.
- (decision) Roles reduced to 2: Admin + Operator. Events Department members get Operator accounts for now; finer split deferred.
- (decision) Retention (P4) deletes ONLY generated PPTX. Worship rows, posters, participant text all persist (manual delete only). MP4 moot (out of scope). Post-cleanup regeneration uses stored assets, need not be byte-identical.
- (decision) Web slideshow caching depth = single Sabbath service (one worship, one deck).
- (decision) Telegram correction targeting: picoclaw asks for confirmation; default proposal = nearest upcoming Sabbath; explicit date supported after confirmation.
- (decision) FR-19 Scripture Display: KJV-only Verse Database; surfaced INSIDE Presenter Mode, not a separate app screen - hence P6 follows P5.
- (change) User supplied annotated 68-slide operational map (when each slide is shown, payload-changeable inventory, closing-prayer person = sermon speaker, announcements occasional/one-off, guest+speaker personal PPTX out of scope) - captured to addendum.md; PRD gains Deck Blueprint distillation in 4.2.
- (assumption) Intercessory response songs (slides 36/38, #671/#684) treated as FIXED skeleton per user slide map even though sample Rundown lists them weekly - flagged as open question.
- (assumption) FR-9 slide-level preview moved to P2; P1 Friday review = Run-Sheet + web-form data + downloaded PPTX - flagged for user confirmation.
- (decision) Reconciled newly-spawned open questions against user's prior answers: RESOLVED retention default=2mo, slides 36/38 fixed standing pair, theme/verse-reading sender-supplied text, Phase-1 review surface=runsheet+data+PPTX. Only 3 items remain, all deferred-by-choice (finer events-dept role, scripture trigger UX, retention per-service granularity). §8 reframed, §12 trimmed to 4 real assumptions.
- (assumption) [C1, load-bearing] Phase-1 Friday review has NO in-browser slide preview (FR-9 is Phase 2), so verifying the deck requires opening the PPTX in desktop PowerPoint - reintroducing the exact desktop dependency the product exists to remove, and leaving SM-4/SM-C1 (readability/no-cramped-lyrics) unobservable in the MVP. Scope challenge S1: pull a minimal render-to-image preview into Phase 1.
- (assumption) [C2, unresolved] Font strategy is contradictory: FR-14 'fonts intact offline' vs 4.2/11 'bundled or substituted'. Undecided between embed-in-file (licensing/size risk) vs install-on-presentation-laptop (per-machine setup dependency). Wrong choice reflows lyrics live on the projector. Acceptance test must be 'renders on a clean machine that never had these fonts'.
- (assumption) [H1, unspecified] Image->role binding has NO syntax in the rundown; picoclaw's mapping of uploaded images to sermon-graphic vs family/youth-photo vs 0..N ordered announcement flyers is undesigned. Source slide 56 already shows a broken-image placeholder that week. Needs an explicit binding convention + a missing/unbound-image visible failure before finalize.
- (assumption) [H2, silent failure] A valid-but-WRONG SDAH number is undetectable: FR-2 only flags INVALID numbers; the human-typed title next to the number (a free checksum) is discarded, so a transposed digit yields confident correct-looking lyrics for the wrong hymn. Scope challenge S2: fuzzy-compare typed title vs DB-resolved title, flag on mismatch.
- (assumption) [H3, n=1 risk] Parser is fit to ONE sample rundown; only hymn-invalidity fails visibly - no general 'unmapped input' surface for dropped roles/timings/compound fields/curly quotes/forward-ref 'The Speaker'. Scope challenge S3: collect 5-10 historical rundowns+decks before locking parse rules; generalize the flag beyond hymns.
- (assumption) [H4, foundational] Hymnal Database + picoclaw are unvalidated deps not yet in hand (brief memlog: 'belum di tangan'); FR-5 readability presumes clean verse/refrain structure. Make 'acquire+validate Hymnal DB (structure/coverage/numbering)' and 'confirm picoclaw customizable' explicit Phase-1 go/no-go spikes.
- (assumption) [C3+M5, bus factor] The 3-layer solo-dev system relocates the single-point-of-failure from a learnable manual task to an unlearnable distributed system, against a hard weekly Sabbath deadline with the manual skill deliberately atrophied. Needs an explicit break-glass hand-editable-template fallback + dev-unavailable runbook in 9.
- (assumption) [M1, PII] Family/Youth-of-the-Week PII (full names, real photos, prayer requests, likely minors - see source slide 56) persists indefinitely: retention (FR-10b/SM-C2) deletes ONLY the PPTX, never the payload; 9 assumes no consent/retention regime needed. Validate the church's actual expectation; consider a retention window on PII-bearing payload.
- (decision by user) [resolves C1] Phase-1 WITHOUT a web slide preview is ACCEPTED; the priority is that the workflow is no longer manual. Consequence accepted explicitly: the system must be reliable - generation correctness becomes load-bearing and Friday verification leans on the downloaded PPTX. Preview stays Phase 2.
- (decision by user) [resolves H1] Image binding decided: sender posts the images in sequence AND writes a textual description explaining them; picoclaw orders/assigns roles by interpreting that description. Residual to spec: the description must reliably distinguish sermon-graphic vs family/youth-photo vs 0..N flyers, plus a visible failure for count/description mismatch and missing images (broken placeholder seen on source slide 56).
- (decision by user) [resolves C2] Font strategy decided: PREFER embedding fonts in the generated PPTX; if headless embed is not feasible, fall back to a STANDARDIZED font that is installed on the presentation laptop(s). Pick a freely-licensed face. Acceptance: verify render on a clean machine when embedded, or document+install the standard font on the presentation machine(s) when relying on install.
- (decision by user) [waives C3/M5] Solo-dev 3-layer maintainability / bus factor is explicitly NOT a consideration for the maintainer: a stable system yields large net time savings. Risk accepted by the owner. (Cheap optional insurance, no added maintenance: keep the hand-editable master template as a break-glass fallback.)
- (decision by user) [resolves risk-area-5] Offline downloaded PPTX is accepted as a sufficient day-of guarantee for Phase 1; the only sub-risk (fonts) is handled by the font decision above.
- (decision by user) [resolves H2] No auto-checksum. Instead, on save picoclaw reports back to the sender WHAT WAS STORED - including each resolved Hymn title - so a valid-but-wrong SDAH number is caught by the human reading the readback. Folded: FR-2 (API echoes resolved titles) + FR-1 (picoclaw save-readback to sender).
- (decision by user) [resolves M1] PII kept AS-IS: access-by-Role + manual delete only, no consent/retention regime; family/youth names+photos+prayer-requests persist until manual delete. Owner accepts the risk for v1. Folded: 12 (marked decided, not open) + 9 unchanged.
- (decision by user) [resolves M3] Intercessory response songs #671/#684 stay FIXED skeleton (not editable payload) - confirmed, matches the user slide map. No PRD change; the prior open assumption is now closed.
- (decision by user) [resolves M4] Service keyed by date: a re-send of the Rundown for the same date UPDATES the existing Service's payload (no duplicate). Consequence: Phase 1 has no web-vs-Telegram concurrency guard (that is FR-13b/Phase 3), so a re-send overwrites current payload incl. prior web-form edits. Folded: FR-1 consequence + 6 Phase-1 note.
- (decision) Added FR-11b requirement: Create a Service via Web Form in Phase 1 as a second channel alongside Telegram/picoclaw
- (decision) Split weekly photo into two separate fields (familyPhotoUrl and youthPhotoUrl) in the Weekly Data Payload and slide plan
- (decision) Integrated service form flyer management directly with the live announcement_items database table (allowing reordering, deleting, and toggle recurring/one-off of master and service-specific flyers)
- (decision by recovered) FR-20 + section 4.10 (Artifact Registry & Template Authoring) added RETROSPECTIVELY by Correct Course 2026-07-29: Epic 16 had shipped a runtime-editable template system with no FR ancestry while epics.md declared 'PRD FR numbers are authoritative'. Scope boundary written into FR-20: this is one global template set for BIC, NOT per-church configurability (section 5 non-goal), and FR-4/FR-5/FR-6 obligations plus NFR-3 readability are unchanged by moving layout into data.
- (decision by recovered) NFR-1..NFR-7 given stable ids 2026-07-29 by the same Correct Course, NO wording changed, ids follow the order the bullets already had; NFR-7 (fonts) lifted into section 10 from 4.2/11 where it lived split. Cause: unnumbered prose meant no story or test could cite one — story 6.6 cited an NFR-4 that resolved to nothing.
- (override by user (recovered)) OWNER WAIVER 2026-07-29: the two Phase-1 pre-requisite spikes that require the church are dropped and will not be sought — fidelity sign-off on a sample rebuilt slide set, and the 5-10 historical Rundown corpus. Consequences accepted as carried risks, not open questions: fidelity is unvalidated by the people who will see it (compensating control = pre-launch projector inspection tracked in sprint-status.yaml), and the parser stays fit to one sample. The font-on-a-clean-machine gate is technical, NOT waived, and remains open with the maintainer.
- (override by user (recovered)) OWNER WAIVER 2026-07-29: the SM-3 build-order gate (a quarter of weekly use before Phases 2-6) is waived RETROACTIVELY — Phases 2-6 were all built and shipped as Epics 8-12 without the gate being evaluated. Rationale as given: shipping the full feature set beats holding working capability behind a 13-week window on a solo-maintainer project. NOT covered by the waiver: SM-3 stays a live product metric, counter-metrics SM-C1/C2/C3 still bind, and the five spikes are a separate matter. Practice established: future capability records its go/no-go here when decided, not reconstructed afterwards.
- (decision by user (recovered)) OWNER CORRECTION 2026-07-29 to section 9 Cost: the LiveServer production topology is a TARGET, NOT YET DEPLOYED — the deployment tooling exists and is configured, nothing is running. There is no live database, no live projector, and no Sabbath currently depending on this system. Every 'production' reference across the artifact set is to be read against that.
- (decision by user) EPIC 20 IS COMMITTED, recorded 2026-07-30 at the time the decision was taken — which is the practice section 6's phase-gate block established after the SM-3 waiver was reconstructed a day late. spec-artifact-registry-authoring was adopted whole as the final reference for development, 8 story keys are in the tracker; the FR is written as committed capability, not as specified-but-contingent. Consistent with how Phases 2-6 were treated once the SM-3 build-order gate was waived.
- (decision by user) UPDATE SCOPE = Epic 20 + Glossary/vocabulary. In: new FR for the ordered authoring capability; repair of the FIVE FR-20 statements that AD-16/AD-17/AD-19/AD-22 falsify or qualify; stable ids for the two unnumbered FR-20 feature-NFRs; Glossary entries for the registry vocabulary; explicit separation of Slide Type (semantic, per slide) from Slide Kind (authoring authority, per registry entry). OUT, deferred as open items: the UJ-4 phone conflict with EXPERIENCE.md Responsive & Platform, and the open font-on-a-clean-machine gate.
- (change) APPLIED to prd.md 2026-07-30. Section 3: eight Glossary entries added (Artifact Template, Artifact Registry, Slide Kind, SongSet Slot, Placeholder Catalog, Service Registry Snapshot, Sync Artifact) and Slide Type amended to say explicitly that it is a SEMANTIC axis and Slide Kind an AUTHORING-AUTHORITY axis — not synonyms, which section 3's own no-synonym rule required once both existed. Section 4.10: retitled for its two halves; five FR-20 consequences given inline supersession notes; FR-21 added with 10 testable consequences. Section 6: new 'Committed outside the phase plan' block recording the owner's go/no-go at the time taken. Section 8: new 'Open, and it blocks a named story' category for the NFR-3 lyric-readability question. Section 10: NFR-8 and NFR-9 added, NFR-3 rebound to FR-20 and FR-21. Section 12: blocking assumption indexed. Frontmatter: status active -> final, updated -> 2026-07-30.
- (decision) NFR-9 is a REWRITE, not a lift. The unnumbered FR-20 feature-NFR it replaces said seeding inserts missing template ids only and that a changed default is migrated by hand — behavior FR-21 deliberately removes (AD-17). Promoting it verbatim would have given a stable id to a statement the product is about to falsify. The PRD-altitude requirement underneath it is that authored structure survives restart and redeploy; the mechanism (bootstrap-once seeding, versioned data migrations) stays the architecture's.
- (assumption) OPEN AND BLOCKING: who answers for the readability of a hand-authored lyric page. FR-21 moves the three fixed liturgical songs into Registry entries, one per lyric page, so they leave FR-5's verse/Reff splitter while NFR-3 still binds them. Three non-equivalent answers named in section 8: keep those pages on the splitter, give the authoring surface its own readability check, or name the human who eyeballs them. Doing nothing picks the third without naming anyone. Not deferrable — Story 20.1 authors the seed and needs this before its AC can be written.
- (change) FINALIZE step 3, Reviewer Gate: rubric walker run sequentially in-parent (subagents unavailable this session), full review at review-rubric-2026-07-30-update-fr21.md. Verdict: the update holds; four dimensions strong, two adequate, none thin or broken. TWO HIGH findings, both self-inflicted by adding FR-21 and both FIXED in the same run. (1) NFR-8 as first drafted required every Blueprint slide to have a Registry entry, which FR-21's delete verb would violate on first use — an NFR that forbade the capability the FR grants; now split into a property true at any authored state and one true of what ships. (2) Deck Blueprint and the ordered Registry both claimed authority over deck structure — section 4.2 and the Glossary said the Blueprint was authoritative for every slide position; both now scope it to what the deck was, what the Registry ships as its starting point, and what FR-4/FR-6 are tested against, with a deliberate administrator departure permitted rather than defective.
- (change) SM-8 added to section 7 from the reviewer's medium finding: FR-20 and FR-21 were validated by NO success metric, despite being the two largest engineering investments in the document and despite section 7's own convention that each SM names the FRs it validates. SM-8 measures the claim both actually make — a layout or structure change reaches next Sabbath's deck with no code change, no release, no developer — and names SM-C1 as its counter-metric, because a structure an administrator can change is one they can break.
- (change) Two reviewer medium findings also applied: a 'Read with FR-21' line directly under FR-20's heading (the supersession warning sat above the heading, so a story run jumping to the anchor would miss it, and stories 16.1-16.5 cite FR-20), and the Sync consequence now states inline that what an Operator sees is EXPERIENCE.md's call including 'deliberately nothing', with the PRD keeping only the constraint that an Operator is never offered a control they will be refused.
- (decision by user) RESOLVES the blocking NFR-3 question, 2026-07-30: the three fixed liturgical songs are authored MANUALLY and completely when the artifact registry is edited — text by hand, slide count by hand, line breaks by hand, readability judged by the administrator's own eye. NO automation here: they do not pass FR-5's verse/Reff splitter and the canvas gets NO readability check of its own. Option C of three; the coach had recommended keeping them on the splitter (option A) and the owner chose manual authoring, which is consistent with the same posture that replaced the church fidelity sign-off with one person looking. Two consequences accepted rather than mitigated: a later correction to data/hymns.json does not reach these three songs, and a future edit to one of these pages has only the same human check behind it.
- (change) HANDOFF owed to Story 20.1, recorded here because the PRD cannot carry it: the seed story's acceptance criteria must name who performs the eye check on the three authored lyric pages, and state that it happens on the surface that will project them rather than on the canvas thumbnail.
- (change) Blocker retired across all five places that carried it, in the same change set: FR-21's ASSUMPTION tag became a stated decision, section 8's 'Open, and it blocks a named story' category was removed and the item moved into Resolved, section 12's index entry became Decided, NFR-3's parenthetical now names the administrator's eye as the mechanism, and section 6's committed-outside block stopped saying the question was unresolved. That last one is the same stale-cross-reference class the readiness report found in four other documents earlier today — a claim that was true when written and false twenty minutes later.
- (change) STRUCTURAL POLISH applied 2026-07-30, all 11 recommendations from bmad-editorial-review-structure. Section 6 SPLIT: three decision blocks moved out of the phase lists into one labelled Decision record at the end, so the question 'what are the phases?' is answered in 434 words instead of 1,447; the five pre-requisite gates now carry their state inline (two Run, one Still open, two Waived) which retired a 216-word separate block. Section 3 PARTITIONED: 12 feature-scoped nouns moved to a new section 13 Extended Glossary, cutting the pre-features wall from 1,333 to 889 words. FR-20's five inline supersession caveats COLLAPSED into one five-row table after the consequences, so the requirement reads as a requirement while stories 16.1-16.5 keep their trail. Font requirement MERGED from six statements to one canonical home (NFR-7) plus three legitimate references and two value-adding pointers. Provenance narratives CONDENSED in 4.10 and 10. Section 11 rewritten to state what each dependency's absence costs rather than restating its definition.
- (change) The structural pass caught FOUR STALE CLAIMS the content work had not: section 0 and section 6's intro both asserted 'Phase 1 is the MVP and the only committed phase', false since Phases 2-6 shipped and FR-21 was committed; section 4.2 and section 12 both said church fidelity sign-off 'is a Phase-1 pre-requisite', which the owner waived on 2026-07-29. Each falsehood lived in two places, which is why single-source-of-truth was worth enforcing rather than just tidying. Section 0 now states that delivery status lives in section 6 and nowhere else, and says why.
- (change) PROSE POLISH applied 2026-07-30, all 12 entries from bmad-editorial-review-prose. Biggest: ADMINISTRATOR COLLAPSED INTO ADMIN, 24 occurrences. The document used both for the same actor 41 times while section 3 defines only Admin and its own rule calls a synonym a discipline violation. Chose collapse over declaring the synonym, on evidence found mid-edit: Operator ALREADY doubles as both Role name and person noun ('An Operator can...' and '**Operator** can review...'), so declaring Administrator would have introduced an asymmetry the document does not otherwise have, and collapsing makes the no-synonym rule actually true instead of merely declared. The Glossary entry now states the collapse and its date. Downstream note, out of scope for this pass: spec-artifact-registry-authoring and EXPERIENCE.md both say 'administrator', so the PRD now leads them rather than matching them.
- (change) Other prose fixes: ambiguous 'which' antecedent in 4.10's two-halves sentence; three collapsed runs restored to bulleted lists (the two carried risks, and both 'Not covered:' blocks) — those three were defects the structural pass introduced an hour earlier by collapsing bullets into prose, which traded scannability for six lines and is exactly the passage type a future reader scans; FR-21's Slide Kinds bullet split so the '[kind] label' requirement stops disappearing at the end of 77 words; 'picoclaw agent' sentence fragment in section 11 repaired (also mine, from the same pass); supersession table header now parses over its column ('As FR-21 makes it'); SM-8 recast active so its subject governs one verb; section 12's 'the same day ... the same day' given a date and a referent; section 1's three lowercase Glossary terms capitalised; 'Reff' declared in the Song Block entry as BIC's own label for the refrain; 'the two presentation surfaces' named outright in section 3's pointer.
