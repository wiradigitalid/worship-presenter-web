---
topic: Web boundary untuk input & edit baris worship service — channel kedua selain Telegram/PicoClaw
goal: Brainstorm arsitektur dan UX web form untuk operator bisa create/edit worship detail langsung via web app Next.js yang sudah ada, termasuk field-field yang dibutuhkan, flow validasi, dan integrasi ke parsed_data existing
mode: autonomous
status: complete
updated: 2026-07-19T14:44
---

- (technique) started Role Playing — stakeholder perspectives on worship web input
- (idea by coach) Dedicated /admin/worship/new page with step-by-step wizard: Date > Rundown Roles > Hymns > Scripture > Sermon > Preview
- (idea by coach) Reuse existing EditForm.tsx textarea + parsed preview pattern — operator types markdown rundown, sees live parse result
- (idea by coach) Structured form approach: individual fields for each worship element (themeVerse, hymns[], sermon, specialSong, etc.) instead of free-text
- (idea by coach) Hybrid approach: structured fields PLUS raw textarea — structured fields auto-generate the raw_payload for parser compatibility
- (idea by coach) Operator picks date first, system checks for existing service on that date — merge/update or create new
- (idea by coach) Hymn picker with autocomplete from hymns table — type number or title, shows preview of first line
- (idea by coach) Scripture reference input with live resolution from bible_verses — type ref, see KJV text inline
- (idea by coach) Template system: operator loads a default worship template, then fills in the variable parts (speaker name, hymn numbers, etc.)
- (idea by coach) Copy from previous service: pick a past service as base, modify only changed fields
- (idea by coach) Role-based sections with drag-and-drop reorder: each worship element is a card that can be moved in the order
- (technique) started Morphological Analysis — independent parameters x options for worship web form
- (idea by coach) Parameter: Input Channel — options: web-form-only, web+telegram-dual, web+future-API-generic
- (idea by coach) Parameter: Data Model — options: write directly to parsed_data JSON, generate raw_payload then re-parse, new worship_drafts table as staging
- (idea by coach) Parameter: Hymn Input — options: number-only with lookup, title-search autocomplete, browse-and-pick modal, paste multiple comma-separated
- (idea by coach) Parameter: Validation — options: real-time field-level, submit-time full validation, background async validation with notification
- (idea by coach) Parameter: Auth Scope — options: any session user, operator+admin only, dedicated worship-editor role
- (idea by coach) Parameter: Edit Flow — options: inline-edit on service detail page, dedicated edit page, modal overlay on dashboard
- (idea by coach) Parameter: Announcement Attachment — options: separate step after worship save, integrated upload in same form, link to existing announcement manager
- (idea by coach) Morpho combo: web-form-only + write directly to parsed_data + number-autocomplete + real-time validation + operator role + dedicated page
- (idea by coach) Morpho combo: web+API-generic + generate raw_payload then parse + title-search + submit-time validation + any session user + inline edit
- (idea by coach) Parameter: Concurrency — options: optimistic locking via updated_at (existing pattern), pessimistic lock with edit-session, last-write-wins
- (idea by coach) Parameter: Preview — options: live PPTX-slide-like preview, parsed-data JSON preview, rundown-text preview like EditForm
- (technique) started Disney Method — Dreamer/Realist/Critic on worship web input
- (idea by coach) DREAMER: Full worship planning suite — drag-drop liturgy builder with visual timeline, hymn audio preview, scripture display preview, sermon notes area, volunteer assignment
- (idea by coach) DREAMER: AI-assisted form — type worship date, AI suggests template based on liturgical calendar and past patterns
- (idea by coach) DREAMER: One-click clone — pick last weeks service, auto-increment date, operator just swaps out changed names and hymns
- (idea by coach) DREAMER: Mobile-first input — operator fills worship details on phone while in planning meeting, syncs to web
- (idea by coach) DREAMER: Collaborative editing — multiple operators can edit same worship simultaneously with conflict resolution
- (idea by coach) REALIST: Extend existing POST /api/services to accept structured JSON body (not just webhook raw text) — add source field to distinguish web vs telegram
- (idea by coach) REALIST: New page at /services/new with WorshipForm component — date, hymn inputs (4 slots), themeVerse, verseReading, sermon speaker+title, specialSong, closingPrayer, familyYouth
- (idea by coach) REALIST: WorshipForm generates a ParsedRundown object client-side, serializes to parsed_data, and also generates a synthetic raw_payload for backward compat
- (idea by coach) REALIST: Reuse coerceStructuredFields + applyStructuredFields for the PUT path — edit form sends field patches, backend applies them to existing parsed_data
- (idea by coach) REALIST: Hymn input uses existing hymns table — debounced search by number or title, returns {number, title, firstLine} for confirmation
- (idea by coach) CRITIC: Generating synthetic raw_payload is fragile — parser changes could break roundtrip; better to write parsed_data directly and skip raw_payload for web-created services
- (idea by coach) CRITIC: Date collision — what if operator creates service for date that Telegram already created? Need upsert-or-merge logic, not just INSERT
- (idea by coach) CRITIC: optimistic concurrency (updated_at 409) already exists for PUT — must be honored in web edit form too, show operator a clear message on stale write
- (idea by coach) CRITIC: Hymn slot count is not fixed — some services have 3 hymns, some have 5 — form must handle dynamic list, not fixed 4 inputs
- (technique) started Build on What Works — amplify existing patterns from codebase
- (idea by coach) EditForm already does textarea+parse preview — extend it with a toggle: raw mode (textarea) vs structured mode (form fields)
- (idea by coach) coerceStructuredFields already handles field-by-field patches — web form can send partial updates using exact same API path PUT /api/services/[id]
- (idea by coach) Webhook POST /api/webhook already does upsert by date — web create can use same dedup logic: check existing service by date, offer to edit instead of duplicate
- (idea by coach) parseRundownText already extracts hymns, roles, scripture — web form could optionally generate rundown text and run it through same parser for consistency
- (idea by coach) AnnouncementsManager already has drag-drop image upload — can be embedded or linked from worship form as optional step
- (idea by coach) Service detail page already shows parsed preview — web create should reuse same preview component to show what the PPTX will look like
- (idea by coach) updated_at optimistic concurrency already enforced in PUT — web form must fetch current updated_at on load and send it back, handling 409 gracefully with refresh prompt
- (idea by coach) Existing /services/new does not exist yet — clean slate for route, but UI should match existing service detail page aesthetics
- (idea by coach) POST /api/services already exists and accepts raw_payload + parsed_data — web form can POST directly to same endpoint with source=web marker
- (idea by coach) BroadcastChannel sync pattern exists for presenter — could use similar pattern to show live preview in second tab while editing
- (insight by coach) KEY INSIGHT: The existing codebase already has 80% of the backend pieces — the gap is purely a frontend form that speaks the same ParsedRundown shape
- (insight by coach) KEY INSIGHT: Two modes needed — CREATE (new service from scratch) and EDIT (modify existing). Edit is already partially solved by EditForm. Create is the real gap.
- (insight by coach) KEY INSIGHT: The web channel should produce identical parsed_data shape to what Telegram/webhook produces — downstream (slide-plan, PPTX, presenter) stays untouched
- (insight by coach) KEY INSIGHT: raw_payload can be optional for web-created services — set it to a generated summary string, not pretend it came from Telegram
- (insight) SYNTHESIS: Best approach is Structured Form that produces ParsedRundown directly — no synthetic raw_payload needed
- (insight) SYNTHESIS: Two routes needed: /services/new (create) and enhance existing /services/[id] edit with structured form mode
- (insight) SYNTHESIS: Hymn input should be dynamic list with autocomplete from hymns table — not fixed slots
- (insight) SYNTHESIS: Date-based dedup check on create — warn operator if service already exists for that date, offer to edit instead
- (insight) SYNTHESIS: Backend needs minimal changes — POST /api/services already exists, just needs to accept structured body alongside raw text
- (insight) SYNTHESIS: Web-created services set raw_payload to generated summary (source: web) for traceability, but parsed_data is the authority
- (insight) SYNTHESIS: Existing coerceStructuredFields + applyStructuredFields pattern is the edit backbone — web form maps directly to it
- (insight) SYNTHESIS: Preview should reuse the same slide-plan rendering so operator sees exactly what will be projected
- (decision) CHOSEN DIRECTION: Structured form at /services/new, enhanced EditForm with field mode toggle at /services/[id], same ParsedRundown shape as webhook output
