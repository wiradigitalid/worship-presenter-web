# Assumptions

**Loaded when:** swept once per gate; MAY be skipped.

The **default** class of a question. The agent takes the answer itself and records it here, one
line: the assumption, plus the cost if it is wrong. This file **holds nothing**.

A row here MUST move to `blocking.md` as soon as it passes one of the three tests that file
states.

## Open

| id | Assumption | If wrong | Taken | By |
|---|---|---|---|---|
| OQ-47 | W2's SPEC keeps three redundancies and one stale sentence, accepted without action rather than re-derived: `CAP-5 is allocated from requirements.yaml` and the non-goal `Changing FR/NFR wording` both restate the preamble's projection rule; Assumptions duplicates the isolation note `waves.yaml` already owns; and `Today's Next.js process in src/` is stale now that Next.js is fully removed and guarded against. W2's wave shipped long ago, so its SPEC is a historical record with no downstream consumer left to mislead. | If W2 is ever reopened or its SPEC re-read as current, the stale sentence describes a runtime that no longer exists, and the duplicated isolation note can drift from the wave record. | 2026-08-21 | agent |
| OQ-43 | An upload whose slide is never saved leaves an unreferenced file, and nothing reclaims it. The insert-image control uploads first and persists the reference on save, so abandoning the edit orphans the file. DEC-004 defers the orphan-purge Admin tool, so there is no owner for it today. | Uploads accumulate on disk with no surface listing them; the eventual purge tool has more to sort through, and disk grows unmeasured. | 2026-08-21 | agent |
| OQ-44 | Bold and italic on a text element whose content is a `{key}` token behave like any other text element -- the style is element-level and the token is substituted at hydrate, so emphasis is assumed to survive. Not exercised by a test. | If hydrate rebuilds the element rather than substituting into it, emphasis is silently lost on exactly the slides that carry weekly values. | 2026-08-21 | agent |
| OQ-45 | A preview refresh that fails after a successful Sync Artifact is swallowed: the success toast fires, the refresh is best-effort, and the Operator is not told the preview is stale. | The Operator is told sync worked while looking at the pre-sync structure, and trusts it. | 2026-08-21 | agent |
| OQ-46 | The Family and Youth name fields carry no length bound, while sibling weekly fields are bounded 1-120. | A name long enough to overflow its slide reaches the projector untested; the failure is visible to the congregation rather than to the form. | 2026-08-21 | agent |
| OQ-40 | Announcement Set group chrome in the Live Preview is not wanted yet. DEC-004 says groups MAY appear as operator chrome, and today grouping is SongSet-only: `preview-model.ts` documents it as "present only on members of a group (currently SongSets)" and `slide-plan.ts` types `group.role` as `'title' | 'lyric'`, so an ann-set expands flat. Widening that union reaches the Presenter. | An Admin composing several Announcement Sets cannot tell in the preview where one set ends and the next begins, and asks for it after the fact. Cost is the widening plus a Presenter regression pass, not rework. | 2026-08-21 | agent |
| OQ-41 | Underline stays unavailable on the Registry canvas. `textDecoration` is absent from `allowedStyleKeys` in `internal/plan/validate_artifact.go`, so a control for it could not survive a save; bold and italic shipped, underline did not. | An Admin authoring a layout that needs underline has no way to express it and works around it with a shape or an image. Adding it later means a spine-level change to the allowed style keys plus both render paths. | 2026-08-21 | agent |
| OQ-42 | The Telegram webhook may keep accepting `announcements[]` and writing `announcement_items` after FR-3's retirement. `.how/hub/02-contracts/03-announcements.md` retires the five `/api/announcements` routes and says nothing about webhook ingestion, so the silence was read as "not covered" rather than "also retired". | A live write path survives into a table the corpus calls dead, so `announcement_items` keeps growing rows no surface reads and the eventual drop migration is not safe to run. | 2026-08-21 | agent |
| OQ-17 | The Operator has this week's Rundown content in time to enter it in Hub. | No Service that week. | 2026-08-19 | user |
| OQ-2 | One church, one worship flow, for this product's scope. | Scope In is not enough; that is a second product or a new PRD. | 2026-08-18 | agent |
| OQ-3 | The venue has a laptop that can play PPTX (PowerPoint or equivalent). | The offline guarantee (BG-3) is not fulfilled. | 2026-08-18 | agent |
| OQ-5 | PPTX remains the offline guarantee; the browser slideshow is best-effort after one Service is loaded. | BG-3 is read as if the slideshow must be offline. | 2026-08-18 | agent |
| OQ-8 | Same date: webhook overwrites (upsert); the Hub form does not create a second row without confirmation. | If both last-write, Hub fields vanish silently; if both hold, Telegram fails. | 2026-08-18 | agent |
| OQ-10 | An empty `WEBHOOK_SECRET` string is treated the same as unset (reject intake). | 401 vs 503 confuses OQ-4 go-live. | 2026-08-18 | agent |
| OQ-12 | "Nearest Sabbath" = the next Sabbath or today in the venue time zone, not the previous one. | A Telegram correction changes the wrong week. | 2026-08-18 | agent |
| OQ-13 | Accessibility floor is out of v1 scope; the canvas editor stays pointer-first with no keyboard equivalent. | A later NFR or owner decision needs a story the current corpus does not promise. | 2026-08-19 | agent |
| OQ-14 | A stale snapshot has no extra operator affordance beyond Admin Sync on the Run-Sheet — "nothing extra" is the chosen answer, not an omission. (AD-16 table shipped W1.) | A later story must add a badge or line; the spine already records the three constraints. | 2026-08-19 | agent |
| OQ-15 | Reset restores the shipped label (including a rename), and an authored row exposes no Reset; that as-built pair stands until Story 20.3 designs otherwise. | Two rows in one list will keep offering different verbs and surprise the administrator. | 2026-08-19 | agent |
| OQ-16 | An unmapped rundown line stays NFR-5's visible miss; no extra explanation names the colliding books when a second translation makes a former hit ambiguous. | The operator reads a typo; Stories 21.4 / 21.5 are not blocked (spine). | 2026-08-19 | agent |
| OQ-18 | As-built AD essays in the spine stay until a dedicated condensing pass; this review does not rewrite them. | Builders skip the spine; G4 still has the Rule sentences. | 2026-08-19 | agent |
| OQ-19 | Frontmatter `binds:` is the FR/NFR range; NFR-7 (fonts) stays the Deferred exception, not a decided invariant. | A gate reader treats the font licence as closed. | 2026-08-19 | agent |
| OQ-20 | Generate is not a Hub payload edit; BR-4's stale-precondition applies to field save (UC-5), not generate. | Operator save is rejected after their own regenerate. | 2026-08-19 | agent |
| OQ-21 | No readable date → no Service row (Hub and Telegram). A named date with no Service rejects (SCN-3); it does not fall back to nearest Sabbath (OQ-12). | Dateless rows, or a correction that edits the wrong week. | 2026-08-19 | agent |
| OQ-22 | Unparseable Hub body with a date still saves what was readable (NFR-5). Telegram images attach or fail visibly; they are not dropped. | Hub create silently differs from Telegram; photos never appear. | 2026-08-19 | agent |
| OQ-23 | After a rejected save: if the Service is gone, that is UC-7 not-found (do not recreate). Session expiry at save or delete rejects with no partial write. | Operator recreates a deleted Service, or the UI looks saved. | 2026-08-19 | agent |
| OQ-24 | Registry `gone` is terminal. Reset is live→live only and does not undelete. UC-15/SCN-5 "way back to seed" means a still-live seed row (OQ-15). | Admin ships undelete, or Reset on a gone id is undefined. | 2026-08-19 | agent |
| OQ-25 | Blank covers an open overlay; unblank reveals that overlay if it is still open. Reload of control or projector resends index, overlay, and blank. | Congregation loses the overlay or a reloaded window drifts. | 2026-08-19 | agent |
| OQ-26 | Plan identity travels on presenter shared-state messages (AD-10). Empty verse reference and lookup timeout fail closed (SCN-4). No projector → refuse lookup. Missing plan → return to Hub as UC-11. | Overlay hangs or has no screen. | 2026-08-20 | agent |
| OQ-27 | CAP-11 webhook mismatches (dateless insert; silent image filter) stay `[MISSING]` on Hub SDD until that phase; they are not `BUG-` rows in `defects.yaml` this wave. | G5 can look green while Telegram still writes a dateless Service. | 2026-08-19 | agent |
| OQ-28 | AD-16 is listed on Hub Inherited Constraints now that the snapshot table ships (W1). Presenter still consumes the plan, not the freeze table directly. | Create/present would be documented as live-registry if this were reversed. | 2026-08-19 | agent |
| OQ-29 | Overlay-on-sync and no-projector refuse stay planned (OQ-25 / OQ-26), not `BUG-` this wave. Control remount cannot recover overlay from the projector (AD-29). Caller-omitted `translation` and miss-as-`notFound()` stay `[PARTIAL]`. | Control reload loses overlay; a late verse or Hub-redirect story waits. | 2026-08-19 | agent |
| OQ-30 | Registry Inherited Constraints lists ADs that change Registry rows or plan input (AD-5…22 as already quoted). AD-1, AD-2, AD-4, AD-10, AD-24 stay on the spine / container, not copied here. | A later G4 review treats those five as missing quotes. | 2026-08-19 | agent |
| OQ-31 | Admin delete HTTP compacts `position` to `0..N-1` in the same transaction (W1). The old SQL-delete proof path is no longer the product verb. | A future raw-SQL delete in a test could still leave gaps; production delete does not. | 2026-08-19 | agent |
| OQ-32 | When Sync Artifact ships, a corrupt live Registry row is omitted and logged like plan read; it is not frozen into the snapshot. | An unrenderable snapshot, or Sync fails closed with no recovery. | 2026-08-19 | agent |
| OQ-33 | PUT `/api/announcements` with `items: []` is a total replace and wipes recurring rows. A confirm or 400 is a later story. | Operator loses the master flyer list with no extra prompt. | 2026-08-19 | agent |
| OQ-34 | Announcement PUT/PATCH are last-write-wins; AD-6 does not apply to this resource. | Two Operators silently overwrite flyer order or URLs. | 2026-08-19 | agent |
| OQ-35 | The production image includes a Node binary solely to exec the PPTX worker; it is not a 24/7 Node server. | PPTX download fails in Docker, or a Node HTTP server is left running “for convenience”. | 2026-08-19 | agent |
| OQ-36 | Development uses a SPA bundler plus the Go API on another origin; production Go serves the SPA same-origin. | Session cookies fail across origins, or Next.js is kept as a BFF. | 2026-08-19 | agent |
| OQ-37 | Presenter sync stays in the SPA (`BroadcastChannel`, AD-10); Go is not the sync bus. | A WebSocket story appears and AD-10 must be reopened. | 2026-08-19 | agent |
| OQ-48 | Admin CRUD of Song Books is served but not promised. `POST`/`PATCH`/`DELETE /api/admin/song-books` and the Song Books admin tab exist, and `.how/registry/SDD-registry.md` attributes them to UC-25. UC-25 promises only the background library, and FR-31 is explicit: an image-only background library and a global default background for Verse/Reff. FR-2 and FR-23 promise hymn *resolution* from a selected book, not its maintenance. Assumed until the owner rules: the endpoints stay, and the promise is the gap — either UC-25 widens to both Admin-maintained libraries, or Song Book maintenance earns its own FR/UC. | A boundary nobody promised keeps shipping. `DELETE /api/admin/song-books/[bookCode]` is the endpoint that made the AD-17 resurrection defect reachable at all, and no `FR` or `UC` was ever reviewed for whether an Admin may delete a book. | 2026-08-22 | agent |
| OQ-49 | `.how/registry/SDD-registry.md` Failure Behaviour omits the three Operator-facing registry reads — `GET /api/song-books`, `GET /api/background-library`, `GET /api/song-set-entries` — and its as-built rows delegate to "git history of this file". Assumed until the SDD is amended: their failure behaviour is the same shape as the Admin list routes, minus the 403. | `guarded` requires failure behaviour for every boundary. Three Operator-facing reads have none, and a section that points at git history cannot be checked by a reader or a validator. | 2026-08-22 | agent |
| OQ-50 | The spine's AD-4 anchor said no deployment existed as of 2026-07-30; dev first deployed 2026-08-20. AD-18's total-replacement licence and AD-21's released-version freeze both hinge on *until first deploy*, so the hinge has tripped. Assumed until the owner rules: **both licences are spent** — a value migration now travels as a numbered migration under AD-21 rather than a total replacement, which is already how every migration since 8→9 has been written. | If the licences are instead still open, someone may total-replace data on a database the congregation's dev instance is being tested against. If they are spent and nobody says so, a future wave argues the point from a stale date. | 2026-08-22 | agent |
| OQ-51 | Two coupled gaps in `wdi-build`, surfaced by W2's retrospective and not fixable in this repo because the method file is not ours to patch. (a) An owner isolation override that puts a wave on `main` with no PR **disarms Step 5**, whose CI check attaches to a pushed head SHA that then never exists; the override is recorded in the SPEC with no note that it removes the gate. (b) Nothing flags a wave left open long after its last story is `done`; V19 flags only a missing retrospective, and only as advisory. Assumed until the owner rules: an override obliges the coordinator to watch the branch's own CI runs instead, and wave closure stays a coordinator duty with no validator behind it. | W2 shipped with thirty consecutive red CI runs and all three stories green, then sat open for weeks while its conventions stayed trapped in a wave folder, the codebase structure map lost six `internal/` packages including the AD-5 authorization boundary, and the platform inventories drifted to 45 gaps. None of it registered as a failure. | 2026-08-22 | agent |
| OQ-52 | `AD-36` restates two cost items from DEC-005 in the spine, under the heading *Cost, carried from DEC-005 rather than restated in full*. `wdi-blueprint`'s own check 5 says a spine `AD-N` carries no alternatives and no cost, because those live in the `DEC-` behind it and a second copy drifts. AD-37 was written the same way on 2026-08-22 and trimmed to a bare pointer during review; AD-36 was left alone because it is not this pass's artifact. Assumed until the owner rules: AD-36's cost paragraph is trimmed to a pointer the next time that AD is edited for another reason, not in a pass of its own. | Two statements of one cost drift apart, and the spine is the copy a builder reads. | 2026-08-22 | agent |
| OQ-53 | The remote pairing code's lifetime and the stream's freshness window are one exported pair (the discipline AD-29 sets for the heartbeat), but the numbers are undecided. Assumed until the build justifies otherwise: long enough for an Operator to walk from the laptop to the back of the hall, short enough that a code left on an unwatched screen is not a standing key. | A number picked twice, once at each end, is how a healthy remote gets reported dead — AD-29 already paid for that lesson. A generous code lifetime turns a projected screen into an access token. | 2026-08-22 | agent |
| OQ-54 | A second remote claiming a live pairing is refused with 409 rather than queued or promoted. Assumed until the owner rules: one remote per presenting client. Two phones would be two controllers by another route, which AD-37 and AD-29 both forbid, so admitting a second is a new decision and not a limit to relax quietly. | A hall with two volunteers will ask for two phones. Relaxing it without a decision reintroduces exactly the split-controller failure AD-10 exists to prevent. | 2026-08-22 | agent |
| OQ-55 | Whether a pairing survives the presenting client reloading its page is undecided. Assumed until the owner rules: it does **not** — the pairing dies with the stream and the Operator pairs again. Safer, and more annoying. | A pairing that outlives the page lets an Operator reload without walking back to the laptop; one that does not means a stray refresh mid-service costs a trip to the front of the hall. | 2026-08-22 | agent |
| OQ-56 | Playwright entered devDependencies with the browser acceptance suites (2026-08-22) and **no decision is recorded anywhere** — not in `.control/decisions/`, not in the spine. The spine's Testing convention says outright: no second runner without a decision recorded in this spine. The dispatch brief told the worker to install it, make it work, and report that it needs a recorded decision rather than assume approval; the handover reported the dependency as routine. Assumed until the owner rules: the suites stay, because one of them caught a real defect the same day, and the decision is owed rather than waived. | A runner nobody decided on becomes the precedent for the next one. It also lands in CI cost and install weight without anyone having weighed them, and the spine convention exists precisely so that trade is made deliberately. | 2026-08-22 | agent |
| OQ-57 | The Live Slide Preview does not show the badge vocabulary the owner asked for. The running app renders `Song Set` for a song-set group header and `lyric` for every lyric child; the owner asked for `song-set-1`, `song-set-2` and the child's lyric role (`bait 1`, `reff`). Cause located: `src/components/SlidePreviewList.tsx:140` builds the group row with `label: entry.groupLabel || 'Song Set'` and renders that label rather than passing `groupOrdinal` through `resolvePreviewBadge`; lyric children reach `resolvePreviewBadge` with an empty `slide.title` and fall to the unlabelled-lyric fallback. Assumed: this is a defect to fix, not a design change — the owner stated the vocabulary and the corpus records it. | I declared FR-9 done on 2026-08-22 after verifying `resolvePreviewBadge` in isolation with hand-made inputs. The component that calls it was never verified, so the owner's original request has been unmet since. A browser test surfaced it in a console log while its own assertion was too loose to fail. | 2026-08-22 | agent |
| OQ-56b | Whether CI should run the browser acceptance suites at all, now that it must. Getting CI green needed `npx playwright install --with-deps chromium` before `npm test`, which adds a browser download to every run on a pipeline that only became healthy on 2026-08-22. Assumed until the owner rules: CI runs them, because a suite CI does not run is a suite nobody trusts. The alternative is a separate script and a separate job, which keeps the node suite fast but sits against the spine's rule that a new suite is registered in `scripts.test` in the same change set. | A slow pipeline gets skipped, and this one was red for weeks before anyone looked. Equally, browser tests nobody runs rot faster than no tests at all. | 2026-08-22 | agent |
