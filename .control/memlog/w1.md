---
topic: Wave W1 registry-order
artifact: .control/registry/waves.yaml
updated: 2026-08-19T15:00
---

- (decision) W1 is FR-21 (UC-15 delete/reorder, then UC-16 Sync). Size S. Release `registry-order`. SPEC folder `_bmad-output/specs/w1-registry-order/`. Create/add stays a later wave (SRS Non-Goal; AD-17 with create).
- (event by user) Owner: sweep prior-knowledge and open wdi-build so each wave deletes its archive slice.
- (event by user) Owner accepted DEC-002. Applied: addenda cite slice retirement; waves.yaml names `retires` / `retires_fastpath`.
- (event) Spec folder on disk is `_bmad-output/specs/spec-w1-registry/` (slug after `0fc36cd`).
- (event) Stories 1-1 and 1-2 shipped on `main` without Orca (owner override). Cursor Orca hook blocks tools on this Windows host.
- (event) Phase 4: contracts landed (OQ-A/B), inventories refreshed, companions distilled, retrospective skipped (size S).
- (event) DEC-002: W1 `retires` / `retires_fastpath` deleted at close.
