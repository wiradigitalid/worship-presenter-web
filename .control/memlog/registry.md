---
topic: Registry
artifact: .what/registry/SRS-registry.md
updated: 2026-08-20T20:15
---

- (event) G4 guarded as-built: UC-14..16; LC-11. Snapshot AD-16 [MISSING] in the DDL.
- (event) mode deep as-built: SCN-5; SM ArtifactTemplate; artifacts contract; LC-15; model; delete flow; ABCE UC-15. Snapshot AD-16 remains [MISSING] planned FR-21. 01-ux skipped.
- (event) wdi-review medium. Not stamped. AD-16 remains planned. Assumption OQ-9 delete/reorder store-only.
- (event by user) Owner set policy.doc_language and doc_filename_language to English and overrode the language-guide default: existing corpus migrated. Markers [MISSING] [ASSUMED] [PARTIAL]; critical column yes/no. Filename slugs English (e.g. rundown-to-service, UC-7-delete-service).
- (event) wdi-component deep as-built: no Telegram recut. Labelled [MISSING] Admin delete/reorder HTTP, Sync Artifact HTTP, AD-16 snapshot table (planned FR-21). Quoted AD-5/AD-6. OQ-9 closed: store has no delete/reorder. 01-ux still unwritten.
- (event) wdi-review medium applied: UC-14 Announcement not a free canvas; UC-15 SongSet/empty delete + Reset layout vs override (AD-22); UC-16 leftover/new slots + announcement live; FR-21 add is Non-Goal; POST reset no-seed is 404. domain-model identity; Constraints AD-14; SDD UC-16 Hub-only; mermaid=dictionary; plan omit vs GET list. Not stamped. AD-16/FR-21 HTTP still [MISSING]. OQ-9 stays closed.
- (event) wdi-review structure+prose+edge-case-hunter at 9ab0996. Reset-after-delete: gone is terminal, Reset live-only (OQ-24, with OQ-15). Trace stamped.
- (event) wdi-component deep as-built amendment (behaviour+design): UC-15/SCN-5 Outcome — Delete is terminal (gone); Reset is live→live only on a still-live seed row and does not undelete (OQ-24, OQ-15). Main Flow condensed to actor/system turns; restart/old-Service stay in SCN-5/Outcome (BR-8/BR-9). SM: gone remains terminal, no gone→live. Failure Behaviour filled for inventory 25–28 + /admin/artifacts. Contracts five lanes; DELETE/reorder remain [MISSING] planned FR-21. Snapshot table [MISSING] planned AD-16. Open Items OQ-24 · OQ-15 · OQ-14. No g4_passed, no 01-ux, no Sync Artifact route, no wdi-review.
- (event) wdi-review SDD: 403 is Forbidden not empty list; 06-flows last-row/songset/gaps/corrupt-reset; Inherited Constraints exclusion OQ-30; position gaps OQ-31. Not stamped.
- (event) Blueprint vs code: screen inventory row 7 drops UC-16. AdminArtifactsPage is UC-14/UC-15 only; Sync Artifact has no route and is a Hub surface once AD-16 ships.
- (event) G3: two-surface constraint; catalog coverage floor (no spellings); BR-11 Announcement expands whole Hub list; UC-14 SongSet canvas read-only as-built.
- (event) wdi-review: Reset/GET 500 on corrupt JSON.parse; UC-16 corrupt live row omit-and-log when Sync ships (OQ-32). Stamp pending.
- (event) Owner held G4. V13 stamped SRS+SDD at 02f8d3a. g4_passed true.
- (event) G5 W1 opened: FR-21 UC-15 then UC-16. Create/add remains later. DEC-002 applied: prior-knowledge slices retire at wave close.
- (event) DEC-003: Registry LC-11/15 on api; Admin UI on spa; AD-5/AD-9 quotes updated; AD-30 quoted.
- (decision by wdi-blueprint) G3 catalog amended for DEC-004: UC-24 (FR-29 song-set list), UC-25 (FR-31 background library) added; UC-14/15/16 alt-flows corrected (Song Set free-canvas trio, ann-set nested lists, unrecognised-token failure flow); BR-11 retired, BR-12/BR-13 added; domain-model.md rewritten with Song Set Entry / Announcement Set / Background Library / Song Book entities; deck-frame.md's four-slot section superseded by admin-configurable Song Set entries.
- (gate) G4 registry (behaviour+design) ran for DEC-004 nested registries: wrote UC-20/24/25 flow files, extended state-machines.md (SongSetEntry/AnnouncementSet/BackgroundLibraryImage), rewrote SDD-registry.md with AD-31..35 inherited, closed the physical-shape decision (own tables: song_set_layouts, announcement_sets, announcement_set_slides, background_library_images, song_books), added 4 new contract specs, rewrote data-model.md dictionary, wrote copy-paste-share-by-reference.md and predefined-field-migration.md flows. Open: reference/text split disambiguation routed to wdi-question; Hub-side stored-value migration reported, not written (out of scope fence).
- (event) Correction pass applying owner's 4 answers to G4-registry's open questions (2026-08-20, DEC-004 S13): (1) legacy `reference`/`text` migration resolved per slide identity, not a blanket default — verified against `data/default-registry.json`, exactly two shipped Generals bind these keys (`verse-reading` -> `scripture_*`, `bible-verse-contemplation` -> `theme_*`), any other slide gets `scripture_*` + `needs-review`; rewrote `06-flows/predefined-field-migration.md`. (2) `variable_name` reuse after delete confirmed allowed, no reservation/tombstone — state-machines.md, UC-24, data-model.md, `02-song-set-entries.md` contract updated. (3) Announcement Set delete-while-referenced confirmed refused-never-cascaded, made unambiguous with explicit remove-marker-first sequencing in the error surface — `03-announcement-sets.md` contract, state-machines.md, UC-15. (4) REVERSAL: shared Title/Verse/Reff trio now frozen into the per-service snapshot (`service_song_set_layouts`, keyed `(service_id, role)`) at Service creation, joining everything else AD-16 clones, instead of the earlier live-read-at-render design — SDD-registry.md, data-model.md, LC-15-store.md, `02-song-set-entries.md` contract, domain-model.md, UC-16 all updated; UC-20 already assumed frozen and needed no change. DEC-004 S13 (owner rulings) appended, S13-old "Still open: none" replaced, new S14 "Still open: none" added. Scope-fenced to `.how/registry/**`, `.what/registry/**`, DEC-004, this memlog — no touch to `.how/hub/**`, the spine, `.control/registry/*.yaml`, or `touches:`/`applied` status.
- (event) wdi-review structure+prose+edge-case-hunter post-DEC-004, both SRS-registry.md and SDD-registry.md plus all slots, at ea54cdb. Findings: (1) SRS Assumptions bullets omitted OQ-32 though Open Items and UC-16 already cited it — added the missing bullet. (2) UC-25-background-library.md Failure Flows referenced a non-existent step 4 against a 3-step Main Flow (copy-paste residue from UC-24's 4-step shape) — corrected to step 1. (3) `01-artifacts.md`'s Validation lane still described General placeholder keys as bound only to the whole-element Placeholder Catalog, not reflecting AD-32's text/image split (text is now inline `{key}` tokens checked against the Predefined Field Catalog, same rule `03-announcement-sets.md` already states for Announcement slides) — lane text updated to cover both cases. (4) `data-model.md`'s Physical notes sentence "including `song_set_layouts` directly (it is never frozen, see above)" was true only in a narrow sense and reads as reintroducing the retired live-read design — reworded for clarity, no behavior change. Frozen-trio reversal itself verified consistent everywhere else (SDD Decision Summary, AD-16/AD-33 quotes, all UC files, both `02-song-set-entries.md` and LC-15-store.md, data-model dictionary/invariants) — no stale live-read sentence found beyond finding (4)'s ambiguity. Five new tables cross-checked against the four new contracts: columns, cardinalities, and default-invariants all agree. Predefined-field migration's per-slide `reference`/`text` disambiguation verified executable against `data/default-registry.json`: `verse-reading` (line 331) and `bible-verse-contemplation` (line 804) are the only two rows binding those keys, matching the doc's citations exactly. Depth check: both documents' section-level tags match the `deep` pattern used by hub/presenter (no section over- or under-claims). All four findings fixed in-scope; none deferred. Trace stamped on both at 2026-08-20 / ea54cdb3f80610648510ed95120b3c1b1afcbd30.
- (event) wdi-review structure+prose+edge-case-hunter (medium) run over `02-contracts/05-song-books.md` only — a new slot of SDD-registry.md written after the `ea54cdb` stamp above, carrying today's OQ-38 closure (admin-created book with no corpus file) and its AD-25->AD-36 citation rewrite. Findings fixed in this slot: (1) structure — its Constraints section did two jobs (general AD-36 posture vs. the admin-created-book ruling) in one unbroken block; split with bold lead-ins, and cut a closing sentence that re-derived "no new behaviour needed" from the marker rule stated two sentences earlier. (2) edge-case — the bootstrap marker set at creation could be misread as blocking the Admin's own one-hymn-at-a-time authoring; added an explicit "Marker granularity" paragraph: the marker gates corpus-seeding only, never a per-row administrator write. (3) edge-case — DELETE checked `hymns.book_code` but not `song_set_inputs.song_book_code` (Hub-owned), so an in-use empty book could be deleted out from under a Service; applied the same refuse-never-cascade posture already ruled for Announcement Sets — Source of truth, Operations, Error behaviour, and Compatibility all updated to check both tables. (4) prose — "and no longer binds" -> "but no longer binds" (contrast, not addition); the POST row's OQ-38 parenthetical reworded to match the Constraints section's wording verbatim; a dangling relative clause and a missing "that" clarified. Deferred, filed as OQ-39 (assumptions.md, not blocking): AD-26's locale/licence/provenance requirement has no source file for an admin-created book — recorded as a working assumption (locale required, licence/provenance optional), not settled here. Deferred, reported rather than fixed (belongs to whoever owns the spine): AD-36's own text would benefit from the same explicit marker-granularity sentence just added to this contract. **Not re-stamped:** SDD-registry.md's kernel `reviewed:` trace (still `ea54cdb3f80610648510ed95120b3c1b1afcbd30`) is left as-is — this pass reviewed one slot, not the kernel plus its full slot set (which now also includes `02-song-set-entries.md`, `03-announcement-sets.md`, `04-background-library.md`, none reviewed this pass), so stamping the kernel today would repeat the exact overstatement this pass was called to fix. A full registry SDD re-review is needed before that trace can honestly move.
- (event) wdi-review structure+prose+edge-case-hunter (medium) run over the full kernel + slot set: SDD-registry.md plus `02-song-set-entries.md`, `03-announcement-sets.md`, `04-background-library.md`, `05-song-books.md` (re-check), `06-flows/copy-paste-share-by-reference.md`, `06-flows/predefined-field-migration.md`, `05-model/data-model.md`. Fixed in-scope: (1) kernel Evidence table and Open Items both still claimed the `reference`/`text` split disambiguation was `[NEEDS CONFIRMATION]` and "routed to `wdi-question`" — no such question was ever filed in `.control/questions/`, and `06-flows/predefined-field-migration.md`'s Disambiguation section already shows the owner's 2026-08-20 ruling resolving it per slide identity (verified executable against `data/default-registry.json`: `verse-reading` ~331, `bible-verse-contemplation` ~804). Both rows corrected to `resolved`, citing the flow doc. (2) `05-song-books.md`'s Error behaviour table had no row for deleting the current default book, unlike `04-background-library.md`'s explicit "DELETE the current default -> 200, no default remains" — added the matching row, covering the admin-created-empty-book case named by today's ruling. Verified clean, no fix needed: frozen-trio reversal (no surviving live-read sentence in any of the 8 files — `song_set_layouts` stays correctly described as the live table with the freeze living only in `service_song_set_layouts`); the empty-song-book ruling (no invariant/error path anywhere assumes a book has hymns; `05-song-books.md`'s POST/Constraints already state a zero-hymn admin-created row explicitly); copy-paste-share-by-reference.md's binary-sharing guarantee (explicit "never deletes an image another slide... still references", no contradiction found elsewhere); cross-slot column/cardinality/error-code agreement between `data-model.md` and all four contracts. **Unresolved, not fixed here — blocks a clean kernel stamp:** the kernel's Evidence table and AD-18's compliance row both assert a migration exists for AD-31's physical-shape retirement (today's four fixed `songset-*` slots -> `song-set-entry` rows + the extracted `song_set_layouts` trio) — "migration path is code + data, not a reseed" — but no flow document designs it anywhere in this component (unlike S1's fully-specified `predefined-field-migration.md`), it is not listed among the kernel's Slots, and the kernel's own AD-18 row is silent about it (names only S1). This is a real `mode: deep` depth shortfall on a change that unambiguously reaches rows already persisted in production installs (AD-18's own trigger condition), not a decidable-vs-undecidable question, so it was not filed as a `wdi-question` — it needs an actual design pass (new flow doc), which is beyond this review's remit. **Kernel `reviewed:` trace: removed, not re-stamped.** The stale trace this pass was called to correct (`ea54cdb3f80610648510ed95120b3c1b1afcbd30`, claiming a full pass that had actually reviewed only `05-song-books.md`) has been deleted from SDD-registry.md's frontmatter rather than replaced with a new claim of completeness — this pass did cover the full kernel + slot set and found and fixed two real defects, but the AD-31 migration gap above is an unresolved, non-cosmetic finding, and per wdi-review's own rule a trace MUST NOT be written while a finding stays open. The trace can honestly go back once that migration is designed (or the kernel's claim about it is corrected to say it is deferred to Build).
- (event) Owner ruling (live session, 2026-08-20): design the missing AD-31 physical-shape migration now, not deferred to Build — closing the gap the prior wdi-review pass left open. Wrote `06-flows/song-set-physical-shape-migration.md` at the same depth as the sibling S1 flow. Verified against real code/data before designing (not assumed): `data/default-registry.json` actually carries **five** `base_type: 'song-set'` rows, not four — the task brief's "four fixed slots" — the fifth (`id: "song-set"`, position 20) drives Hub's `plan.go:354-359` `case "song-set":` loop over `c.dsMiddle` (the old "repeat one generic template per middle song" mechanism AD-31 retires), not a fixed slot; all five rows' `title`/`lyric` layouts verified byte-identical by hash. Shared-trio derivation ("the hardest question"): rule is lowest live-spine-`position` row is the trio source (verified selects `bt-opening-song`); `title`/`verse` are migrated from that row, `reff` is seeded as a copy of `verse` (no prior `reff`-specific value ever existed); a divergence check compares every other live song-set row's `title`/`lyric` against the source and flags `needs-review` rather than silently discarding disagreement (missing layout counts as diverging too). Added the song-set-expansion-key vocabulary rename (`hymnNumber`->`song_number`, `songTitle`->`song_title`, `label`->`verse_number`, `lyrics`->`verse_content[]`/`reff[]`) into this migration, since S1 explicitly excludes these keys and nobody else's migration covered them — domain-model.md's line 25 is the source. Existing `service_registry_snapshots` rows are left untouched per AD-18's own "a migration does not rewrite service snapshots" rule — a pre-existing service keeps rendering from its old-shape embedded payload until an explicit Sync Artifact, which is where it actually gains a `service_song_set_layouts` row; this migration does not backfill one. Sequencing: this migration runs first, `data_version` 3->4, then S1 second, 4->5 — schema-shape before vocabulary, and it shrinks S1's own scan (song-set rows go to `payload=NULL` or are removed first). Corrected the sibling `predefined-field-migration.md`'s stale aside claiming the trio was "seeded fresh, not migrated" (written before this migration existed) to point here instead. Updated kernel: Evidence table row now names the verified five-row finding and this flow doc instead of asserting a migration exists with nothing behind it; AD-18 Inherited-Constraints row now names both migrations in sequence; Slots list and Robustness-Analysis flow list both add the new file.
  Ran wdi-review (structure, prose, edge-case-hunter — registry's `risk_accepted: medium`) over the kernel + the new flow together, via three parallel lens passes. Real findings surfaced and fixed in-scope, none deferred: (structure) a fully-redundant "Assumptions made" section restating the body in full — trimmed to an index with pointers; the trio-derivation/vocabulary-rename/divergence-check narrative merged into one oversized section unlike the sibling doc's separated tables — split "Vocabulary carried into the trio" into its own section; a wording ambiguity conflating the migration's own numbered steps with AD-21's `data_version` counter — reworded. (edge-case, the substantive round) the original step order derived the trio *after* nulling the source row's own payload, a real read-after-write bug — reordered so trio derivation reads an in-memory snapshot taken before any row is converted, independent of what happens to that row afterward; no stated transaction boundary, so a mid-pass crash or a trio-validation failure after rows were already converted could leave inconsistent partial state — wrapped the whole step in one transaction, with per-row validation failures as controlled skips inside it versus a trio validation failure aborting the whole transaction; zero live `song-set` rows at migration time was undefined — now an explicit refusal (rollback, no version bump, logged) rather than a guess; a custom row's derived `variable_name` could collide with one of the four reserved defaults or an existing entry outside this migration's own pass — de-dup set now seeded with both; an empty-string label slug (non-ASCII/emoji-only) was unhandled — added a `song_set_entry_<id>` fallback; a missing (not just differing) `title`/`lyric` layout on a divergence-check candidate was unspecified — now explicitly counts as diverging; S1's own handling of the newly-`NULL` payloads this migration produces was unstated — added an explicit "nothing to parse, not a parse failure" sentence; concurrent multi-instance boot was noted and correctly deferred to the already-existing AD-4 single-process rule, not re-solved here. (prose) spelling inconsistency (serialized/serialised) standardised to the corpus's British forms; a run-on missing a comma after a long inline quote; a redundant "five...five" and a redundant two-clause restatement of the same reff justification, both tightened; "squeezed" swapped for register-matching "folded". Verified clean, no fix needed: the trio-source/vocabulary tables' own internal consistency; the failure-mode table's coverage against every partition branch. **Kernel `reviewed:` trace re-stamped** at `e4499dcdaf22b8032e46bd564ce9b9eb54d99dde`, 2026-08-20, lenses `[structure, prose, edge-case-hunter]` — first honest stamp since the trace was removed by the prior pass; no finding remains open.
  Closed OQ-39 (via `wdi-question`, not reversed): owner ruling — for an admin-created song book, locale, licence and provenance are all filled in by the Admin (same posture `book_code`/`name` already had), consistent with the spine's AD-26/AD-36 update made in parallel by another agent this session. OQ-39's original required/optional split (locale required, licence/provenance optional/nullable) stood undisturbed — the ruling settles *who* supplies the fields, not their requiredness. Moved from `assumptions.md` to `answered.md`. Updated `02-contracts/05-song-books.md`: POST/PATCH bodies now carry `locale`/`licence`/`provenance`, Validation lane states locale-required/licence-provenance-optional, and a new Constraints paragraph states the admin-created-book sourcing explicitly. Did not touch `.how/_platform/ARCHITECTURE-SPINE.md` (out of this task's scope fence; already updated by the parallel agent) or any `.control/registry/*.yaml`.
