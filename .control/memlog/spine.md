---
topic: Spine Worship Presenter Web
artifact: .how/_platform/ARCHITECTURE-SPINE.md
updated: 2026-08-22T14:06
---

- (decision) Spine welded from as-built; AD-n not renumbered. Container web. Go+SPA = OQ-6, not a new AD.
- (event) wdi-init mode: global + 3 PC catalog/guarded raised to deep at the owner's request. risk_accepted stays medium. LC-12..16 registered in Control. structure maps refreshed.
- (event) wdi-review structure+prose. Finding to cut 17k words ignored: spine is as-built, not a G4 failure. Not stamped (dirty working-tree SHA).
- (event by user) Owner set policy.doc_language and doc_filename_language to English and overrode the language-guide default: existing corpus migrated. Markers [MISSING] [ASSUMED] [PARTIAL]; critical column yes/no. Filename slugs English (e.g. rundown-to-service, UC-7-delete-service).
- (event) Link repair after English slug migration: source-material paths expanded to prior-knowledge/; deferred-work citations now the archive file; Artifact reset route written in full; BMad custom config pins document_output_language English.
- (event) DEC-001 applied: prior-knowledge retired. Deferred-work, design-system, private-data, deployment, deck-frame extracted; live citations re-pointed; archive folder removed.
- (event) wdi-blueprint platform: C4 L1/L2/L3 and spine paradigm recut to Hub intake now, Telegram/picoclaw later (CAP-11). No AD added or edited. Webhook inventory row stays as-built; finding records plan-vs-code. Roll-up regenerated.
- (event by user) Owner held G3 on .control/generated/blueprint.md: Hub intake now, Telegram/picoclaw later (CAP-11). gates_passed now G1 G2 G3. V13 unstamped and G4 Hub flows still current-tense are not part of this hold.
- (event) wdi-review structure+prose: accepted Stack (seed) marker and AD-17 past-tense on the closed substitution loop. Dismissed rewriting AD-2/AD-3/AD-28 Rules — product phase is not an invariant; Design Paradigm and mermaid already recut Hub now / CAP-11 later. Trace not stamped (blockers adjudicated; working tree dirty).
- (event) wdi-blueprint platform amendment: inventory readers landed (96dfd61). Three inventories aligned to engine columns (API Host web, screen web/<Component>, States not Actor). inventory.py --write then prose restored (What it holds / Description / UC). Rows 12-13 dropped (rebuild names, numbers retired). Spine/C4/containers/platform_owns unchanged (amendment not create; bmad-architecture not re-dispatched). Roll-up regenerated. V13 still empty until wdi-review.
- (event) wdi-review structure+prose at 9ab0996. No contract BLOCKER. Editorial CONDENSE of AD essays deferred (OQ-18); binds vs NFR-7 as range-plus-exception (OQ-19). Trace stamped.
- (event) DEC-003 applied: AD-30 process split; AD-2/4/5/9/24 amended in place; C4 L2/L3 recut to api + spa + pptx-worker; inventories Host/Screen renamed without renumbering.
- (event by user) Owner held G3 and G4 over the DEC-003 delta (2026-08-19). Proceed to wdi-build W2. V13 stamp waits for the commit that contains the delta.
- (decision by wdi-blueprint) Amended ARCHITECTURE-SPINE.md for DEC-004: new AD-31 (Song Set/Announcement Set are admin-authored open lists, supersedes AD-19 in part), AD-32 (inline predefined-field tokens, supersedes AD-19 in part), AD-33 (Song Set free-canvas trio + ann-set splice, supersedes AD-22 in part), AD-34 (live background is a presenter-session override, new), AD-35 (Announcement Sets are cloned structural rows, supersedes AD-16 in part). Superseded-in-part annotations added on AD-16/AD-19/AD-22. Capability map CAP-4/5/7/8 annotated as-built-only pending the DEC-004 G4 wave. components.yaml: AnnouncementItem moved from hub.owns to registry.owns as AnnouncementSet.
- (event) wdi-review structure+prose+edge-case-hunter (risk_accepted medium) over AD-31..36 and their superseded-in-part annotations. AD-31..36 confirmed as six genuinely distinct decisions, each narrowing a different clause of a different prior AD, no duplication. Edge-case-hunter found six dangling references left by the DEC-004 layering, all fixed in place: AD-19 Binds still named the retired four-slot SongSet enumeration; AD-22's surviving placeholder-immutability clause contradicted AD-33's free-canvas grant and its Binds still named the retired SongSet configuration surface; AD-16's clone rule still required cloning AD-22 override records that AD-33 retires; CAP-3 lacked the As-built caveat its siblings carry; two Structural Seed diagram captions still called songset-* slots the Epic 20 target. Structure lens: moved AD-11/AD-14's supersession bullets to lead (matching AD-16/18/19/22/25), condensed the Reference-data convention row from ~286 to ~50 words, added an AD-36 closure pointer to AD-25's song-book gap paragraph. One item deferred to owner, not fixed: AD-25's Binds still claims the song_books registry (not just hymns) under its boot-reconcile model, while AD-36 also Binds that same registry under a bootstrap-once model -- neither AD says which governs the registry row itself (as opposed to hymns content rows); needs a wdi-question or DEC- to resolve. Trace stamped at e4499dc (working tree dirty with today's DEC-004/DEC-005 edits and this review's fixes; no commit exists yet capturing either).
- (event by owner, live session) The owner ruled on the deferred AD-25/AD-36 registry-row ambiguity: the whole song database — hymn rows and the `song_books` registry row alike — is seeded once from the committed file, then administrator-owned and correctable, never re-read from the file on a later boot. Landed as edits, not a new AD (no new decision, an existing one's Binds/Rule stated precisely): AD-25's Binds no longer names `song_books`, carved out alongside `hymns` exactly as before; its supersession bullet now says the song-book half covers content and registry together. AD-36's Binds/Prevents/Rule/Cost bullets now name the `song_books` row explicitly beside `hymns`, one bootstrap-once-then-owned rule covering both. AD-26's Supersedes gained a matching clause naming AD-36 as the reason its "registry row is derived from the corpus file" sentence dies for `song_books` alone; AD-26's Rule paragraph carries the struck clause in place (~~"a registry row is derived from the corpus file's own declared metadata"~~) with the bible half (`bible_translations`) stated as untouched. Consistency Conventions "Reference data" row updated to state the registry row explicitly on both sides. AD-27 checked for contradiction: none found, it never speaks to `song_books`. wdi-review structure+prose re-run over the changed AD-25/26/36 blocks and the Conventions row; one prose finding (an awkward "two things under one rule, not one" Binds opener in AD-36) fixed in place, no other findings. Trace re-stamped at e4499dc (working tree still uncommitted). Deferred to the owner as a component-level check, not edited: `.how/registry/02-contracts/05-song-books.md` is the likely place a song-book contract doc still describes the registry row as file-derived and should be checked against this ruling.
- (event by owner, live session) The owner closed OQ-38: an Admin-created `song_books` row with no corpus file behind it is allowed — no menu exists yet, so nothing builds now, but the capability is intended, and a future bulk JSON import is the likely companion (named, not designed). Ruled an **extension** of AD-36, not a reversal: AD-36's Rule only ever describes the corpus-bootstrap path and forecloses nothing else, so OQ-38 (which already framed itself as "not covered by that rule") was a genuine silence, not a contradiction. AD-36 amended in place with one new bullet naming what an admin-created book is (a `song_books` row created directly, its bootstrap marker set at creation, zero `hymns` rows, filled only by later authoring or the deferred bulk import) and answering the corpus-collision trap: setting the marker at creation makes a later same-`book_code` corpus file inert for that code under AD-36's own existing "never re-seeds an already-marked code" rule — no new mechanism, no special case. `.how/registry/02-contracts/05-song-books.md` updated: the OQ-38 annotation removed from the POST row, and Constraints states the settled rule and the collision behaviour. Bulk-import capability recorded once in the spine's *Deferred* ledger ("Dimensions this spine does not own yet"), no design. OQ-38 closed in place in `.control/questions/answered.md` (moved from `assumptions.md`) per `wdi-question`'s closure procedure — routed as a direct AD amendment rather than through `wdi-decision`, per this task's explicit extension-vs-reversal instruction, since the skill's own default (route an expensive-to-reverse answer to `wdi-decision`) is superseded here by a more specific instruction that already classified the case. Trace re-stamped at e4499dc, marked dirty (no commit yet captures this edit) — reviewed lenses unchanged (structure, prose); self-checked, not re-run through wdi-review, given the change's size (one AD bullet, one Deferred row, one contract section).
- (event) wdi-review structure+prose actually re-run (bmad-review) over the AD-36 OQ-38 amendment, the new Deferred bulk-import row, and the reviewed: note, after the prior stamp was found to be a self-check rather than a real lens run. No component owns the spine, so lens set is structure+prose per wdi-review's own rule for no-component artifacts (not edge-case-hunter). Structure: the new AD-36 bullet and the Deferred row are consistent with the document's existing dense-paragraph idiom and the sibling Deferred rows; no CUT/MERGE/CONDENSE found, nothing PRESERVEd against a cut that wasn't proposed. Prose: one minor clarity nit on the "same...bootstrap marker...set at creation" clause (long qualifier chain before the operative phrase) — judged readable in context against the rest of the document's register, not fixed. Content-consistency check (outside the editorial lenses' own "content is sacrosanct" stance, done directly since that was the actual ask): the new bullet does not contradict AD-36's Rule, AD-25, or AD-26 — AD-36's Rule is written entirely in terms of a corpus-file-driven bootstrap and never states an exclusivity clause forbidding another creation vector, so the admin-created case is a genuine silence and the extension-not-reversal judgement holds. Zero-hymn-rows state does not break any invariant found elsewhere (AD-28's reference matching is bible-only; no AD asserts a song_books row always has hymns) because the new bullet itself names the zero-row state as the expected starting condition. One non-blocking gap surfaced and not fixed (editorial lenses may not touch content, and .control/questions/** is out of scope for this task): AD-26 requires a registered corpus entity to carry locale, licence and provenance, and neither AD-36's Rule nor its new admin-created bullet says whether/how those fields are populated for a book with no corpus file to declare them from — likely already handled at the `05-song-books.md` contract/API level, but the spine itself is silent. Flagged for the owner rather than filed as an OQ. Trace re-stamped honestly at e4499dc (still the dirty working tree, no new commit): reviewed.note now says the lenses actually ran this time and points here for the one open gap.
- (event) Read-as-written defect closed: AD-36's OQ-38 extension said the bootstrap marker is "set at creation" for an admin-created book without ever stating the marker's *granularity*, so the sentence read as a blanket book-level gate — which would have blocked the Administrator's own per-row hymn authoring, the entire point of an admin-created empty book, and the "Save to Song Book" write (UC-28/DEC-005). `.how/registry/02-contracts/05-song-books.md` and `.how/hub/SDD-hub.md` had already been fixed with this wording by a parallel review; this task lands the matching clarification in the spine, keeping one phrasing across all three rather than inventing a fourth. Added to AD-36's `Extended` bullet: the marker gates corpus-seeding only (whether a shipped file's hymns get bulk-inserted for that `book_code`), never an administrator's own per-row write. Checked AD-17 (no-resurrection) and AD-21 (migration ordering) for the same misreading: neither binds `hymns`/`song_books` at all — AD-17's marker is the Artifact Registry's own, AD-21's counter is explicitly stated as distinct from any marker — so no edit needed there; AD-36 already says its marker is only "parallel to AD-17's," not the same marker, and nothing elsewhere in the spine treats the per-`book_code` marker as book-level.
- (event) wdi-review structure+prose actually re-run (bmad-review, two lenses dispatched as separate agents, structure first then prose fed its findings per the lens's `after: structure` dependency) over the amended AD-36 `Extended` bullet and the document at large. Structure: two CONDENSE findings, both isolated to that one bullet — the new marker-granularity sentence and the existing corpus-collision sentence each restated `05-song-books.md` (`Marker granularity` / `Collision` paragraphs) nearly verbatim, ~85-95 words apiece. Rest of AD-36 and the sampled document clean; uniform density judged house style, not local bloat. Owner overrode the lens's own fix (replace with a bare pointer) as wrong altitude: both are spine invariants, not contract-only detail, so both were condensed to one compact clause each — "the marker gates corpus-seeding only, never an administrator's per-row write" and "a corpus file arriving later for an already-marked code is inert for that code" — with the worked-example apparatus cut and a citation to `05-song-books.md`'s matching paragraph added for the fuller treatment, applied identically to both sentences for bullet consistency. Prose lens then ran on the condensed passage: one finding, a subject-verb agreement slip in a pre-existing sentence ("no special-casing is needed" → "are needed", two singular subjects joined by "and"), fixed in place; everything else in the passage, including the new condensed clauses, read as clean deliberate house style. Trace re-stamped at e4499dc (working tree still dirty, no new commit capturing today's edits): reviewed.note updated to describe this pass's actual findings and disposition.
- (event by owner, live session) The owner closed OQ-39, the non-blocking gap the previous pass surfaced and left open: for an admin-created `song_books` row, "locale, licence and provenance are simply filled in by the Admin." AD-26's Rule paragraph, its uniqueness bullet, and AD-36's `Extended` bullet amended in place — no new AD, this fills a gap the way OQ-38's amendment did, not a reversal. AD-26's Rule now states that neither of its two file-vs-directory refusal checks has anything to gate for an admin-created book (there is no file), and that such a row still carries the full five-field registered-entity shape (code, name, **locale**, licence, provenance) intact, sourced from the Admin rather than a file; points to AD-36's `Extended` bullet for the positive statement. AD-26's uniqueness bullet answers the uniqueness question directly: an admin-created `book_code` is inside AD-26's global uniqueness, never outside it (one column, one constraint, whichever writer reaches it) — what differs is the enforcement moment, since an admin write has no boot to refuse at: a duplicate code on creation is refused by the write path itself as a request-time 409 (AD-6 precondition discipline, `05-song-books.md`), while the reverse order (a corpus file later declaring a code an Admin already claimed) is not a refusal at all but AD-36's existing inert-collision resolution — cross-cited, not restated. AD-36's `Extended` bullet gained the positive closure: `book_code` and `name` were already Admin-supplied at creation; this ruling settles the remaining three — locale, licence, provenance — the same way, and states plainly that nothing about AD-26's five-field shape is relaxed for an admin-created book, only the source of three of its fields moves from file to Admin. AD-27 checked for contradiction: none, it never speaks to `song_books`. `.how/registry/02-contracts/05-song-books.md` (POST/PATCH carrying the three fields) updated by a parallel agent in the same session; not touched here. wdi-review structure+prose dispatched as two parallel agents over the whole spine, priority on the three amended passages. Structure: two findings — (1) the uniqueness bullet's cross-collision sentence had restated AD-36's marker-inert mechanism in full, reintroducing the near-verbatim-restatement pattern condensed out of AD-36 in the prior pass; fixed by cutting AD-26's copy to a bare cross-citation, AD-36 keeps sole ownership of the mechanism; (2) a miscount — AD-26 said an admin-created book closes "a fifth-field hole" where AD-36 settles three fields (locale, licence, provenance), not one, `book_code`/name already being Admin-supplied; fixed to read three fields consistently across both ADs. Prose: two clarity fixes, both applied — a missing comma in "one column with one constraint whichever writer reaches it" (misparsed as constraint being the object of "reaches"), and the ambiguous idiom "carries this rule's full shape in exactly one piece" replaced with "intact." Re-verified after fixes: clean. Trace re-stamped at e4499dc (working tree still dirty, no new commit capturing today's edits): reviewed.note describes this pass's actual findings and disposition; OQ-39 fully closed with nothing left open in the spine.
- (event) wdi-blueprint platform: three inventories refreshed from code, 45 plan-versus-code gaps to 0. api +31 DEC-004/DEC-005 rows, 5 /api/announcements rows retired; db +8 tables; screen /announcements retired.
- (event) wdi-blueprint correction: AD-4's deploy anchor said no deployment existed as of 2026-07-30; dev first deployed 2026-08-20. The spine calls that date load-bearing for AD-18 and AD-21's until-first-deploy hinge, so the hinge has tripped and the consequence is routed to the owner as OQ-50 rather than inferred. AD-6's not-yet-closed proof cited two announcement routes deleted on 2026-08-21.
- (decision) DEC-006 accepted: a server realtime channel is admitted for the remote-to-laptop leg only, superseding AD-10's no-server-realtime clause in part. AD-10's Prevents and AD-29's one-sender rule survive intact because the phone is an input to the single controller, not a second controller. Bounded by the invariant that the laptop-to-projector path acquires no connectivity dependency. Transport left to G3.
