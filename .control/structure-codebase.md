---
type: structure
scope: codebase
verified: 2026-08-18
commit: 8701fb8
---

# Codebase Structure

Written and refreshed only by `wdi-init` intent `structure`, never by hand. Rules live in
`.constitution/structure-guide.md`. Naming: `codebase/conventions-guide.md`. Versions:
`codebase/stack-guide.md`.

## Verified

2026-08-18, commit `8701fb8`, tree read from disk honouring `.gitignore`.

## Top level

```text
worship-presenter-web/
├── src/                      # Next.js App Router + lib — not a registered container yet
├── public/                   # static assets (slide backgrounds)
├── data/                     # committed seed corpora; not the live DB
├── scripts/                  # [tooling] human/CI scripts
├── tests/                    # [tooling] node:test suite
├── .github/                  # CI
├── .agents/ · .claude/       # installed skills (BMad + WDI)
├── .constitution/            # [docs] method kit
├── .control/                 # [docs] living control plane
├── .what/ · .how/            # [docs] corpus — empty
├── _bmad/                    # BMad installer
├── _bmad-output/             # [docs] pre-method artifacts
├── docs/                     # leftover inventory
├── .cursor/                  # editor rules
├── Dockerfile                # ★ prod image (standalone Next)
├── docker-compose.yml
├── package.json              # ★ version authority
└── AGENTS.md                 # ★ session entry
```

No `built: true` container is registered in `components.yaml`, so this map has no Container heading.
`src/` is the running unit today; it stays a top-level line until G2 births a Product Component and
`wdi-blueprint` registers the container.

## Containers

None registered.

## Libraries

None. `src/lib/` is application code inside `src/`, not a separately consumed artifact.

## Tooling

```text
scripts/                      # setup, seed, registry doctor, corpus verify, docker helpers
tests/                        # ★ package.json `test` script lists every file
tests/public-repo-guard.test.mjs  # ★ absence-guard for the public repo
```

**Flow (today, as-built):** `src/proxy.ts` → `src/app/**` (pages + `api/`) → `src/lib/**` → SQLite at
`DB_PATH`. PPTX is `src/lib/pptx.ts` inside the same process.

## Generated

| Path | Generated by |
| --- | --- |
| `.next/` (gitignored) | `next build` |
| `.control/generated/` | `validate.py` / `timeline.py` — empty |

## Unclaimed

- `docs/` — pre-method operator/dev notes. Sort after G3, do not treat as corpus.
- `.cursor/` — editor config tracked beside `.agents/`; purpose is local tooling, not product.

---

★ = key file: entry point, wiring root, the single place a rule is enforced, or a file that must be
opened before behaviour in its folder can be changed.
